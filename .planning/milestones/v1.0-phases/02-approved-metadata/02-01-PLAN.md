---
phase: 02-approved-metadata
plan: 01
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - scripts/generate-faq-metadata.js
  - src/faq/generated-en-metadata.json
  - src/faq/index.ts
  - scripts/verify-faq-metadata.js
  - package.json
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
estimate:
  tokens: 64000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The committed metadata artifact contains exactly 1,195 unique workbook rows joined to current English FAQ content IDs through the Phase 1 online-URL evidence."
    - "A maintainer can regenerate metadata with a dependency-free Node --write command and validate the committed snapshot with --check without the external workbook."
    - "Every mapped FAQ renders the approved title base with exactly one literal ` - FastGPT` suffix and a total title length at or below 60 characters."
    - "Every mapped FAQ renders the approved description under the established 160-character policy and preserves the approved comma-separated keyword string and ordering."
    - "The metadata overlay changes only Title, Description, and Keywords for mapped records; authored Question, Answers, and Category values remain source-owned and byte-for-byte stable."
    - "The 205 English records outside the approved workbook continue to render through the existing legacy metadata fallback and the full 1,400-record catalog remains available."
    - "The static export contains every mapped canonical FAQ route, and exported HTML metadata matches the committed approved artifact by content identity."
    - "The existing FAQ detail shell, H1, answer, category, and keyword presentation retain their current visual contract; Phase 2 adds no new interaction or visual redesign."
    - "Duplicate, unmapped, malformed, missing, incomplete, or content-drifting inputs fail with record-level diagnostics before replacing the committed artifact."
  artifacts:
    - path: "scripts/generate-faq-metadata.js"
      provides: "Dependency-free Week04 workbook parser, contentId join, content-integrity snapshot, deterministic --write/--check generator"
    - path: "src/faq/generated-en-metadata.json"
      provides: "Committed 1,195-record approved metadata snapshot keyed by durable contentId"
    - path: "src/faq/index.ts"
      provides: "English metadata overlay with workbook precedence and legacy fallback for unmapped records"
    - path: "scripts/verify-faq-metadata.js"
      provides: "Source and optional exported-HTML verifier for META-01/02/03"
    - path: "package.json"
      provides: "Runnable verify:faq-metadata command"
  key_links:
    - from: "scripts/generate-faq-metadata.js"
      to: "src/faq/generated-en-route-registry.json"
      via: "online URL slug resolves to exactly one Phase 1 contentId before metadata is emitted"
    - from: "src/faq/index.ts"
      to: "src/faq/generated-en-metadata.json"
      via: "approved metadata map keyed by contentId merges after legacy metadata and before canonical route export"
    - from: "src/app/[lang]/faq/[id]/page.tsx"
      to: "src/faq/index.ts"
      via: "existing getFaqItem and normalizeFaqMetadata path renders approved metadata without changing authored fields"
    - from: "scripts/verify-faq-metadata.js"
      to: "src/faq/generated-en-metadata.json"
      via: "record-level source and export comparisons use one committed approved snapshot"
  prohibitions:
    - statement: "Do not join workbook rows by question text, title text, spreadsheet number, or regenerated slug when the Phase 1 URL evidence is available."
      status: required
      verification: source-diff-review
    - statement: "Do not rewrite authored Question, Answers, or Category values, retire the existing legacy category overlay, or alter public canonical slug allocation in this phase."
      status: required
      verification: source-diff-review
    - statement: "Do not add a runtime workbook read, network dependency, package, redirect projection, canonical/hreflang/sitemap rewrite, or SEO graph migration."
      status: required
      verification: plan-scope-review
---

<objective>
Import the approved Week04 metadata for exactly 1,195 English FAQ records and render it through the existing static FAQ catalog while preserving authored content and the Phase 1 canonical route identity.

Purpose: Satisfy META-01, META-02, and META-03 with a reproducible build-time snapshot, explicit fail-closed mapping, and export evidence that ties every approved field to one durable contentId.
Output: A deterministic metadata generator/snapshot, a contentId-keyed catalog overlay, and a focused source/export verifier runnable without adding dependencies.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-approved-metadata/02-CONTEXT.md
@.planning/phases/02-approved-metadata/02-RESEARCH.md
@.planning/phases/02-approved-metadata/02-UI-SPEC.md
@.planning/phases/01-canonical-faq-routes/01-CONTEXT.md
@.planning/phases/01-canonical-faq-routes/01-01-SUMMARY.md
@src/faq/generated-en-route-registry.json
@src/faq/english-route-evidence.json
@src/faq/en.ts
@src/faq/index.ts
@src/faq/legacyMeta.ts
@src/faq/legacyCategories.ts
@src/lib/faqMetadata.ts
@src/lib/faqMetadata.constants.json
@src/app/[lang]/faq/[id]/page.tsx
@scripts/generate-faq-route-registry.js
@scripts/verify-p2.js

The reviewed source workbook is /Users/longnv/bin/repo/fastgpt-data/Week04/存量修复-补Meta第2批/FastGPT-存量FAQ补Meta-第2批1195条-V1.0-星触达-20260811.xlsx; its FAQ Data online URL column is the authoritative mapping input. No runtime or network access to that external path may remain after generation.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Generate and consume the approved metadata snapshot</name>
  <files>scripts/generate-faq-metadata.js, src/faq/generated-en-metadata.json, src/faq/index.ts</files>
  <read_first>
    <file>.planning/phases/02-approved-metadata/02-CONTEXT.md</file>
    <file>.planning/phases/02-approved-metadata/02-RESEARCH.md</file>
    <file>.planning/phases/02-approved-metadata/02-UI-SPEC.md</file>
    <file>.planning/phases/01-canonical-faq-routes/01-01-SUMMARY.md</file>
    <file>src/faq/generated-en-route-registry.json</file>
    <file>src/faq/english-route-evidence.json</file>
    <file>src/faq/en.ts</file>
    <file>src/faq/index.ts</file>
    <file>src/faq/legacyMeta.ts</file>
    <file>src/faq/legacyCategories.ts</file>
    <file>src/lib/faqMetadata.ts</file>
    <file>scripts/generate-faq-route-registry.js</file>
    <file>/Users/longnv/bin/repo/fastgpt-data/Week04/README.md</file>
    <file>/Users/longnv/bin/repo/fastgpt-data/Week04/存量修复-补Meta第2批/FastGPT-存量FAQ补Meta-第2批1195条-V1.0-星触达-20260811.xlsx</file>
  </read_first>
  <action>Implement scripts/generate-faq-metadata.js with the existing dependency-free unzip/XML reader and TypeScript AST source-reader patterns. Support --write --workbook &lt;path&gt; (with a documented environment/default path seam) and --check using only the committed snapshot. Read the FAQ Data sheet, validate the expected headers, require fastgpt.io/faq/&lt;slug&gt; online URLs, decode the path segment once, and resolve it through the Phase 1 registry/evidence to exactly one durable contentId per row (D-01, D-02, D-08). Require exactly 1,195 data rows, unique source slugs/content IDs, nonempty title/description/keywords, title base length 32–50, and title rendering through a Node policy-equivalent helper that mirrors the shared faqMetadata constants/normalizeFaqMetadata behavior at one ` - FastGPT` suffix and at most 60 characters; do not require the TypeScript module from Node. Retain the raw approved description and keyword strings for the artifact and enforce the shared 160-character description ceiling (D-03, D-04). Snapshot the exact authored Question, Answers, and raw Category values from src/faq/en.ts as SHA-256 digests per contentId; preserve workbook row numbers and source slugs as provenance. Emit stable JSON sorted by contentId with a version, source sheet/row count, and records carrying contentId, sourceSlug, workbookRow, raw title, description, keywords, and authored-field digests. Validate every condition before writing through a temporary file/rename, and make --check compare the expected serialized bytes so reruns are idempotent (D-07). Import the generated JSON into src/faq/index.ts and merge approved Title, Description, and Keywords by contentId after the existing legacy metadata layer and before the existing legacy category overlay. Keep raw authored Question, Answers, and Category source ownership and the canonical slug registry unchanged; retain legacy metadata fallback for records outside the 1,195 mapped IDs (D-05, D-06). Keep the JSON artifact independent of the external workbook path and add no package dependency.</action>
  <verify>
    <automated>node scripts/generate-faq-metadata.js --write --workbook '/Users/longnv/bin/repo/fastgpt-data/Week04/存量修复-补Meta第2批/FastGPT-存量FAQ补Meta-第2批1195条-V1.0-星触达-20260811.xlsx' &amp;&amp; node scripts/generate-faq-metadata.js --check &amp;&amp; npx tsc --noEmit &amp;&amp; git diff --check -- scripts/generate-faq-metadata.js src/faq/generated-en-metadata.json src/faq/index.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The generator reports 1,195 mapped rows, rejects duplicate/unknown/malformed/incomplete inputs before writing, and serializes stable contentId-sorted JSON.</criterion>
    <criterion>Every artifact record resolves from the workbook online URL through the Phase 1 registry/evidence and carries its raw approved title, description, keywords, row provenance, and authored-field digests.</criterion>
    <criterion>Each mapped FaqItem receives only the approved metadata fields; existing legacy metadata remains the fallback for the 205 out-of-batch records and the canonical route registry remains the source of slug identity.</criterion>
    <criterion>The generator and verifier use a policy-equivalent Node helper plus the existing runtime normalizeFaqMetadata contract to yield the approved title base with one exact suffix and no more than 60 characters; approved description/keyword source values remain available for exact verification.</criterion>
    <criterion>node scripts/generate-faq-metadata.js --check passes without opening the external workbook, proving the committed snapshot is build-time self-contained.</criterion>
  </acceptance_criteria>
  <done>The committed metadata snapshot and contentId-keyed overlay form one build-time path from the reviewed workbook to the existing FAQ detail metadata while preserving authored content and route identity.</done>
</task>

<task type="auto">
  <name>Task 2: Verify approved metadata and static export fidelity</name>
  <files>scripts/verify-faq-metadata.js, package.json</files>
  <read_first>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/phases/02-approved-metadata/02-CONTEXT.md</file>
    <file>.planning/phases/02-approved-metadata/02-RESEARCH.md</file>
    <file>.planning/phases/02-approved-metadata/02-UI-SPEC.md</file>
    <file>src/faq/generated-en-metadata.json</file>
    <file>src/faq/generated-en-route-registry.json</file>
    <file>src/faq/en.ts</file>
    <file>src/faq/index.ts</file>
    <file>src/lib/faqMetadata.ts</file>
    <file>src/lib/faqMetadata.constants.json</file>
    <file>src/app/[lang]/faq/[id]/page.tsx</file>
    <file>scripts/verify-p2.js</file>
    <file>scripts/verify-faq-routes.js</file>
    <file>package.json</file>
  </read_first>
  <action>Create scripts/verify-faq-metadata.js as an assert-based verifier with a source mode and an optional --html mode. Source mode must parse the same English AST fields as the generator, assert exactly 1,195 artifact records and one-to-one contentId/sourceSlug mapping, recompute authored Question/Answers/raw Category digests, compare approved raw title/description/keywords, validate one suffix plus title/description limits through the shared normalizer, assert full 1,400-record catalog coverage with legacy fallback outside the artifact, and report the affected row/contentId on failure. HTML mode must map each artifact contentId through the canonical route registry, resolve each exported FAQ route using the existing `.html` or `&lt;route&gt;/index.html` candidate pattern, and compare the serialized &lt;title&gt;, description, and keywords metadata against the approved source/normalized expectation while retaining the existing FAQ shell and route identity checks. Include explicit checks for zero duplicate suffixes, unchanged H1/FAQ JSON-LD question identity, and no missing mapped HTML files. Add a verify:faq-metadata package script that runs source mode by default; document npm run verify:faq-metadata -- --html for post-build export evidence. Exercise deterministic failure behavior with a temporary/in-memory mutation path for duplicate/unmapped/content-drift records so the verifier demonstrates record-level diagnostics without committing fixture data (D-02, D-03, D-05, D-07). Keep metadata, route, and static-export checks in this focused command; leave canonical/hreflang/sitemap/redirect aggregation to later phases (D-06, D-08). Do not modify the existing FAQ visual shell or add a UI component; satisfy the metadata-focused UI contract in 02-UI-SPEC.md.</action>
  <verify>
    <automated>node scripts/generate-faq-metadata.js --check &amp;&amp; npm run verify:faq-metadata &amp;&amp; npm run verify:faq-routes &amp;&amp; npx tsc --noEmit &amp;&amp; git diff --check -- scripts/verify-faq-metadata.js package.json</automated>
  </verify>
  <acceptance_criteria>
    <criterion>npm run verify:faq-metadata exits 0 on the committed source snapshot and prints one concise success message with the 1,195 mapped count and 1,400 catalog coverage.</criterion>
    <criterion>Source verification fails nonzero with row/contentId diagnostics for duplicate or unmapped rows, missing required values, title/suffix/length drift, authored-field digest drift, invalid artifact cardinality, or missing fallback records.</criterion>
    <criterion>After NEXT_PUBLIC_SITE_VARIANT=io NEXT_PUBLIC_HOME_URL=https://fastgpt.io npm run build, npm run verify:faq-metadata -- --html checks every mapped canonical English FAQ export for exact approved title/description/keywords and intended H1/FAQ JSON-LD identity.</criterion>
    <criterion>The existing npm run verify:p2 and npm run verify:faq-routes checks remain green, while no canonical slug, redirect, sitemap, alternate, or authored Question/Answers/Category behavior changes in this task.</criterion>
    <criterion>All six dimensions in 02-UI-SPEC.md remain satisfied: approved metadata copy, inherited visuals/typography/spacing/colors, and registry-safe build-time wiring.</criterion>
  </acceptance_criteria>
  <done>A maintainer has one focused command for exact approved metadata/source protection and one post-build flag that proves every mapped static page renders the approved fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewed XLSX/XML cells → generator | Workbook values and URL paths become committed metadata strings and identity joins. |
| Generated JSON → TypeScript catalog | A committed artifact controls metadata applied to static FAQ routes. |
| Encoded route slug → FAQ lookup | Request/build route parameters select canonical records through the Phase 1 registry. |
| Metadata values → Next.js HTML head | Approved strings are serialized by framework metadata APIs into static HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | scripts/generate-faq-metadata.js | high | mitigate | Validate workbook headers, host/path, exact row count, duplicate sets, field limits, content digests, deterministic ordering, and atomic write before replacing JSON. |
| T-02-02 | Spoofing | Workbook URL-to-content join | high | mitigate | Resolve only through Phase 1 sourceSlug/contentId evidence; reject question/title/row-number guesses and unknown slugs. |
| T-02-03 | Tampering | src/faq/index.ts overlay | high | mitigate | Merge approved metadata by durable contentId, preserve authored fields and legacy fallback, and run source digest checks. |
| T-02-04 | Tampering | Static export metadata | high | mitigate | Post-build HTML mode compares all 1,195 mapped routes with the committed snapshot and canonical registry. |
| T-02-05 | Injection | Workbook strings → HTML head | medium | mitigate | Pass values through Next.js metadata APIs; never interpolate workbook cells into raw HTML or shell syntax. |
| T-02-06 | Supply chain | npm/package surface | high | mitigate | Reuse current Node/TypeScript dependencies and add no package or runtime fetch. |
| T-02-07 | Information disclosure | Generator/verifier diagnostics | low | accept | Diagnostics include row/contentId/field and source path context only; no credentials or workbook secrets are emitted. |
</threat_model>

## Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Reproducibly apply approved Week04 metadata while authored content stays intact | 02-01 | COVERED | Tasks 1–2 provide generator, overlay, source checks, and static HTML checks. |
| REQ | META-01 | Exact 1,195 unique rows with duplicate/unmapped failures and regeneration | 02-01 | COVERED | Task 1 generator and Task 2 verifier. |
| REQ | META-02 | Approved title/description/keywords rendered for all mapped pages | 02-01 | COVERED | Task 1 overlay; Task 2 source/export comparisons. |
| REQ | META-03 | Question/Answers/Category preserved verbatim | 02-01 | COVERED | Task 1 digests; Task 2 recomputation and source diff checks. |
| RESEARCH | R-01 | Workbook FAQ Data headers, 1,195 rows, URL authority, observed title/description limits | 02-01 | COVERED | Task 1 parser and validations. |
| RESEARCH | R-02 | Phase 1 route registry is durable contentId/slug source | 02-01 | COVERED | Task 1 join and Task 2 HTML mapping. |
| RESEARCH | R-03 | Existing normalizeFaqMetadata owns suffix/description rendering policy | 02-01 | COVERED | Task 1 and Task 2 shared-helper checks. |
| RESEARCH | R-04 | Static export and existing verify:p2 patterns | 02-01 | COVERED | Task 2 --html, build, and verify:p2 checks. |
| CONTEXT | D-01 | Online URL slug → Phase 1 registry → contentId join | 02-01 | COVERED | Task 1 action and key link. |
| CONTEXT | D-02 | Exactly 1,195 rows and fail-closed mapping | 02-01 | COVERED | Task 1 validation; Task 2 mutation diagnostics. |
| CONTEXT | D-03 | Raw approved fields and one suffix via established policy | 02-01 | COVERED | Task 1 overlay; Task 2 output comparisons. |
| CONTEXT | D-04 | Suffix-free title base and 60-character render limit | 02-01 | COVERED | Task 1 title checks. |
| CONTEXT | D-05 | Metadata-only overlay and authored-field protection | 02-01 | COVERED | Task 1 merge order/digests; Task 2 verifier. |
| CONTEXT | D-06 | Workbook precedence for 1,195 and legacy fallback for 205 | 02-01 | COVERED | Task 1 merge and Task 2 full-catalog assertions. |
| CONTEXT | D-07 | Dependency-free deterministic --write/--check | 02-01 | COVERED | Task 1 generator; Task 2 command. |
| CONTEXT | D-08 | Metadata keyed by contentId independent of public slug | 02-01 | COVERED | Task 1 artifact schema and Task 2 registry mapping. |

## Artifacts this phase produces

- scripts/generate-faq-metadata.js — dependency-free workbook parser, validator, and deterministic snapshot writer/checker.
- src/faq/generated-en-metadata.json — committed approved metadata and authored-content digest snapshot for 1,195 records.
- src/faq/index.ts — contentId-keyed approved metadata overlay with existing fallback/category behavior.
- scripts/verify-faq-metadata.js — source and optional static-export verifier.
- package.json — verify:faq-metadata command.

## Verification

1. `node scripts/generate-faq-metadata.js --write --workbook '/Users/longnv/bin/repo/fastgpt-data/Week04/存量修复-补Meta第2批/FastGPT-存量FAQ补Meta-第2批1195条-V1.0-星触达-20260811.xlsx'`
2. `node scripts/generate-faq-metadata.js --check`
3. `npm run verify:faq-metadata`
4. `npm run verify:faq-routes`
5. `npx tsc --noEmit`
6. `NEXT_PUBLIC_SITE_VARIANT=io NEXT_PUBLIC_HOME_URL=https://fastgpt.io npm run build`
7. `npm run verify:faq-metadata -- --html`
8. `npm run verify:p2`
9. `git diff -- src/faq/en.ts src/faq/zh.ts` confirms authored FAQ source remains unchanged.

## Success Criteria

- META-01, META-02, and META-03 appear in frontmatter and are proven by source and export checks.
- The approved snapshot is deterministic, committed, self-contained, and exactly 1,195 rows.
- Every mapped canonical English FAQ page renders the approved metadata with one suffix and intended route identity.
- Authored Question, Answers, and Category remain source-owned and byte-for-byte stable; out-of-batch fallback remains available.
- The existing FAQ visual contract and Phase 1 route registry remain intact.
