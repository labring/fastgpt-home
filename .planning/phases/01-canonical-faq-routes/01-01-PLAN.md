---
phase: 01-canonical-faq-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-faq-route-registry.js
  - scripts/verify-faq-routes.js
  - src/faq/english-route-evidence.json
  - src/faq/generated-en-route-registry.json
  - src/faq/index.ts
  - src/lib/localizedRoutes.ts
  - src/app/[lang]/faq/[id]/page.tsx
  - src/app/faq/[id]/page.tsx
  - package.json
autonomous: true
requirements:
  - URL-01
  - URL-02
  - URL-03
estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every key in src/faq/en.ts appears exactly once as contentId in the committed English route registry."
    - "Every canonicalSlug is non-empty, lowercase ASCII a-z0-9-hyphen syntax, and unique across the English registry."
    - "A current route is preserved only when its slug is safe, unique, and resolves to its intended contentId; missing, unsafe, or collided records receive a deterministic repaired slug."
    - "Every preserve/repair decision records its evidence source: Week04 online-URL evidence when available, or the repository's current English object key when the record is outside the approved workbook."
    - "The same committed mapping drives FAQ lookup, canonical path construction, and generateStaticParams, so every registry route resolves to the intended FAQ record at build time."
    - "Malformed or unknown route identities resolve through the existing notFound path and cannot render a different FAQ record."
    - "FAQ list empty results show `No matching FAQ questions` and the `Clear filters` action defined by the UI contract."
    - "The FAQ detail error path exposes the documented return-to-list guidance while retaining the existing not-found behavior."
    - "The FAQ list preserves its responsive one/two/three-column card layout after canonical slug changes."
    - { statement: "Long FAQ questions and answers remain readable through existing wrapping and responsive layout at every final canonical path.", verification: backstop }
  artifacts:
    - "src/faq/english-route-evidence.json contains the normalized, committed preserve-evidence snapshot used by --check."
    - "src/faq/generated-en-route-registry.json contains sorted contentId-to-canonicalSlug records and explicit collision dispositions, including ambiguous no-redirect ledger entries."
    - "scripts/generate-faq-route-registry.js provides deterministic --write and --check operations using repository tooling only."
    - "scripts/verify-faq-routes.js provides one runnable assert-based route/registry regression command."
  key_links:
    - "src/faq/index.ts resolves registry canonical slugs to the existing authored FAQ objects without changing Question, Answers, or Category fields."
    - "src/lib/localizedRoutes.ts and both FAQ detail route entries use the registry-backed canonical slug set for links and static params."
    - "The verifier checks source keys, registry uniqueness, route round-trips, and static-param coverage from one mapping."
  prohibitions:
    - statement: "Do not import or rewrite approved title, description, or keyword metadata in this phase."
      status: required
      verification: plan-scope-review
    - statement: "Do not emit deployment redirects, alter the full SEO graph, or normalize healthy URLs without a recorded evidence decision."
      status: required
      verification: plan-scope-review
    - statement: "Do not modify authored FAQ Question, Answers, or Category values."
      status: required
      verification: source-diff-review
---

<objective>
Give every current English FAQ record one stable identity and one safe canonical route while preserving verified healthy URLs.

Purpose: Satisfy URL-01, URL-02, and URL-03 with one committed build-time mapping that later metadata, SEO, and release phases can consume.
Output: A deterministic route registry, registry-backed FAQ route consumers, and one focused regression command.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
This phase follows locked decisions D-01, D-02, D-03, and D-04 from `01-CONTEXT.md`. The approved Week04 workbook supplies online-URL evidence for preserve/repair classification; later phases own metadata fidelity, SEO graph alignment, redirect projection, and production export checks. The existing FAQ route UI remains the surface described by `01-UI-SPEC.md`; this plan changes route data only.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Generate the committed English route registry</name>
  <files>scripts/generate-faq-route-registry.js, src/faq/english-route-evidence.json, src/faq/generated-en-route-registry.json</files>
  <read_first>
    <file>src/faq/en.ts</file>
    <file>scripts/lib/redirects.js</file>
    <file>scripts/sync-w3-faq.py</file>
    <file>.planning/phases/01-canonical-faq-routes/01-CONTEXT.md</file>
    <file>.planning/phases/01-canonical-faq-routes/01-RESEARCH.md</file>
  </read_first>
  <action>Create `scripts/generate-faq-route-registry.js` as a Node standard-library generator that parses the English object keys with the existing TypeScript AST approach. On `--write`, read the approved online-URL evidence path supplied by an explicit CLI argument or environment variable, normalize it into the committed `src/faq/english-route-evidence.json` snapshot, and write sorted `src/faq/generated-en-route-registry.json`; on `--check`, use that committed snapshot by default and accept an explicit evidence path for reviewed regeneration. Implement D-01 by retaining each source object key as `contentId`. Implement D-02 for every current record: use Week04 online-URL evidence when a row exists, and use the repository's current English object key as repository evidence for records outside the workbook; preserve only a safe unique source slug that resolves to its intended contentId, and repair only missing, unsafe, or collided sources. Implement D-03 by slugifying the complete Question into lowercase ASCII hyphen syntax, reserving preserved/allocated slugs, and appending a stable short SHA-256 digest of `contentId` on collision. Implement D-04 by serializing ambiguous legacy sources into a collision ledger with all candidate contentIds and `disposition: no-redirect`; reserve hard failures for malformed evidence, contradictory identity, duplicate canonical slugs, unsafe canonical syntax, or other internally inconsistent registry data. Record `routeStatus`, `evidenceSource`, source slug, canonical slug, and collision disposition for every record. Support idempotent `--check`; keep authored FAQ fields untouched and add no package dependency.</action>
  <verify>
    <automated>node scripts/generate-faq-route-registry.js --check</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/faq/english-route-evidence.json` is the deterministic normalized evidence snapshot used by default in `--check`; each record names `week04-online-url` or `repository-current-key` as evidenceSource.</criterion>
    <criterion>`src/faq/generated-en-route-registry.json` is deterministic, sorted, and contains one record per `src/faq/en.ts` key plus any explicit collision-ledger entries.</criterion>
    <criterion>Every canonical slug matches `^[a-z0-9]+(?:-[a-z0-9]+)*$` and no two records share a canonical slug.</criterion>
    <criterion>Preserved records have a recorded Week04 or repository-current-key evidence source and a source slug equal to canonicalSlug; repaired records have a stable digest-backed allocation and an explicit routeStatus.</criterion>
    <criterion>Malformed, contradictory, duplicate, or unsafe records make the command exit nonzero with the offending contentId or source value, while ambiguous legacy groups serialize as no-redirect ledger entries.</criterion>
  </acceptance_criteria>
  <done>The committed evidence snapshot and registry encode D-01 through D-04; malformed or internally inconsistent data fails closed, while ambiguous legacy groups remain explicit no-redirect ledger entries.</done>
</task>

<task type="auto">
  <name>Task 2: Wire canonical slugs into FAQ lookup and static routes</name>
  <files>src/faq/index.ts, src/lib/localizedRoutes.ts, src/app/[lang]/faq/[id]/page.tsx, src/app/faq/[id]/page.tsx, scripts/verify-faq-routes.js</files>
  <read_first>
    <file>src/faq/index.ts</file>
    <file>src/lib/localizedRoutes.ts</file>
    <file>src/app/[lang]/faq/[id]/page.tsx</file>
    <file>src/app/faq/[id]/page.tsx</file>
    <file>src/faq/generated-en-route-registry.json</file>
    <file>src/faq/english-route-evidence.json</file>
    <file>scripts/verify-faq-routes.js (new file; create in this task)</file>
    <file>.planning/phases/01-canonical-faq-routes/01-UI-SPEC.md</file>
  </read_first>
  <action>Import the generated registry through `src/faq/index.ts` and expose one registry-backed resolver that maps canonicalSlug to the original contentId before calling the existing locale/content lookup. Update `getFaqIds('en')` and `getFaqItem` so English static params and detail rendering use canonical slugs while preserving the existing FAQ object fields. Update `getFaqPath` or its narrow call-site adapter to encode the canonical slug from the registry, preserve default-locale path rules, and leave non-English behavior unchanged. Ensure both localized and root FAQ detail routes use the same resolver, retain `dynamicParams = false`, call `notFound()` on unknown/malformed/invalidly decoded IDs, and keep the current H1/list/detail UI hierarchy from `01-UI-SPEC.md`. Create `scripts/verify-faq-routes.js` in this task with assert-based `--route-wiring` checks for canonical path round-trips, static-param coverage, intended-record lookup, and malformed/unknown identity handling. Keep metadata, hreflang, sitemap, and redirect behavior owned by later phases unchanged.</action>
  <verify>
    <automated>node scripts/generate-faq-route-registry.js --check &amp;&amp; node scripts/verify-faq-routes.js --route-wiring &amp;&amp; git diff --check -- src/faq/index.ts src/lib/localizedRoutes.ts 'src/app/[lang]/faq/[id]/page.tsx' 'src/app/faq/[id]/page.tsx' scripts/verify-faq-routes.js</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Every canonicalSlug returned by `getFaqIds('en')` resolves through `getFaqItem` to the same original `contentId` and authored question.</criterion>
    <criterion>`getFaqPath('en', canonicalSlug)` emits the encoded final slug, and root/default-locale behavior remains unchanged.</criterion>
    <criterion>Both FAQ detail route entries enumerate registry-backed static params and render `notFound()` for unknown or malformed identities.</criterion>
    <criterion>`scripts/verify-faq-routes.js --route-wiring` passes after the route resolver and static-param changes, covering encoded round-trips plus malformed/unknown identities.</criterion>
    <criterion>Question, Answers, and Category values remain byte-for-byte unchanged; metadata, SEO alternate, sitemap, and deployment redirect behavior remain owned by later phases.</criterion>
  </acceptance_criteria>
  <done>FAQ lookup, path generation, and static params share the committed canonical registry and preserve existing rendered content.</done>
</task>

<task type="auto">
  <name>Task 3: Add the focused route and registry regression command</name>
  <files>package.json</files>
  <read_first>
    <file>scripts/verify-faq-routes.js</file>
    <file>package.json</file>
    <file>src/faq/generated-en-route-registry.json</file>
    <file>src/faq/english-route-evidence.json</file>
    <file>src/faq/en.ts</file>
    <file>src/faq/index.ts</file>
    <file>src/lib/localizedRoutes.ts</file>
    <file>scripts/verify-i18n-seo.js</file>
    <file>.planning/REQUIREMENTS.md</file>
  </read_first>
  <action>Add a `verify:faq-routes` package script that runs the Task 2 verifier. Extend its default mode to assert source-key/evidence/registry cardinality, `evidenceSource` values, canonical slug syntax and uniqueness, preserve/repair status invariants, explicit ambiguous no-redirect ledger entries, and deterministic generator output using the committed evidence snapshot. Keep checks limited to URL-01/02/03; report each failed contentId/slug and exit nonzero while logging one concise success line on pass.</action>
  <verify>
    <automated>npm run verify:faq-routes</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`npm run verify:faq-routes` exits 0 on the committed registry and prints `FAQ route verification passed`.</criterion>
    <criterion>The command exits 1 and names the record when a key/evidence row is missing, a slug is duplicated/unsafe, an evidenceSource is invalid, or a canonical route resolves to another record.</criterion>
    <criterion>The package command runs the existing verifier's route-wiring and registry invariants without starting a server or adding a test dependency.</criterion>
  </acceptance_criteria>
  <done>A maintainer has one runnable regression command for stable FAQ identity and canonical route invariants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workbook/CLI input → registry generator | External spreadsheet path and cell values become committed route data. |
| URL path segment → FAQ resolver | Percent-encoded user input selects a canonical slug and must resolve only through the validated registry. |
| Generated registry → static export | Build-time route data controls which pages are emitted and which FAQ object is rendered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `scripts/generate-faq-route-registry.js` | high | mitigate | Validate workbook rows, source keys, slug syntax, uniqueness, and deterministic sorted output before writing. |
| T-01-02 | Tampering | `src/faq/index.ts` canonical resolver | high | mitigate | Decode once, reject malformed values, and resolve only registry slugs to original content IDs before rendering. |
| T-01-03 | Information disclosure | Route error handling | low | accept | Unknown IDs use existing `notFound()` behavior and expose no record data. |
| T-01-SC | Tampering | npm/package changes | high | mitigate | Reuse Node built-ins and existing dependencies; no package installation is planned. |
</threat_model>

## Artifacts this phase produces

- `scripts/generate-faq-route-registry.js` — `--write`/`--check` registry generator.
- `src/faq/generated-en-route-registry.json` — committed English `contentId`/`canonicalSlug` mapping and collision ledger fields.
- `src/faq/english-route-evidence.json` — normalized preserve/repair evidence snapshot with provenance fields.
- `src/faq/index.ts` — registry-backed English lookup exports/resolver.
- `src/lib/localizedRoutes.ts` — canonical slug path integration.
- `src/app/[lang]/faq/[id]/page.tsx` — localized canonical slug resolution and static params.
- `src/app/faq/[id]/page.tsx` — root alias static-param integration.
- `scripts/verify-faq-routes.js` — assert-based route/registry regression verifier.
- `package.json` — `verify:faq-routes` command.

## Source Coverage Audit

| Source | Covered by |
|--------|------------|
| ROADMAP Phase 1 goal | Tasks 1–3 and `must_haves.truths` |
| URL-01 | Tasks 1–3; registry cardinality and uniqueness checks |
| URL-02 | Tasks 1–3; evidence-backed preserve policy and identity assertions |
| URL-03 | Tasks 1–3; deterministic repaired allocation and static route wiring |
| D-01 | Task 1 registry fields and Task 2 resolver |
| D-02 | Task 1 preserve classification and Task 3 invariant checks |
| D-03 | Task 1 slug/digest allocator and committed output |
| D-04 | Registry collision ledger stores candidate IDs with `no-redirect`; deployment redirect emission remains Phase 4 |
| Research constraints | Tasks 1–3 reuse AST/Node patterns, static export data, and existing notFound behavior |

## Verification

1. `node scripts/generate-faq-route-registry.js --check`
2. `npm run verify:faq-routes`
3. `git diff -- src/faq/en.ts src/faq/zh.ts` confirms authored FAQ fields remain unchanged.

## Success Criteria

- URL-01, URL-02, and URL-03 appear in plan frontmatter and are proven by the focused verifier.
- The registry is deterministic, committed, unique, and build-time available.
- All English canonical slugs resolve to their intended FAQ record through both localized and root route entries.
- Healthy route preservation, repaired route allocation, and ambiguous collision handling are explicit and auditable.
