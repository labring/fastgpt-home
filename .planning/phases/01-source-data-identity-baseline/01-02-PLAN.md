---
phase: 01-source-data-identity-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - scripts/phase1/validate_faq_source.py
  - scripts/phase1/test_faq_source.py
  - artifacts/phase1/faq-source-baseline.json
autonomous: true
requirements:
  - FAQ-01
  - FAQ-02
must_haves:
  truths:
    - "The validator accepts exactly the V1.1 FAQ Data schema and emits all eight publish fields plus both audit fields in original order."
    - "The baseline report proves 60 rows, 14 categories, 60 unique legal slugs, contiguous no values 1-60, and complete publish fields."
    - "A rerun with the same source fingerprint and canonical field hashes is reproducible; schema, fingerprint, row, uniqueness, or field-content drift blocks the baseline."
  artifacts:
    - path: "scripts/phase1/validate_faq_source.py"
      provides: "Strict FAQ Data validator and baseline report writer"
      exports: ["validate_faq_rows", "write_baseline_report"]
    - path: "scripts/phase1/test_faq_source.py"
      provides: "Validation rules and drift regression tests"
    - path: "artifacts/phase1/faq-source-baseline.json"
      provides: "Auditable V1.1 FAQ source fingerprint, row snapshot, field hashes, and validation result"
  key_links:
    - from: "scripts/phase1/validate_faq_source.py"
      to: "scripts/phase1/xlsx_reader.py"
      via: "read_sheet and build_source_manifest imports"
      pattern: "read_sheet|build_source_manifest"
    - from: "artifacts/phase1/faq-source-baseline.json"
      to: "FastGPT-精选规范FAQ-首批60条-V1.1-星触达-20260730.xlsx"
      via: "source sha256, sheet, row number, and field hashes"
      pattern: "53a6f0d89f1ef2cd688e4c50dc7b59d69276a8ebb24e65f9979e0156e8a85547"
---

<objective>
把 60 条新增 FAQ 固化为可审计的十字段源基线。

Purpose: Phase 2 消费这份基线接入运行时，发布门禁需要知道源版本、字段顺序和逐行内容证据。
Output: 校验器、回归测试和提交到仓库的 `faq-source-baseline.json`。
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-source-data-identity-baseline/01-CONTEXT.md
@.planning/phases/01-source-data-identity-baseline/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement strict FAQ source validation</name>
  <files>scripts/phase1/validate_faq_source.py</files>
  <action>Import the Plan 01 reader and target only `FastGPT-精选规范FAQ-首批60条-V1.1-星触达-20260730.xlsx`, sheet `FAQ Data`, rows 1-61. Require the ordered columns `slug`, `no`, `category`, `question`, `answer`, `title`, `description`, `keywords`, `事实来源`, `待客户确认`; retain all ten values per row and reject missing, extra, reordered, truncated, joined, translated, or dropped columns per D-01 and D-04. Validate exactly 60 rows, 14 distinct source categories, 60 unique slugs, slug regex `^[a-z0-9]+(?:-[a-z0-9]+)*$`, `no` sequence 1..60, and non-empty values for each of the eight publish fields per D-05 and D-06. Compute per-field SHA-256 values and a canonical row digest, include source fingerprint/bytes/sheet/header order/data-row count/generated time, and write `artifacts/phase1/faq-source-baseline.json`. A mismatch in any schema, fingerprint, row count, uniqueness, required value, or field digest sets `status: blocked` and exits with code 2; it must never write production FAQ data.</action>
  <verify>
    <automated>python3 scripts/phase1/validate_faq_source.py --workbook "/Users/longnv/bin/repo/fastgpt-data/W2-内容方向与首批内容-20260730/FastGPT-精选规范FAQ-首批60条-V1.1-星触达-20260730.xlsx" --sheet "FAQ Data" --write artifacts/phase1/faq-source-baseline.json &amp;&amp; python3 -c "import json; p=json.load(open('artifacts/phase1/faq-source-baseline.json')); assert p['status']=='passed' and p['summary']=={'rows':60,'categories':14,'unique_slugs':60,'invalid_slugs':0,'missing_required_fields':0}"</automated>
  </verify>
  <done>The baseline artifact contains all ten original columns, exact V1.1 fingerprint, 60 row-level field-hash records, and a passed summary matching 60/14/60 with zero invalid or missing required values.</done>
</task>

<task type="auto">
  <name>Task 2: Test FAQ drift and field-fidelity gates</name>
  <files>scripts/phase1/test_faq_source.py</files>
  <action>Write standard-library unit tests for valid baseline rows and each blocking rule: reordered or missing header, duplicate slug, uppercase or invalid slug, non-contiguous `no`, empty publish field, wrong category count, altered audit-field value, and changed source fingerprint. Assert the report retains both audit columns and the ten-field row order per D-04 and D-05, and that `generated_at` does not affect the canonical digest per D-02. Include a subprocess test for the current source that checks the report's 60/14/60 counts and source SHA-256.</action>
  <verify>
    <automated>python3 -m unittest scripts/phase1/test_faq_source.py -v</automated>
  </verify>
  <done>Every schema, content, identity, and provenance drift case produces a deterministic blocked result, while the locked source passes and produces the committed baseline artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| V1.1 workbook -> validator | Workbook cells cross into a validator that determines release eligibility. |
| Baseline artifact -> Phase 2 | A committed JSON snapshot becomes the source contract for runtime ingestion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | FAQ baseline artifact | high | mitigate | Include locked SHA-256, ordered headers, row numbers, ten-field hashes, and canonical digest; fail closed on drift. |
| T-01-04 | Repudiation | FAQ validation run | medium | mitigate | Record source path, sheet, byte size, generation time, validation status, and deterministic summary in the artifact. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | Reuse the standard-library reader and existing repository dependencies; no package install is planned. |
</threat_model>

<verification>
Run the validator against the locked V1.1 workbook and run `python3 -m unittest scripts/phase1/test_faq_source.py -v`. Inspect the artifact for the exact fingerprint, ten headers, 60 rows, 14 categories, 60 unique slugs, and a passed status.
</verification>

<success_criteria>
FAQ-01 and FAQ-02 are evidenced by a reproducible, field-complete baseline whose failure status blocks downstream publishing on any source drift.
</success_criteria>

<output>
Create `.planning/phases/01-source-data-identity-baseline/01-02-SUMMARY.md` when done.
</output>
