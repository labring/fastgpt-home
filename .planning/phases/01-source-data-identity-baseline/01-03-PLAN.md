---
phase: 01-source-data-identity-baseline
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - scripts/phase1/identity_baseline.mjs
  - scripts/phase1/category_contract.json
  - scripts/phase1/category_contract.mjs
  - scripts/phase1/test_identity_baseline.mjs
  - artifacts/phase1/identity-baseline.json
  - artifacts/phase1/category-contract-report.json
  - artifacts/phase1/publish-gate-contract.json
autonomous: true
requirements:
  - LEG-01
  - LEG-02
must_haves:
  truths:
    - "Every one of the 2,000 inventory rows has a stable source-row identity and a matched repository key or an explicit locked conflict status."
    - "The identity report explains 1,400 question matches, 590 missing repository objects, 1,990 unique URLs/questions, and 10 duplicate URL groups without collapsing rows."
    - "The nine category IDs and locale labels are machine-validated with the locked 2,000-row distribution, confidence counts, and review counts."
    - "A machine-readable publishing gate contract records dry-run, fail-closed, idempotency, snapshot, and rollback invariants for Phase 4 consumption."
  artifacts:
    - path: "scripts/phase1/identity_baseline.mjs"
      provides: "URL/question/repo-key identity resolver with explicit conflict states"
      exports: ["normalizeUrl", "normalizeQuestion", "resolveIdentity"]
    - path: "scripts/phase1/category_contract.json"
      provides: "Nine stable category IDs, Chinese/English labels, and expected V1.1 distributions"
    - path: "scripts/phase1/category_contract.mjs"
      provides: "Category and publishing-gate contract validator"
    - path: "artifacts/phase1/identity-baseline.json"
      provides: "2,000-row identity evidence and conflict report"
    - path: "artifacts/phase1/category-contract-report.json"
      provides: "Category distribution and locale validation evidence"
    - path: "artifacts/phase1/publish-gate-contract.json"
      provides: "Downstream dry-run, fail-closed, idempotency, and rollback contract"
  key_links:
    - from: "scripts/phase1/identity_baseline.mjs"
      to: "scripts/phase1/xlsx_reader.py"
      via: "child-process JSON worksheet reader for the inventory sheet"
      pattern: "分类重挂对照表|751a479680bfc987b9394c7429100d8f09269cc7ad8a698ff97b34415a6733b0"
    - from: "scripts/phase1/identity_baseline.mjs"
      to: "src/faq/en.ts"
      via: "TypeScript AST extraction of the exported faq object and Question values"
      pattern: "createSourceFile|VariableDeclaration|Question"
    - from: "scripts/phase1/category_contract.mjs"
      to: "artifacts/phase1/publish-gate-contract.json"
      via: "validated machine-readable handoff for Phase 4"
      pattern: "dry_run|fail_closed|idempotent|rollback"
---

<objective>
建立存量 FAQ 的唯一身份映射、九类分类契约和后续批量写入门禁。

Purpose: Phase 4 的 Meta/分类写入必须以真实线上对象、明确冲突状态和稳定分类 ID 为前置条件。
Output: 2,000 行身份报告、九类契约报告及可消费的发布门禁 JSON；此计划不改生产 FAQ、slug、URL 或页面代码。
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-source-data-identity-baseline/01-CONTEXT.md
@.planning/phases/01-source-data-identity-baseline/01-01-SUMMARY.md
@src/faq/en.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve inventory rows against repository FAQ identity</name>
  <files>scripts/phase1/identity_baseline.mjs, scripts/phase1/test_identity_baseline.mjs, artifacts/phase1/identity-baseline.json</files>
  <action>Read `存量修复/FastGPT-存量FAQ分类重挂对照表-V1.1-星触达-20260730.xlsx`, sheet `分类重挂对照表`, header row 4, data rows 5-2004 through the Plan 01 reader. Extract the exported `faq` object from `src/faq/en.ts` with the installed TypeScript compiler API, retaining each repo key and Question. Normalize only URL host case, trailing slash, reversible percent encoding, and question edge whitespace plus Unicode NFC per D-07 and D-08; preserve raw URL, raw question, repo key, source fingerprint, sheet, and source row number. Apply candidate priority URL/path evidence, unique normalized question, then repo key consistency check. Mark URL/question disagreement `url-question-conflict`, key/question disagreement `key-question-conflict`, every repeated normalized URL group `duplicate-url` with both rows and line evidence, and rows without a unique candidate `unmatched-source` per D-09, D-10, and D-11. Emit one report row for all 2,000 inputs with stable identity, status, match method, candidate evidence, original classification, suggested category, confidence, and review flag; set report status `blocked` when any conflict exists and retain the exact 1,400/590/10 baseline counts. Do not create slugs, alter URLs, or write production data per D-12.</action>
  <verify>
    <automated>node scripts/phase1/test_identity_baseline.mjs</automated>
  </verify>
  <done>The identity artifact contains 2,000 preserved source rows, 1,990 unique URLs/questions, 1,400 question matches, 590 unmatched objects, 10 duplicate URL groups, explicit conflict statuses, and the locked inventory SHA-256; the expected blocked status is machine-asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Validate nine-category and publishing-gate contracts</name>
  <files>scripts/phase1/category_contract.json, scripts/phase1/category_contract.mjs, scripts/phase1/test_identity_baseline.mjs, artifacts/phase1/category-contract-report.json, artifacts/phase1/publish-gate-contract.json</files>
  <action>Define the nine stable IDs and exact Chinese/English labels from D-13 in `category_contract.json`, plus the locked V1.1 distributions 350/292/272/234/223/190/164/140/135 and confidence totals high 576, medium 43, LLM 913, low 468 with review totals sample 913 and yes 468 per D-14. Validate every inventory row's suggested category against the ID map while retaining source `原分类`, new category ID, confidence, review flag, raw question, and raw URL. Emit `category-contract-report.json` with row total 2,000, per-ID counts, locale labels, confidence/review counts, and source fingerprint. Emit `publish-gate-contract.json` encoding D-15, D-16, D-17, and D-18: dry-run required and zero-write, fingerprint/header/row/field-hash failures set `blocked`, default fail-closed behavior for every listed conflict, explicit allowlist required for any future subset, immutable batch ID plus source fingerprint and pre-write row snapshots, same-batch same-fingerprint replay as idempotent no-op, fingerprint change as a new batch, and rollback accepted only with the matching batch snapshot. Keep this as a contract artifact; transaction and file-lock implementation belong to Phase 4.</action>
  <verify>
    <automated>node scripts/phase1/category_contract.mjs --workbook "/Users/longnv/bin/repo/fastgpt-data/W2-内容方向与首批内容-20260730/存量修复/FastGPT-存量FAQ分类重挂对照表-V1.1-星触达-20260730.xlsx" --write artifacts/phase1/category-contract-report.json --gate artifacts/phase1/publish-gate-contract.json &amp;&amp; node scripts/phase1/test_identity_baseline.mjs</automated>
  </verify>
  <done>All 2,000 rows map to one of nine stable category IDs with exact distribution and locale labels, and the gate artifact makes dry-run, fail-closed, idempotent replay, immutable snapshots, and matching-batch rollback observable for Phase 4.</done>
</task>

</tasks>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Deterministic W2 source and unique identity baseline | 01-01, 01-02, 01-03 | COVERED | Reader, FAQ baseline, identity and category artifacts |
| REQ | FAQ-01 | Read exactly 60 V1.1 FAQ rows with ten preserved fields | 01-01, 01-02 | COVERED | D-01/D-04 cited in actions |
| REQ | FAQ-02 | Prove 14 categories, slug/no/required-field validity and fingerprint | 01-02 | COVERED | Strict validator and regression tests |
| REQ | LEG-01 | Explain 2,000-row identity differences and conflicts | 01-01, 01-03 | COVERED | URL/question/key precedence plus 1,400/590/10 report |
| REQ | LEG-02 | Stable nine-category IDs and locale labels | 01-03 | COVERED | Contract JSON and distribution test |
| RESEARCH | — | No RESEARCH.md; user supplied complete source evidence and requested research skip | 01-01, 01-02, 01-03 | COVERED | No external library or service is introduced |
| CONTEXT | D-01..D-18 | Locked source, fidelity, identity, category, and gate behavior | 01-01, 01-02, 01-03 | COVERED | Each decision is cited by an implementing task |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inventory workbook -> identity resolver | External URLs, questions, categories, and confidence values determine candidate identities. |
| Repository FAQ source -> resolver | Existing keys and questions are trusted application evidence used for matching. |
| Contract artifacts -> future importer | Phase 4 consumes status and gate invariants as write preconditions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | URL/question identity mapping | high | mitigate | Preserve raw evidence, apply only reversible normalization, emit explicit conflicts, and fail closed on anomalies. |
| T-01-06 | Repudiation | Identity and category reports | medium | mitigate | Bind each row to source SHA-256, sheet, source row, raw URL, and raw question; record deterministic summary counts. |
| T-01-07 | Elevation of privilege | Publishing gate contract | high | mitigate | Require dry-run, immutable batch/snapshot pairing, explicit allowlists for subsets, and matching-batch rollback. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | Reuse the existing TypeScript dev dependency and Plan 01 standard-library reader; no new package install is planned. |
</threat_model>

<verification>
Run `node scripts/phase1/test_identity_baseline.mjs` and the category command from Task 2. Inspect all three JSON artifacts for exact counts, statuses, source fingerprints, nine IDs, and gate invariants. The identity report is expected to be `blocked` because the source evidence intentionally contains duplicate and unmatched rows; the report must still be complete and auditable.
</verification>

<success_criteria>
LEG-01 and LEG-02 are evidenced by complete per-row identity/conflict output, stable nine-category IDs with locale labels, and a machine-readable handoff contract that Phase 4 can enforce before any write.
</success_criteria>

<output>
Create `.planning/phases/01-source-data-identity-baseline/01-03-SUMMARY.md` when done.
</output>
