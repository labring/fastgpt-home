---
phase: 02-new-faq-bilingual-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/faq/w2.ts
  - src/faq/zh.ts
  - scripts/phase2/validate_w2_faq.mjs
  - scripts/phase2/test_w2_faq.mjs
autonomous: true
requirements:
  - FAQ-03
  - FAQ-04
must_haves:
  truths:
    - "The runtime contains exactly the 60 locked W2 FAQ records in Chinese and keeps the existing 1,400 English records unchanged."
    - "Every W2 slug, question, answer, title, description, keywords, and source category maps from the Phase 1 baseline without field loss or collision."
    - "The Chinese data union has 1,460 records while the English data set remains 1,400 records."
  artifacts:
    - path: "src/faq/w2.ts"
      provides: "Generated Chinese-only W2 FAQ records mapped to FaqItem"
    - path: "src/faq/zh.ts"
      provides: "Existing Chinese overlay merged with W2 records"
    - path: "scripts/phase2/validate_w2_faq.mjs"
      provides: "Baseline-to-TypeScript field and slug validator"
    - path: "scripts/phase2/test_w2_faq.mjs"
      provides: "Runtime source and collision regression checks"
  key_links:
    - from: "src/faq/w2.ts"
      to: "artifacts/phase1/faq-source-baseline.json"
      via: "generated six-field mapping from ten-field source snapshot"
      pattern: "private-deployment-data-boundary|citation-traceability"
    - from: "src/faq/zh.ts"
      to: "src/faq/w2.ts"
      via: "faqZh overlay merge"
      pattern: "faqW2Zh"

---

<objective>
接入 60 条已审计中文 FAQ，建立数据保真和运行时数量门禁。

Purpose: 后续路由与 SEO 只能消费可追溯的运行时数据，避免手工复制导致 slug、正文或 Meta 漂移。
Output: 独立 W2 数据模块、中文 overlay 合并和可重复字段校验脚本。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Generate and merge W2 Chinese data</name>
  <files>src/faq/w2.ts, src/faq/zh.ts</files>
  <action>Generate `src/faq/w2.ts` from `artifacts/phase1/faq-source-baseline.json` using the exact six runtime fields and preserve source category text. Rename the existing `faqZh` object to a legacy overlay and export a merged `faqZh` that appends `faqW2Zh`. Do not alter existing 1,400 entry values or introduce English copies for the 60 new slugs.</action>
  <verify>
    <automated>node scripts/phase2/test_w2_faq.mjs</automated>
  </verify>
  <done>The TypeScript source has 60 unique W2 entries, the Chinese overlay has 1,460 entries after merge, and the English source remains unchanged at 1,400 entries.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce baseline field fidelity</name>
  <files>scripts/phase2/validate_w2_faq.mjs, scripts/phase2/test_w2_faq.mjs</files>
  <action>Use the TypeScript compiler API to read `faqW2Zh`, compare the baseline slug set and six mapped fields, assert no key collision with `src/faq/en.ts`, and report the source fingerprint and canonical field hashes. Add negative checks for missing rows, changed Meta, changed answers, duplicate slug, and source fingerprint drift.</action>
  <verify>
    <automated>node scripts/phase2/test_w2_faq.mjs</automated>
  </verify>
  <done>The validator fails on any W2 content or provenance drift and passes the committed runtime data exactly.</done>
</task>

</tasks>

<threat_model>
| Threat | Component | Mitigation |
| --- | --- | --- |
| Source tampering | W2 baseline to TypeScript generation | Compare locked SHA-256, slug set, and per-field hashes before accepting runtime data. |
| Data loss | `faqZh` overlay merge | Assert six-field mapping and 1,460 Chinese record count in tests. |
| Locale confusion | New Chinese-only keys | Keep `faqEn` unchanged and test zero W2 keys in English source. |
</threat_model>

<verification>
Run `node scripts/phase2/test_w2_faq.mjs`, inspect all 60 generated keys and field hashes, and run `npx tsc --noEmit`.
</verification>

<success_criteria>
FAQ-03 and FAQ-04 have a reproducible runtime data source with exact W2 content and no English-route key pollution.
</success_criteria>
