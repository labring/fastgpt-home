---
phase: 02-new-faq-bilingual-seo
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/faq/index.ts
  - src/lib/seo.ts
  - src/app/[lang]/faq/page.tsx
  - src/app/[lang]/faq/[id]/page.tsx
  - src/app/sitemap.ts
  - scripts/phase2/test_faq_routes.mjs
autonomous: true
requirements:
  - FAQ-03
  - FAQ-05
  - FAQ-06
must_haves:
  truths:
    - "English static params and sitemap keep 1,400 FAQ IDs while Chinese static params and sitemap expose 1,460 IDs including all 60 W2 slugs."
    - "A Chinese FAQ page canonicalizes to fastgpt.cn and an io/zh duplicate points to the corresponding cn/zh URL."
    - "Only FAQ keys with a real English record emit an English hreflang; W2-only keys use Chinese x-default without fabricated translations."
  artifacts:
    - path: "src/faq/index.ts"
      provides: "Locale-aware FAQ data and ID accessors"
    - path: "src/lib/seo.ts"
      provides: "FAQ canonical and translation-aware alternate URL helpers"
    - path: "src/app/[lang]/faq/[id]/page.tsx"
      provides: "Locale-specific static params, metadata, canonical JSON-LD URLs"
    - path: "src/app/sitemap.ts"
      provides: "Canonical locale-aware FAQ sitemap entries"
    - path: "scripts/phase2/test_faq_routes.mjs"
      provides: "Static source and route contract checks"
  key_links:
    - from: "src/app/[lang]/faq/[id]/page.tsx"
      to: "src/lib/seo.ts"
      via: "getFaqAlternates and canonical FAQ site URL"
      pattern: "getFaqAlternates|faqCanonical"
    - from: "src/app/sitemap.ts"
      to: "src/faq/index.ts"
      via: "getFaqData(locale) ID enumeration"
      pattern: "getFaqData"

---

<objective>
让新增中文 FAQ 可访问、可发现，并提供固定双域 SEO 关系。

Purpose: 静态路由、列表、详情和 sitemap 必须共享 locale-specific 数据边界，Meta 和 JSON-LD 必须使用同一 canonical URL。
Output: locale-aware runtime helpers, route generation, sitemap and SEO regression checks。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Make route enumeration locale-specific</name>
  <files>src/faq/index.ts, src/app/[lang]/faq/page.tsx, src/app/[lang]/faq/[id]/page.tsx, src/app/sitemap.ts</files>
  <action>Keep English data as the base export, add a `getFaqIds(lang)` helper based on `getFaqData(lang)`, and use it in detail `generateStaticParams` and sitemap. Ensure list/detail lookups retain Chinese overlay fallback. Use canonical FAQ URLs for locale-specific breadcrumb JSON-LD and sitemap entries.</action>
  <verify>
    <automated>node scripts/phase2/test_faq_routes.mjs</automated>
  </verify>
  <done>English params remain 1,400, Chinese params become 1,460, and all 60 W2 slugs appear only in Chinese route enumeration and canonical Chinese sitemap entries.</done>
</task>

<task type="auto">
  <name>Task 2: Implement translation-aware FAQ SEO alternates</name>
  <files>src/lib/seo.ts, src/app/[lang]/faq/page.tsx, src/app/[lang]/faq/[id]/page.tsx</files>
  <action>Add explicit `.io` and `.cn` FAQ URL helpers with environment overrides. Detail and list metadata must use `.cn` canonical for Chinese, `.io` canonical for English, `zh`/`zh-CN` mapped to `.cn`, and English hreflang only when the ID exists in `faqEn`. Keep existing generic `getAlternates` behavior for non-FAQ pages.</action>
  <verify>
    <automated>node scripts/phase2/test_faq_routes.mjs</automated>
  </verify>
  <done>Static helper assertions prove canonical, hreflang and x-default behavior for one existing translated ID and one W2-only ID under both env domains.</done>
</task>

</tasks>

<threat_model>
| Threat | Component | Mitigation |
| --- | --- | --- |
| Duplicate indexing | FAQ metadata helpers | Canonical Chinese URL is always `.cn`; `.io/zh` remains a duplicate with explicit canonical. |
| False translation signal | hreflang map | Emit `en` only when the exact key exists in `faqEn`; W2-only keys use Chinese x-default. |
| Route omission | static params/sitemap | Tests assert 1,400 English, 1,460 Chinese and all 60 W2 IDs. |
</threat_model>

<verification>
Run `node scripts/phase2/test_faq_routes.mjs`, `npm run lint`, and `npx tsc --noEmit`.
</verification>

<success_criteria>
FAQ-03, FAQ-05 and FAQ-06 are met with locale-specific route discovery and verifiable dual-domain SEO signals.
</success_criteria>
