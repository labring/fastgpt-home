---
phase: 02-new-faq-bilingual-seo
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - scripts/verify-p2.js
  - scripts/phase2/test_faq_build.mjs
  - .planning/phases/02-new-faq-bilingual-seo/02-03-SUMMARY.md
autonomous: true
requirements:
  - FAQ-05
  - FAQ-06
must_haves:
  truths:
    - "The production static build emits a representative W2 Chinese detail page with independent title, description, keywords, FAQPage and BreadcrumbList metadata."
    - "All generated FAQ detail pages stay within title/description limits and retain one h1 with valid heading order."
    - "Build verification checks both an existing translated FAQ and a new W2 Chinese-only FAQ without weakening existing P2 checks."
  artifacts:
    - path: "scripts/verify-p2.js"
      provides: "Build-time FAQ metadata and route verification including a W2 sample"
    - path: "scripts/phase2/test_faq_build.mjs"
      provides: "Static build artifact checker for W2 route and SEO signals"

---

<objective>
用生产静态构建确认新增详情页的用户可访问性与 SEO 输出。

Purpose: 数据与源代码检查无法证明 Next 静态输出真实包含新页面、Meta 和 JSON-LD；本计划补齐构建级证据。
Output: 更新后的 P2 verifier 和独立 W2 build artifact checker。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Extend P2 verification with a W2 sample</name>
  <files>scripts/verify-p2.js</files>
  <action>Load one W2 slug from the committed baseline instead of relying only on an old shared FAQ ID. Verify its Chinese static HTML title/description length, OG/Twitter parity, canonical URL, FAQPage and BreadcrumbList JSON-LD, while retaining all existing routes and legacy checks.</action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/verify-p2.js</automated>
  </verify>
  <done>The production build and P2 verifier check both legacy and W2 FAQ pages without changing existing expected metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Verify W2 build artifact coverage</name>
  <files>scripts/phase2/test_faq_build.mjs</files>
  <action>Read the baseline slug list and `out` directory after build. Assert every W2 Chinese detail HTML exists, no W2 English detail HTML exists, each page contains its source Title-derived title, Description-derived description, canonical `.cn` URL, FAQPage JSON-LD and BreadcrumbList JSON-LD, and the page count is exactly 60.</action>
  <verify>
    <automated>node scripts/phase2/test_faq_build.mjs</automated>
  </verify>
  <done>All 60 W2 canonical pages are present with independent metadata and structured data, and English-only route pollution is absent.</done>
</task>

</tasks>

<threat_model>
| Threat | Component | Mitigation |
| --- | --- | --- |
| Build omission | Next static output | Enumerate every baseline slug in `out` and fail on missing or English-only output. |
| Metadata drift | HTML head/social tags | Compare output to source-derived expected values and enforce length limits. |
| Structured-data regression | JSON-LD scripts | Require both FAQPage and BreadcrumbList markers on each W2 detail page. |
</threat_model>

<verification>
Run `npm run build`, `node scripts/verify-p2.js`, and `node scripts/phase2/test_faq_build.mjs`. Preserve the build output only as local verification state; do not commit `out`.
</verification>

<success_criteria>
FAQ-05 and FAQ-06 are proven against real static HTML for all 60 new pages and representative legacy pages.
</success_criteria>
