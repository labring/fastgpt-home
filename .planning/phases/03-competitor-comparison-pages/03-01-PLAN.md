---
phase: 03-competitor-comparison-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/competitor/types.ts
  - src/content/competitor/dify.ts
  - src/content/competitor/selfBuild.ts
  - src/content/competitor/index.ts
  - public/images/compare/dify-vs-fastgpt.svg
  - public/images/compare/self-build-vs-platform.svg
autonomous: true
requirements:
  - CMP-02
  - CMP-04
must_haves:
  truths:
    - "The Dify and self-build comparison records preserve the delivered Chinese five-section content contract and expose structured capability, POC, and TCO rows."
    - "Each record has independently addressable metadata, source references, evidence status, page asset metadata, internal-link slots, and date fields for later publication gating."
  artifacts:
    - path: "src/content/competitor/types.ts"
      provides: "Typed comparison-page, fact, table, link, asset, and date contracts"
      contains: "preview"
    - path: "src/content/competitor/dify.ts"
      provides: "Dify vs FastGPT structured Chinese page data"
      contains: "dify-vs-fastgpt"
    - path: "src/content/competitor/selfBuild.ts"
      provides: "Self-build vs platform structured Chinese page data"
      contains: "self-build-vs-platform"
    - path: "public/images/compare/dify-vs-fastgpt.svg"
      provides: "Stable Dify comparison information graphic with fixed viewBox and alt metadata"
    - path: "public/images/compare/self-build-vs-platform.svg"
      provides: "Stable self-build comparison information graphic with fixed viewBox and alt metadata"
  key_links:
    - from: "src/content/competitor/dify.ts"
      to: "src/content/competitor/types.ts"
      via: "typed page export consumed by the shared comparison index"
      pattern: "ComparisonPage"
    - from: "src/content/competitor/index.ts"
      to: "src/content/competitor/dify.ts"
      via: "slug-keyed record export"
      pattern: "dify-vs-fastgpt"
---

<objective>
建立竞品对比内容的可复用类型契约，并把 Dify 与自研/直接运行开源两篇源稿完整导入为结构化中文记录。

Purpose: 为后续 manifest、签发门禁和页面模板提供稳定输入，按 D-305、D-306、D-307、D-308、D-309 保持五段正文、结构化表格、中性措辞、成本模板和零案例纪律。
Output: `src/content/competitor/` 类型与两篇完整记录、共享 slug 索引、两张固定比例信息图。
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-competitor-comparison-pages/03-CONTEXT.md
@/Users/longnv/bin/repo/fastgpt-data/W2-内容方向与首批内容-20260730/竞品对比页-首批4篇/01-Dify与FastGPT怎么选-V1.0-星触达-20260730.md
@/Users/longnv/bin/repo/fastgpt-data/W2-内容方向与首批内容-20260730/竞品对比页-首批4篇/02-自研或直接跑开源与FastGPT怎么选-V1.0-星触达-20260730.md
@/Users/longnv/bin/repo/fastgpt-data/W2-内容方向与首批内容-20260730/FastGPT-竞品对比页选型与口径-V1.1-星触达-20260730.md
@/Users/longnv/bin/repo/fastgpt-data/W2-内容方向与首批内容-20260730/竞品对比页-首批4篇/README.md
@src/faq/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define comparison contracts and import the Dify record</name>
  <files>src/content/competitor/types.ts, src/content/competitor/dify.ts</files>
  <action>Define strict TypeScript unions and interfaces for `ComparisonPage`, `ComparisonSection`, `ComparisonTable`, `ComparisonRow`, `ComparisonFact`, `SourceRef`, `EvidenceStatus`, `InternalLink`, `PageAsset`, `DateFields`, and `Signoff`. Require the only page statuses `preview | published` and evidence statuses `official-public | not-publicly-listed | poc-required | contract-required` per D-312 and D-319. Import the Dify draft into `dify.ts` without changing its meaning: retain the source title/description/keywords, five top-level sections from D-305, all three capability-table groups, license/commercial boundary, cost-item TCO template, same-condition POC method, neutral selection guidance, footer source fields, and claim-level source mappings. Keep the D-307 wording discipline, D-308 price-free body, D-309 zero customer-case content, and D-310 non-logo page asset contract. Store `sourceVerifiedOn=2026-07-20`, the pinned draft version/date from D-311, source IDs, source hash, and explicit pending signoff/link evidence for the later gate.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>`types.ts` compiles with exhaustive status unions; `dify.ts` exports one typed `ComparisonPage` whose slug, metadata, five sections, table groups, POC/TCO rows, source refs, date fields, asset, links, and pending signoff entries are present and source-derived.</done>
</task>

<task type="auto">
  <name>Task 2: Import self-build content and shared comparison index</name>
  <files>src/content/competitor/selfBuild.ts, src/content/competitor/index.ts, public/images/compare/dify-vs-fastgpt.svg, public/images/compare/self-build-vs-platform.svg</files>
  <action>Import the self-build/direct-open-source draft into `selfBuild.ts` using the same typed shape, preserving its four cost groups, platform-layer tradeoff, same-condition POC table, three-year TCO rows, and neutral selection guidance under D-305 through D-309. Create `index.ts` as the single slug-keyed export for these records, with no English or other-locale entries per D-302. Add one stable, non-logo information graphic for each page at a fixed aspect ratio with descriptive Chinese alt text and no price or partner marks per D-310. Keep the asset paths and dimensions in each page record so the later manifest and renderer can verify them deterministically.</action>
  <verify>
    <automated>npx tsc --noEmit && node -e "import('./src/content/competitor/index.ts').then(({comparisonPages}) => { if (Object.keys(comparisonPages).sort().join(',') !== 'dify-vs-fastgpt,self-build-vs-platform') process.exit(1); })"</automated>
  </verify>
  <done>The shared index exports exactly the two expected slugs, both records retain all required source-derived structures, and both referenced assets exist with stable dimensions and alt metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| W2 source package -> typed content modules | External Markdown crosses into committed runtime content and metadata. |
| Typed content modules -> browser/JSON-LD | Source-derived strings are rendered into HTML and structured data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01-01 | Tampering | `src/content/competitor/dify.ts`, `selfBuild.ts` | high | mitigate | Preserve source hashes and source IDs in records; later manifest validation fails closed on drift. |
| T-03-01-02 | Spoofing | Source and signoff metadata | medium | mitigate | Use explicit typed `sourceId`, role-specific signoff fields, and evidence statuses instead of free-form approval text. |
| T-03-01-03 | Information disclosure | Page facts and assets | medium | mitigate | Keep local delivery paths and internal evidence in manifest-only fields; expose only approved public source labels/URLs through the renderer. |
| T-03-01-04 | Code injection | JSON-LD string fields | high | mitigate | Require the shared JSON-LD renderer to escape `<` in serialized content before inserting `application/ld+json`. |
</threat_model>

<verification>
Run `npx tsc --noEmit` and inspect the two records against the four source-draft metadata blocks. Confirm the index has exactly the two assigned slugs and that every referenced asset is present.
</verification>

<success_criteria>
Typed, source-faithful Dify and self-build records exist with structured five-section content, auditable source/date/signoff/link fields, and stable non-logo page assets ready for manifest and route work.
</success_criteria>

<output>
Create `.planning/phases/03-competitor-comparison-pages/03-01-SUMMARY.md` when done.
</output>
