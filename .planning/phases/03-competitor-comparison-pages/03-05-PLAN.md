---
phase: 03-competitor-comparison-pages
plan: 05
type: execute
wave: 5
depends_on:
  - 03-03
  - 03-04
files_modified:
  - artifacts/phase3/competitor-pages-build-report.json
  - scripts/phase3/test_competitor_build.mjs
  - scripts/verify-p3.js
  - package.json
autonomous: true
requirements:
  - CMP-01
  - CMP-02
  - CMP-03
  - CMP-04
  - CMP-05
must_haves:
  truths:
    - "A production static build contains exactly the four Chinese comparison pages, with no English/other-locale routes or unregistered slugs."
    - "The build report proves every page's structure, metadata, canonical/robots state, Article/Breadcrumb JSON-LD, source footer, links, asset, forbidden-claim audit, and sitemap membership against the manifest."
    - "The phase has one repeatable verification command that fails the release on any route, content, SEO, evidence, responsive-contract, or publication-state mismatch."
  artifacts:
    - path: "scripts/phase3/test_competitor_build.mjs"
      provides: "Static-output and source-content exact-set auditor for four comparison pages"
      contains: "Article"
    - path: "scripts/verify-p3.js"
      provides: "Repository verification entry point for Phase 3 gates"
      contains: "test_competitor_build"
    - path: "artifacts/phase3/competitor-pages-build-report.json"
      provides: "Machine-readable build, page, SEO, content, and responsive-contract evidence"
      contains: "passed"
    - path: "package.json"
      provides: "`verify:p3` script for repeatable release checks"
      contains: "verify:p3"
  key_links:
    - from: "scripts/phase3/test_competitor_build.mjs"
      to: "out/zh/compare"
      via: "exact static HTML route resolution"
      pattern: "compare"
    - from: "scripts/phase3/test_competitor_build.mjs"
      to: "artifacts/phase3/competitor-pages-manifest.json"
      via: "status, slug, metadata, links, assets, and date assertions"
      pattern: "competitor-pages-manifest"
    - from: "scripts/verify-p3.js"
      to: "scripts/phase3/test_competitor_build.mjs"
      via: "verification command delegation"
      pattern: "test_competitor_build"
---

<objective>
建立竞品页的静态构建审计与单命令验证入口，输出可交给 Phase 5 的机器证据。

Purpose: 按 CMP-01 至 CMP-05 和 D-317/D-320/D-326 对四个 URL、内容结构、SEO、结构化数据、状态分发、来源/内链/图片和响应式契约做 exact-set 验收，并把失败变成发布阻断项。
Output: `test_competitor_build.mjs`、`verify:p3`、构建报告和可重放验证命令。
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-competitor-comparison-pages/03-CONTEXT.md
@.planning/phases/03-competitor-comparison-pages/03-03-PLAN.md
@.planning/phases/03-competitor-comparison-pages/03-04-PLAN.md
@scripts/phase2/test_faq_build.mjs
@scripts/verify-p2.js
@artifacts/phase3/competitor-pages-manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit the static comparison build and write evidence</name>
  <files>scripts/phase3/test_competitor_build.mjs, artifacts/phase3/competitor-pages-build-report.json</files>
  <action>Implement an exact-set Node auditor modeled on the Phase 2 static checks. Resolve the four expected `zh/compare/{slug}` HTML files without case-insensitive fallback; fail on missing, duplicate, or extra comparison outputs and on any `en`, `zh-hant`, or other-locale comparison route. For each page, assert source metadata title/description/keywords, `og:type=article`, OG/Twitter parity and image, canonical `https://fastgpt.cn/zh/compare/{slug}`, status-derived robots, no fabricated English hreflang, one H1, exactly five top-level H2 sections, capability/POC/TCO table fields, preview marker when applicable, four footer evidence fields, three verified links, asset existence, Article and BreadcrumbList JSON-LD fields from D-321/D-322, and absence of FAQPage. Run the content audit for D-307 through D-310/D-317: detect price values, out-of-scope named competitors, customer names/case numbers, unsupported absolute claims, and temporary markers while recognizing quoted compliance-discipline statements as explicit exemptions and recording every exemption in the report. Verify the responsive contract by checking emitted table data-label attributes, comparison CSS classes, fixed asset dimensions, and no overflow-prone markup. Compare sitemap locations to published manifest entries only, update each page's content-audit result and the manifest/failure report through the fail-closed validator, and write `competitor-pages-build-report.json` with counts, per-page results, exemptions, and blocking failures.</action>
  <verify>
    <automated>node scripts/phase3/test_competitor_build.mjs</automated>
  </verify>
  <done>The build auditor passes only when all four static pages, content/SEO/JSON-LD/source/link/asset checks, preview/public distribution rules, and responsive contracts match the manifest; it emits a complete report and blocks any failure.</done>
</task>

<task type="auto">
  <name>Task 2: Add the repeatable Phase 3 verification command</name>
  <files>scripts/verify-p3.js, package.json</files>
  <action>Create a small repository verification wrapper that runs the manifest gate and static comparison build auditor with `COMPARE_BUILD_OUT` support, then add `npm run verify:p3` beside the existing Phase 0/1/2 commands. Keep the command dependency-free and fail with the first blocking gate while preserving the JSON reports for Phase 5. Document the production sequence in the script help text: build with telemetry disabled, run the exact-set auditor, and inspect the report before changing any page from preview to published.</action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if (p.scripts['verify:p3'] !== 'node scripts/verify-p3.js') process.exit(1)" && npm run lint</automated>
  </verify>
  <done>`npm run verify:p3` is present, invokes the Phase 3 manifest/build gates, returns nonzero on any blocker, and leaves machine-readable evidence for the Phase 5 release checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build output -> verifier | Generated HTML, sitemap, and assets are treated as release evidence. |
| Verifier reports -> publication workflow | Audit results control manifest status and downstream release decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05-01 | Tampering | Static output and manifest comparison | high | mitigate | Use exact route sets, canonical/status comparisons, source/content hashes, and fail on extra/missing output. |
| T-03-05-02 | Repudiation | Build and failure reports | medium | mitigate | Persist per-page assertions, exemption records, command inputs, and blocking failures in JSON artifacts. |
| T-03-05-03 | Information disclosure | Preview/public distribution | high | mitigate | Assert preview noindex/nofollow and sitemap exclusion; assert published-only sitemap entries before release. |
| T-03-05-04 | Code injection | HTML/JSON-LD audit | high | mitigate | Verify JSON-LD escaping and reject unsafe serialized content or untrusted markup in comparison output. |
| T-03-05-05 | Denial of service | Verification command | low | accept | The audit processes four bounded static pages and uses no network fetch in the build path; link checks are recorded by the manifest gate. |
</threat_model>

<verification>
Run `NEXT_TELEMETRY_DISABLED=1 npm run build`, then `COMPARE_BUILD_OUT=out npm run verify:p3`. Confirm the report has four page results, a zero blocking-failure count, and sitemap membership matching published statuses; complete the end-of-phase desktop/mobile human review from Plan 03-04.
</verification>

<success_criteria>
Phase 3 has a deterministic build and verification gate that proves all CMP requirements and leaves auditable reports for Phase 5 without silently publishing unsigned or unverified pages.
</success_criteria>

<output>
Create `.planning/phases/03-competitor-comparison-pages/03-05-SUMMARY.md` when done.
</output>
