# Plan 04-03: Phase 4 integration audit

## Objective

Prove the Meta overlay and category batch boundary do not alter FAQ routes or body content, and prepare the evidence consumed by Phase 5.

## Tasks

1. Add a single Phase 4 verifier that checks source fingerprints, applied/unresolved Meta counts, category dry-run status, protected field hashes, batch evidence, and runtime field coverage.
2. Run TypeScript, lint, static build, existing Phase 2 checks, and the new Phase 4 checks. Record the macOS case-insensitive static-export limitation separately from case-sensitive CI evidence.
3. Write Phase 4 UAT and verification reports with pass/block status, exact counts, open identity blockers, and Phase 5 handoff inputs.
4. Update roadmap/state only after the phase evidence is complete, preserving the full-batch category blocker and 24 Meta gaps as explicit release items.

## Verification

- `npx tsc --noEmit`
- `npm run lint`
- `NEXT_TELEMETRY_DISABLED=1 npm run build`
- `npm run verify:p4`
- `git diff --check`

