---
phase: 05-guide-content-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/content/guides/zh/05-SaaS智能体平台企业落地的四项书面核实选型清单-V1.0-星触达-20260811.md"
  - "src/content/guides/en/05-EN-Enterprise-SaaS-AI-Agent-Platform-Select-V1.0-XstraStar-20260811.md"
  - src/content/guides/registry.json
  - src/content/guides/registry.ts
  - src/lib/guideContent.ts
  - scripts/verify-guide-content.js
autonomous: true
requirements: [GUIDE-01, GUIDE-02, GUIDE-03]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "GUIDE-01 / D-01 / D-03: one bilingual slug proves repository source → eight-entry identity manifest → typed facade → server-only reader → standalone verifier end to end."
    - "GUIDE-02 / D-02: the parser normalizes only line endings, removes exactly one byte-zero delivery comment, preserves the complete normalized suffix including two blank lines, and verifies its SHA-256."
    - "D-03 / D-04: the registry already enumerates all eight lower-case slugs and stores each locale's own H1/metadata/canonical/hreflang/schema snapshot without cross-locale text equality."
    - "D-05 / D-06: source image/link directives are distinct from approved assets and configured links; no approval is inferred from prose."
    - "D-07: filesystem reads are server-only, dependency-free, and contained under src/content/guides/{zh,en}."
  prohibitions:
    - statement: "Build or verification code depends on the Downloads directories after source import."
      status: resolved
      verification: "Only task preconditions and byte-copy checks name delivery paths; runtime roots at src/content/guides."
    - statement: "Phase 5 adds Guide route, UI, render, SEO, JSON-LD, sitemap, image, or export surfaces."
      status: resolved
      verification: "files_modified contains only content contract, loader, and verifier artifacts."
    - statement: "Source labels become inferred URLs or image requests become generated assets."
      status: resolved
      verification: "Typed source directives remain separate from configuredInternalLinks and approvedAsset."
  artifacts:
    - path: src/content/guides/registry.json
      provides: "Single cross-runtime eight-pair manifest"
    - path: src/content/guides/registry.ts
      provides: "Typed registry facade and lookup API"
    - path: src/lib/guideContent.ts
      provides: "Server-only strict source parser and body builder"
    - path: scripts/verify-guide-content.js
      provides: "Standalone full, slug-focused, and locale-focused verifier"
  key_links:
    - from: src/content/guides/registry.ts
      to: src/content/guides/registry.json
      via: "runtime assertion narrows one manifest to GuideEntry[]"
      pattern: "registry\\.json"
    - from: src/lib/guideContent.ts
      to: src/content/guides/registry.ts
      via: "getGuideSource selects a contained locale file before readFileSync"
      pattern: "getGuideSource"
    - from: scripts/verify-guide-content.js
      to: src/content/guides/registry.json
      via: "CommonJS reads the same registry manifest"
      pattern: "registry\\.json"
---

<objective>
Prove the complete Guide source-contract architecture with the `saas-platform-enterprise-gaps` bilingual pair while establishing the final typed identity and validation interfaces for all eight pairs, implementing D-01 through D-07.

Purpose: Catch a wrong source/registry/parser/verifier design before the remaining fourteen documents are imported.
Output: One repository-owned bilingual tracer pair, the final eight-entry registry contract, a strict server-only loader, and a focused standalone verifier.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-guide-content-contract/05-CONTEXT.md
@.planning/phases/05-guide-content-contract/05-RESEARCH.md
@src/lib/tech-center-content.ts
@src/content/competitor/types.ts
@src/content/competitor/index.ts
@src/content/competitor/loader.ts
@scripts/verify-deep-content.js
@src/lib/siteRouting.ts
@next.config.js
@package.json

<interfaces>
- `src/lib/tech-center-content.ts` establishes `import 'server-only'`, `fs.readFileSync`, `path.join(process.cwd(), ...)`, newline normalization, and descriptive build-time failures.
- `src/content/competitor/index.ts` establishes typed locale-pair lookup and slug enumeration; `src/content/competitor/types.ts` establishes explicit link/asset records.
- `scripts/verify-deep-content.js` establishes dependency-free `node:fs`, `node:path`, `node:crypto`, source hashes, and slug-specific CLI failures.
- `src/lib/siteRouting.ts` exports `getLocaleOwner`, `getSiteBaseUrl`, `getOwnedLocalePath`, and `getOwnedLocaleUrl` for later owned canonical derivation.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Carry one bilingual source through the complete contract path</name>
  <precondition>`/Users/longnv/Downloads/Week04/深度内容-第2批8篇` contains eight numbered zh articles and `/Users/longnv/Downloads/Week04/深度内容-英文版8篇` contains eight numbered en articles plus the GSC appendix.</precondition>
  <reversibility rating="costly">D-03's identity and field shape will be consumed by Phases 6–8, so changing it later requires coordinated route, SEO, sitemap, and release updates.</reversibility>
  <files>
src/content/guides/zh/05-SaaS智能体平台企业落地的四项书面核实选型清单-V1.0-星触达-20260811.md,
src/content/guides/en/05-EN-Enterprise-SaaS-AI-Agent-Platform-Select-V1.0-XstraStar-20260811.md,
src/content/guides/registry.json,
src/content/guides/registry.ts,
src/lib/guideContent.ts,
scripts/verify-guide-content.js
  </files>
  <read_first>
- `.planning/phases/05-guide-content-contract/05-CONTEXT.md` — D-01 through D-07 are locked.
- `.planning/phases/05-guide-content-contract/05-RESEARCH.md` — exact slugs, metadata, schema, asset, and link facts.
- `/Users/longnv/Downloads/Week04/深度内容-第2批8篇/05-SaaS智能体平台企业落地的四项书面核实选型清单-V1.0-星触达-20260811.md` — approved zh tracer bytes.
- `/Users/longnv/Downloads/Week04/深度内容-英文版8篇/05-EN-Enterprise-SaaS-AI-Agent-Platform-Select-V1.0-XstraStar-20260811.md` — approved en tracer bytes.
- `src/lib/tech-center-content.ts`, `src/content/competitor/types.ts`, `src/content/competitor/index.ts`, `scripts/verify-deep-content.js`, `src/lib/siteRouting.ts` — implementation patterns and owned URL authority.
  </read_first>
  <action>
Copy the two numbered `05` sources byte-for-byte to the exact basenames under `src/content/guides/zh` and `src/content/guides/en` per D-01. Create `registry.json` as the single cross-runtime data manifest and enumerate exactly these eight lower-case slugs: `saas-platform-enterprise-gaps`, `self-build-three-year-tco`, `server-sizing-guide`, `complex-doc-golden-set`, `support-bot-four-steps`, `manufacturing-itops-invoice-audit`, `pharma-compliance-docs`, and `education-retail-support-insight`. Every entry has exactly `zh` and `en`; every locale record stores the exact source basename, raw-source SHA-256, LF-normalized publishable-body SHA-256, localized H1, `Meta title`, `Meta description`, `keywords`, canonical, raw hreflang, schema tokens, raw `配图需求`, `sourceInternalLinkLabels`, optional `approvedAsset`, and `configuredInternalLinks`. Populate snapshots from each locale's own source, preserving truncated English endings per D-03/D-04.

Create `registry.ts` as the typed facade. Export `GUIDE_LOCALES`, `GuideLocale`, `GUIDE_SCHEMA_TYPES`, `GuideSchemaType`, `GuideSourceSnapshot`, `GuideAssetPolicy`, `GuideInternalLinkMapping`, `GuideEntry`, `guideEntries`, `guideSlugs`, `getGuideEntry`, and `getGuideSource`. Validate unknown JSON before narrowing: eight unique lower-case slugs, exact locale pairs, basename-only source names, and schema arrays limited to `Article`, `BreadcrumbList`, and optional `HowTo`.

Create `guideContent.ts` with `import 'server-only'`, fixed repository roots, `normalizeGuideSource(source)`, `parseGuideDeliverySource(source, expected)`, and `readGuideDocument(slug, locale)`. Normalize only CRLF/CR to LF; require one comment beginning at byte zero; parse known metadata lines at the first colon; require the suffix to start with `\n\n#`; return it without trimming; compare all source-specific snapshot fields and both hashes. Reject absolute names, separators, traversal, and resolved paths outside the fixed locale directory per D-02/D-07.

Create side-effect-free CommonJS `verify-guide-content.js` exporting `parseArgs`, `parseDeliverySource`, `verifyGuideRegistry`, `verifyGuideContent`, and `main`. Support no filter, `--slug <slug>`, or `--locale <zh|en>`; filters still validate global eight-entry identity and limit only filesystem/source checks. Use Node built-ins and prefix failures with slug, plus label for link failures. Verify the `saas-platform-enterprise-gaps` pair through registry, path, metadata, schema, directive, comment, and digest checks. Add no route, UI, SEO, sitemap, image, dependency, or client fetch surface.
  </action>
  <acceptance_criteria>
- Both committed `05` files are byte-identical to the supplied inputs and retain the exact basenames.
- The manifest has eight unique slugs and exactly two locale snapshots per slug; the typed facade exports all named types, constants, records, and lookups.
- The loader returns the exact LF-normalized suffix beginning `\n\n#` for both tracer sources, and both source/body digests match.
- `--slug saas-platform-enterprise-gaps` validates global identity plus the two tracer documents and prints one concise English success line.
- Dependency objects and package-lock remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-guide-content.js --slug saas-platform-enterprise-gaps &amp;&amp; npx --no-install tsc --noEmit</automated>
  </verify>
  <done>The permanent source→identity→loader→verifier path works for one real bilingual Guide pair.</done>
</task>

<task type="auto">
  <name>Task 2: Encode final asset, link, schema, and containment invariants</name>
  <files>src/content/guides/registry.json, src/content/guides/registry.ts, src/lib/guideContent.ts, scripts/verify-guide-content.js</files>
  <read_first>
- The files produced by Task 1 — retain their public identifiers.
- `.planning/phases/05-guide-content-contract/05-RESEARCH.md` sections `Exact pair identity and delivery directives`, `Common Pitfalls`, and `Security Domain`.
- `/Users/longnv/Downloads/Week04/深度内容-第2批8篇/*.md` and `/Users/longnv/Downloads/Week04/深度内容-英文版8篇/[0-9]*.md` — source-only metadata/hash inputs for registry snapshots; do not create a runtime dependency.
  </read_first>
  <action>
Finalize schema arrays exactly: `server-sizing-guide`, `complex-doc-golden-set`, and `support-bot-four-steps` use `HowTo`, `Article`, `BreadcrumbList`; the other five use `Article`, `BreadcrumbList`. Preserve raw schema lines while excluding `FAQPage` as a token. Encode asset status exactly per D-05: three `none` pairs (`saas-platform-enterprise-gaps`, `support-bot-four-steps`, `pharma-compliance-docs`), four `requested-unapproved` pairs (`server-sizing-guide`, `complex-doc-golden-set`, `manufacturing-itops-invoice-audit`, `education-retail-support-insight`), and one `source-exception` pair (`self-build-three-year-tco`) retaining separate zh blank and en malformed sign-off values. Current entries contain no `approvedAsset`; a future `required` status needs a contained repository-relative public path plus non-empty authored alt.

Store every parsed `内链` label in `sourceInternalLinkLabels` and keep current `configuredInternalLinks` arrays empty because no approved destinations exist. Validate any configured mapping only when it supplies an exact source label plus an absolute canonical target on `fastgpt.cn` or `fastgpt.io` whose pathname resolves through the known app/Guide/technical/comparison inventory. Empty, fragment-only, foreign, malformed, and missing-inventory targets throw slug+label errors per D-06. Ensure the English GSC appendix basename appears in no entry and filenames remain contained. Keep all remaining source files absent until Plans 05-02 and 05-03 import them.
  </action>
  <acceptance_criteria>
- Registry counts are eight slugs, sixteen snapshots, three `none`, four `requested-unapproved`, one `source-exception`, zero approved assets, and zero configured links.
- Source directives preserve the self-build blank/malformed values line-by-line and all 20 source link labels without inferred targets.
- Allowed schema tokens are exactly `Article`, `BreadcrumbList`, and `HowTo`; every filename passes basename and containment policy.
- Focused tracer verification and TypeScript compilation remain green after final invariant encoding.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-guide-content.js --slug saas-platform-enterprise-gaps &amp;&amp; npx --no-install tsc --noEmit</automated>
  </verify>
  <done>The final typed contract can accept the remaining fourteen raw sources without an interface or policy change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Delivery package → repository | Approved external Markdown becomes build input. |
| Registry filename → filesystem | Manifest data selects a locale source path. |
| Delivery comment → body | Internal metadata must be removed exactly once. |
| Link/asset configuration → owned inventory | Configuration may reference foreign, missing, or escaping resources. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Tampering / Elevation of Privilege | Registry filenames and `readGuideDocument` | high | mitigate | Basename validation, absolute/separator rejection, and resolved containment under fixed locale roots. |
| T-05-02 | Information Disclosure | `parseGuideDeliverySource` | medium | mitigate | One byte-zero comment, exact `\n\n#` suffix, no trim, and body SHA-256. |
| T-05-03 | Tampering | Metadata, schema, links, and assets | medium | mitigate | Source-specific snapshot comparison, schema union, owned inventory checks, and explicit policy unions. |
| T-05-SC | Tampering | npm supply chain | high | mitigate | Node built-ins plus existing `server-only`; no install, dependency, or lockfile change. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- Two raw tracer Markdown files at the exact paths in frontmatter.
- `registry.json`: one eight-entry bilingual runtime manifest.
- `registry.ts`: `GUIDE_LOCALES`, `GuideLocale`, `GUIDE_SCHEMA_TYPES`, `GuideSchemaType`, `GuideSourceSnapshot`, `GuideAssetPolicy`, `GuideInternalLinkMapping`, `GuideEntry`, `guideEntries`, `guideSlugs`, `getGuideEntry`, `getGuideSource`.
- `guideContent.ts`: `GuideDeliveryMetadata`, `GuideDocument`, `normalizeGuideSource`, `parseGuideDeliverySource`, `readGuideDocument`.
- `verify-guide-content.js`: `parseArgs`, `parseDeliverySource`, `verifyGuideRegistry`, `verifyGuideContent`, `main`, `--slug`, and `--locale`.
</artifacts_this_phase_produces>

<verification>
1. `node scripts/verify-guide-content.js --slug saas-platform-enterprise-gaps`
2. `npx --no-install tsc --noEmit`
3. `git diff --exit-code -- package-lock.json`
</verification>

<success_criteria>
- The tracer pair passes every final source-contract layer.
- The typed registry enumerates the final eight-pair identity and source policies without requiring all raw files yet.
- Runtime source reads are repository-contained and server-only.
- Phase 6 publishing files and dependency state remain untouched.
</success_criteria>

<output>
Create `.planning/phases/05-guide-content-contract/05-01-SUMMARY.md` when done.
</output>
