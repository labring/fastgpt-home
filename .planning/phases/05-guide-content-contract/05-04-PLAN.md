---
phase: 05-guide-content-contract
plan: "04"
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - scripts/verify-guide-content.js
  - scripts/verify-guide-content.test.js
  - package.json
autonomous: true
requirements: [GUIDE-03]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "GUIDE-03 / D-08: one node:test regression command proves slug-specific failures for duplicate slug, incomplete pair, localized metadata mismatch, invalid schema, missing required asset, unresolved configured link, malformed leading comment, and changed body suffix."
    - "D-05: setting asset status to required activates contained public-path, existence, and non-empty authored-alt validation; current unapproved requests remain non-renderable data."
    - "D-06: configuring an empty, fragment-only, foreign, malformed, or unknown-inventory link activates slug+label failure; raw source labels alone remain unpublished."
    - "D-07: verifier imports are silent and side-effect-free, use Node built-ins, and add no package."
    - "All GUIDE-01/02/03 phase checks pass together for exactly eight slugs and sixteen documents."
  prohibitions:
    - statement: "Regression tests mutate committed Guide sources or leave temporary fixtures behind."
      status: resolved
      verification: "Tests use deep clones or temporary directories and cleanup in finally."
    - statement: "package.json edits change dependency or devDependency objects, or package-lock.json changes."
      status: resolved
      verification: "Only two verification script keys are added; lockfile diff gate remains clean."
    - statement: "The phase implements routes, UI/rendering, canonical metadata output, JSON-LD, sitemap, assets, or export verification."
      status: resolved
      verification: "files_modified is verifier, test, and package scripts only."
  artifacts:
    - path: scripts/verify-guide-content.test.js
      provides: "Minimal node:test mutation matrix for GUIDE-03/D-08"
    - path: package.json
      provides: "Stable source-contract and regression command names"
  key_links:
    - from: scripts/verify-guide-content.test.js
      to: scripts/verify-guide-content.js
      via: "direct pure-function imports and isolated fixture mutation"
      pattern: "require\('./verify-guide-content'\)"
    - from: package.json
      to: scripts/verify-guide-content.test.js
      via: "verify:guide-content-regression invokes node --test"
      pattern: "verify:guide-content-regression"
---

<objective>
Seal the complete GUIDE-03 and D-08 failure contract behind one focused regression command while preserving the green 8×2 source verifier.

Purpose: Turn every locked source/identity/body/asset/link invariant into executable evidence before Phase 6 consumes the contract.
Output: A side-effect-free mutation regression suite and stable npm verification commands.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05-guide-content-contract/05-CONTEXT.md
@.planning/phases/05-guide-content-contract/05-RESEARCH.md
@.planning/phases/05-guide-content-contract/05-01-SUMMARY.md
@.planning/phases/05-guide-content-contract/05-02-SUMMARY.md
@.planning/phases/05-guide-content-contract/05-03-SUMMARY.md
@scripts/verify-guide-content.js
@scripts/verify-release.test.js
@scripts/verify-deep-content.js
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove every slug-specific negative contract with isolated mutations</name>
  <files>scripts/verify-guide-content.js, scripts/verify-guide-content.test.js</files>
  <read_first>
- `scripts/verify-guide-content.js` — retain its full, slug, and locale CLI behavior while exposing pure validation seams.
- `scripts/verify-release.test.js` — follow `node:test`, `assert/strict`, temp-fixture cleanup, and silent CommonJS import conventions.
- `scripts/verify-deep-content.js` — retain concise slug-specific metadata/hash/asset/link diagnostics.
- `.planning/phases/05-guide-content-contract/05-CONTEXT.md` D-05 through D-08 — exact negative matrix and no-inference rules.
  </read_first>
  <behavior>
- Approved manifest plus sixteen repository sources passes and prints `Guide content verified: 8 slugs, 16 documents`.
- Duplicate `saas-platform-enterprise-gaps` fails with that slug and `duplicate`.
- Removing `en` from `server-sizing-guide` fails with that slug and `locale pair`.
- Mutating one localized title fails with its slug and `metadata`; mutating suffix bytes fails with its slug and `body differs`.
- Adding `FAQPage` fails with its slug and `schema`.
- Marking an asset `required` without a contained existing public path and non-empty authored alt fails with its slug and `asset`.
- Adding a configured link with empty, fragment-only, foreign, malformed, or unknown-inventory target fails with its slug and exact source label.
- Missing byte-zero comment, unterminated comment, or second leading comment before H1 fails with its slug and `delivery comment`.
  </behavior>
  <action>
Create `verify-guide-content.test.js` with Node `node:test` and `node:assert/strict`. Start with failing tests for the approved-corpus success message and every behavior above, then expose/refine pure `parseArgs`, `parseDeliverySource`, `verifyGuideRegistry`, and `verifyGuideContent` seams until green. Use `structuredClone` for manifest mutations and `fs.mkdtempSync` for source/body fixtures; restore or remove fixtures in `finally` and never edit committed sources. Add a test that `require('./scripts/verify-guide-content.js')` writes no stdout/stderr and performs no CLI work. Each negative assertion must match the slug; link cases must also match the label. Cover D-05/D-06 future configured states without inventing a current approved asset or target.
  </action>
  <acceptance_criteria>
- The test file covers approved success plus duplicate, pair, metadata, schema, asset, link, comment, and body-fidelity mutations.
- Each negative check asserts the affected slug; all link mutations assert the source label.
- Temporary fixtures are removed even after assertions throw, and committed Guide sources remain unchanged.
- Requiring the verifier is silent and side-effect-free.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/verify-guide-content.test.js</automated>
  </verify>
  <done>The complete D-08 mutation matrix fails for the intended reason with actionable slug-specific diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Publish stable phase verification commands and run the contract gate</name>
  <files>package.json</files>
  <read_first>
- `package.json` — preserve existing scripts and all dependency versions.
- `scripts/verify-guide-content.js`, `scripts/verify-guide-content.test.js` — command entry points produced by the phase.
  </read_first>
  <action>
Add `verify:guide-content` with value `node scripts/verify-guide-content.js` and `verify:guide-content-regression` with value `node --test scripts/verify-guide-content.test.js`. Change no dependency/devDependency key or value and leave package-lock.json untouched. Run both commands plus `npx --no-install tsc --noEmit`; retain the exact full-corpus success count and all mutation failures. Keep build/export verification assigned to Phase 7 and route/UI/SEO/sitemap work assigned to Phase 6.
  </action>
  <acceptance_criteria>
- Both named npm scripts exist with the exact commands and exit 0.
- Full verification reports exactly eight slugs and sixteen documents.
- Regression verification covers every GUIDE-03/D-08 case and exits 0 only when all expected failures are observed.
- TypeScript compilation passes, dependency objects are unchanged, and package-lock has no diff.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:guide-content &amp;&amp; npm run verify:guide-content-regression &amp;&amp; npx --no-install tsc --noEmit</automated>
  </verify>
  <done>Maintainers have stable, dependency-free source and regression commands for the complete Phase 5 contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Mutation fixtures → verifier | Adversarial registry/source values exercise trust-boundary validation. |
| npm command → repository corpus | A stable command must verify the committed source of truth without external inputs. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-10 | Tampering | Mutation regression fixtures | medium | mitigate | Deep clones/temp directories, explicit cleanup, and unchanged committed-source checks. |
| T-05-11 | Repudiation | Slug/link diagnostics | medium | mitigate | Every failure includes slug; link failures also include exact label and reason category. |
| T-05-12 | Tampering / Information Disclosure | Comment, path, asset, and link boundaries | high | mitigate | Dedicated adversarial tests for second/malformed comment, body drift, required missing asset, foreign/unknown links, and path containment. |
| T-05-SC | Tampering | npm supply chain | high | mitigate | Scripts only; Node built-ins, no install, no dependency or lockfile change. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `scripts/verify-guide-content.test.js`: node:test mutation harness.
- `scripts/verify-guide-content.js`: finalized pure exports `parseArgs`, `parseDeliverySource`, `verifyGuideRegistry`, `verifyGuideContent`, `main`.
- `package.json`: `verify:guide-content` and `verify:guide-content-regression`.
</artifacts_this_phase_produces>

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | One validated, source-faithful bilingual contract for all publishable Guide articles. | 05-01–04 | COVERED | Tracer, two bounded locale imports, then regression seal. |
| REQ | GUIDE-01 | Exactly eight unique slugs with one zh and one en approved source each. | 05-01–03 | COVERED | Final full verifier counts 8×2. |
| REQ | GUIDE-02 | Exactly one leading comment removal with normalized suffix preserved. | 05-01–04 | COVERED | Parser, two digests, corpus checks, mutations. |
| REQ | GUIDE-03 | Slug-specific pair/metadata/schema/asset/link failures. | 05-01, 05-04 | COVERED | Standalone verifier plus node:test matrix. |
| RESEARCH | — | Commit 16 numbered documents; exclude English GSC appendix. | 05-01–03 | COVERED | Exact numbered paths only. |
| RESEARCH | — | Source-specific localized metadata, reciprocal identity, allowed Article/BreadcrumbList/HowTo schemas. | 05-01–04 | COVERED | Typed snapshots plus full/mutation checks. |
| RESEARCH | — | Four image requests, three `无`, self-build blank/malformed, zero approved assets. | 05-01, 05-04 | COVERED | Explicit policy counts and required-asset test. |
| RESEARCH | — | Link labels need explicit owned targets; unresolved configuration fails. | 05-01, 05-04 | COVERED | Raw/publishable separation and adversarial targets. |
| RESEARCH | — | Node built-ins, server-only, path containment, SHA-256, no package. | 05-01, 05-04 | COVERED | Named implementation and supply-chain gates. |
| CONTEXT | D-01 | Repository-owned 16-file boundary with exact names/bytes. | 05-01–03 | COVERED | One tracer pair plus seven zh plus seven en. |
| CONTEXT | D-02 | LF normalization, exactly one byte-zero comment, preserved suffix/digest. | 05-01–04 | COVERED | Loader and regression. |
| CONTEXT | D-03 | One typed eight-entry registry. | 05-01 | COVERED | One JSON manifest plus typed facade, not parallel registries. |
| CONTEXT | D-04 | Locale compares to its own source; cross-locale equality remains identity-only. | 05-01–03 | COVERED | Localized snapshots and corpus checks. |
| CONTEXT | D-05 | Preserve asset directives; configured required asset validates path/alt. | 05-01, 05-04 | COVERED | Explicit policies and mutation. |
| CONTEXT | D-06 | Explicit owned link targets; unresolved/foreign/malformed fails with slug+label. | 05-01, 05-04 | COVERED | No inferred current mappings and full negative set. |
| CONTEXT | D-07 | Server/build-only, strict Node/path/hash checks, no dependency. | 05-01–04 | COVERED | Server-only loader and standalone Node gate. |
| CONTEXT | D-08 | Standalone verifier and runnable regression cover all named failures. | 05-04 | COVERED | Two stable npm commands. |
| CONTEXT | Deferred | Route/UI/rendering/SEO/schema output/sitemap/export work. | — | EXCLUDED | Explicitly assigned to Phases 6–7. |
</source_audit>

<risks_dependencies>
## Risks and dependencies

- Plan 05-01 fixes the shared contract; Plans 05-02/03 only copy approved bytes; Plan 05-04 tests the final state. This ordering keeps code interfaces stable and each expansion at seven files.
- The Week04 delivery directories are needed only during Plans 05-01–03 for byte copying/comparison. The committed verifier has no external path dependency.
- The source package supplies no approved image files/alt text and no approved link destinations. The safe current registry state records directives as unpublished; any later configured state must pass the regression-backed validators.
- Phase 6 depends on the exported registry/loader names. This phase intentionally leaves route, render, SEO, schema-output, and sitemap integration untouched.
</risks_dependencies>

<verification>
1. `npm run verify:guide-content`
2. `npm run verify:guide-content-regression`
3. `npx --no-install tsc --noEmit`
4. `git diff --exit-code -- package-lock.json`
5. Confirm the phase diff contains exactly 16 raw sources, registry JSON/TS, server-only loader, verifier/test, and package scripts; no Phase 6 path appears.
</verification>

<success_criteria>
- GUIDE-01: full verifier prints exactly `8 slugs, 16 documents`, with the appendix excluded.
- GUIDE-02: each body equals the exact LF-normalized suffix after one comment and matches its digest.
- GUIDE-03: every locked negative case produces the expected slug-specific diagnostic; link failures include labels.
- D-01 through D-08 are covered without new dependencies or Phase 6 publishing work.
</success_criteria>

<output>
Create `.planning/phases/05-guide-content-contract/05-04-SUMMARY.md` when done.
</output>
