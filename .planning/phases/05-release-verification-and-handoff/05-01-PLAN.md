# Plan 05-01: Build the complete W2 release handoff

## Objective

Create a machine-readable release manifest with exact 64/2,100 item coverage, source fingerprints, URLs, batch IDs, statuses, blockers, commands, and rollback instructions.

## Tasks

1. Read the Phase 1 FAQ baseline, Phase 3 comparison manifest, Phase 4 Meta report, and Phase 4 category dry-run.
2. Generate `artifacts/phase5/release-handoff.json` with item-level rows and a strict aggregate gate.
3. Add a verifier that rejects count drift, missing source evidence, compare pages marked published without signoffs, and category writes above zero.

## Verification

- `node scripts/phase5/build_release_handoff.mjs`
- `node scripts/phase5/test_release_handoff.mjs`
