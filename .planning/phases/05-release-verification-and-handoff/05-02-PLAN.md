# Plan 05-02: Run final checks and document handoff gates

## Objective

Re-run the cross-phase checks, record the actual environment results, and produce the final UAT and verification evidence without hiding open publication blockers.

## Tasks

1. Run TypeScript, lint, production build, Phase 1/2/3/4 checks, and the release verifier.
2. Record manual browser and live URL checks as pending when the environment cannot perform them.
3. Add `05-UAT.md` and `05-VERIFICATION.md`, then update roadmap/state to the final release-gated status.

## Verification

- `npx tsc --noEmit`
- `npm run lint`
- `NEXT_TELEMETRY_DISABLED=1 npm run build`
- `npm run verify:p4`
- `COMPARE_BUILD_OUT=out npm run verify:p3`
- `npm run verify:p5`
- `git diff --check`
