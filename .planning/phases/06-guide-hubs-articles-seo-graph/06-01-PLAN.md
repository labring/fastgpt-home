---
phase: 06-guide-hubs-articles-seo-graph
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/guides/registry.json
  - src/content/guides/registry.ts
  - src/lib/guideSeo.ts
  - src/components/guide/GuideArticleRoute.tsx
  - src/components/guide/GuideArticlePage.tsx
  - src/app/guide/[slug]/page.tsx
  - scripts/verify-guide-content.js
  - scripts/verify-guide-content.test.js
  - scripts/verify-guide-seo-graph.js
autonomous: true
requirements: [HUB-01, ARTICLE-01, ARTICLE-02, ARTICLE-03, SEO-04, SEO-05, SEO-06, SEO-07]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "ARTICLE-01 / D-01 / D-04: one real root /guide/<slug> path traverses registry lookup, contained source loading, route metadata, server rendering, authored H1/body, visible breadcrumb, JSON-LD, and a focused verifier before the inventory expands."
    - "HUB-01 / D-03: every registry entry carries exactly one publication group from decision, implementation, or industry, with the approved 4/1/3 assignment."
    - "SEO-04 / D-07: every localized snapshot carries typed ISO datePublished and dateModified values, both fixed to the approved Week04 delivery date 2026-08-11, and article metadata plus JSON-LD consume those fields."
    - "SEO-05 / D-07: Guide URL projection produces one self canonical and exactly en, zh-CN, and x-default alternates through owned-domain helpers."
    - "SEO-06 / D-08: the tracer emits Article and BreadcrumbList with registry-derived names and owned URLs through JsonLdScript."
    - "ARTICLE-03 / D-05 and ARTICLE-02 / D-06: current asset policies and configuredInternalLinks stay unchanged; publication branches remain conditional on approved registry data."
  artifacts:
    - path: src/lib/guideSeo.ts
      provides: "Typed Guide locale, route, canonical, alternate, metadata, and schema projection"
      exports:
        - resolveGuideLocale
        - getGuideBuildLocales
        - getGuidePath
        - getGuideCanonicalUrl
        - getGuideAlternates
        - getGuideArticleMetadata
        - getGuideHubMetadata
    - path: src/components/guide/GuideArticleRoute.tsx
      provides: "Server-only Guide article shell and structured-data composition"
    - path: src/components/guide/GuideArticlePage.tsx
      provides: "Approved H1/body tracer presentation using MarkdownContent"
    - path: src/app/guide/[slug]/page.tsx
      provides: "Closed root Guide article route inventory"
    - path: scripts/verify-guide-seo-graph.js
      provides: "Dependency-free focused source graph verifier with tracer and expansion modes"
  key_links:
    - from: src/app/guide/[slug]/page.tsx
      to: src/components/guide/GuideArticleRoute.tsx
      via: "default-locale root alias delegates rendering and metadata"
      pattern: "GuideArticleRoute|getGuideArticleMetadata"
    - from: src/components/guide/GuideArticleRoute.tsx
      to: src/lib/guideContent.ts
      via: "readGuideDocument loads the approved normalized body"
      pattern: "readGuideDocument"
    - from: src/components/guide/GuideArticleRoute.tsx
      to: src/lib/guideSeo.ts
      via: "canonical, alternates, Open Graph URL, timing, and schema URLs share one projection"
      pattern: "getGuide"
    - from: src/lib/guideSeo.ts
      to: src/lib/siteRouting.ts
      via: "owned path and URL helpers establish both production domains"
      pattern: "getOwnedLocale(Path|Url)"
---

<objective>
Establish the Phase 6 publication architecture with one production-quality root Guide article tracer, then expand its registry contract to all eight bilingual entries, implementing D-01, D-03, D-04, D-07, and D-08 while preserving the conditional D-05/D-06 policies.

Purpose: Prove source → registry → owned URL/SEO projection → App Router → server-rendered article → JSON-LD → dependency-free verification before the hub and localized adapters build on it.
Output: Typed publication fields, central Guide SEO projection, root article route/components, and a focused Phase 6 verifier entry point.

Decision trace: D-01: owned root article topology; D-03: registry groups; D-04: source-faithful Markdown rendering; D-05: conditional required assets; D-06: conditional configured links; D-07: metadata, canonical, alternates, and timing; D-08: registry-derived article schema.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-guide-hubs-articles-seo-graph/06-CONTEXT.md
@.planning/phases/06-guide-hubs-articles-seo-graph/06-RESEARCH.md
@.planning/phases/05-guide-content-contract/05-01-SUMMARY.md
@.planning/phases/05-guide-content-contract/05-04-SUMMARY.md
@src/content/guides/registry.json
@src/content/guides/registry.ts
@src/lib/guideContent.ts
@src/lib/siteRouting.ts
@src/lib/seo.ts
@src/components/compare/ComparisonRoute.tsx
@src/components/tech-center/MarkdownContent.tsx
@src/components/tech-center/TechArticlePage.tsx
@src/components/tech-center/TechArticlePage.module.css
@src/components/JsonLd.tsx
@scripts/verify-guide-content.js
@scripts/verify-guide-content.test.js
@package.json

<interfaces>
- `src/content/guides/registry.ts` exports `GUIDE_LOCALES`, `GuideLocale`, `GuideSourceSnapshot`, `GuideEntry`, `guideEntries`, `guideSlugs`, `getGuideEntry`, and `getGuideSource`; extend these identifiers in place.
- `src/lib/guideContent.ts` exports `readGuideDocument(slug, locale): GuideDocument`; its returned `body` is the exact normalized authored suffix and its `source` is the validated localized snapshot.
- `src/lib/siteRouting.ts` exports `getBuildLocaleCodes()`, `getOwnedLocalePath(locale, path)`, and `getOwnedLocaleUrl(locale, path)`; the two Guide locales already map to unprefixed owned paths.
- `src/components/tech-center/MarkdownContent.tsx` accepts `{ markdown, title }` and suppresses the first matching authored H1 while retaining subsequent headings, lists, tables, blockquotes, code, emphasis, and links.
- `src/components/JsonLd.tsx` exports `JsonLdScript` and `BreadcrumbJsonLd`; `JsonLdScript` serializes and escapes structured data.
- `src/components/compare/ComparisonRoute.tsx` demonstrates `getDictionary`, `Navbar`, `Footer`, `HomeThemeFix`, route-level `notFound()`, metadata, and owned schema URLs.
</interfaces>
</context>

<tasks>

<step type="checkpoint:decision" status="resolved">
  <name>Resolved decision checkpoint: Confirm the D-01 owned root topology</name>
  <decision>D-01: publish indexable Guide hubs/articles at owned root `/guide` paths, with localized build adapters sharing those canonicals.</decision>
  <context>This is a one-way public URL contract; later reversal requires redirects and search-index migration.</context>
  <resolution>The user confirmed this exact topology in 06-CONTEXT.md and reconfirmed autonomous execution. The executor records the decision and continues without pausing.</resolution>
</step>

<task type="tracer">
  <name>Task 1: Publish one root Guide article through every runtime layer</name>
  <files>src/lib/guideSeo.ts, src/components/guide/GuideArticleRoute.tsx, src/components/guide/GuideArticlePage.tsx, src/app/guide/[slug]/page.tsx, scripts/verify-guide-seo-graph.js</files>
  <read_first>
- `.planning/phases/06-guide-hubs-articles-seo-graph/06-CONTEXT.md` D-01, D-02, D-04, D-07, and D-08 — root topology, server shell, source rendering, SEO, and schema contracts.
- `src/content/guides/registry.ts` and `src/lib/guideContent.ts` — existing identity and contained source-reader interfaces.
- `src/lib/siteRouting.ts` and `src/lib/seo.ts` — owned URL and exact bilingual alternate patterns.
- `src/components/compare/ComparisonRoute.tsx`, `src/components/tech-center/TechArticlePage.tsx`, `src/components/tech-center/TechArticlePage.module.css`, and `src/components/tech-center/MarkdownContent.tsx` — shell, long-form styling, accessibility, and body rendering patterns.
- `src/components/JsonLd.tsx` — escaped JSON-LD primitive and breadcrumb shape.
  </read_first>
  <action>
Create `src/lib/guideSeo.ts` as the single Guide URL/SEO projection. Export typed locale resolution that accepts only `zh` or `en`; `getGuidePath(slug?)`; `getGuideCanonicalUrl(locale, slug?)`; `getGuideAlternates(locale, slug?)`; build-locale filtering based on `getBuildLocaleCodes()` plus the current default-locale fallback; and article/hub metadata helpers with an `indexable` option. Build every path through `getOwnedLocalePath()` and every absolute URL through `getOwnedLocaleUrl()` per D-01/D-07. Emit exactly `en`, `zh-CN`, and `x-default`, with English and x-default sharing the io URL. Keep hub copy as one typed zh/en constant in this module so later routes consume one implementation.

Create `GuideArticleRoute.tsx` and `GuideArticlePage.tsx` for the real `saas-platform-enterprise-gaps` path. Resolve `getGuideEntry()` and `readGuideDocument()` at build time; route missing identity through `notFound()`. Render the existing localized dictionary, `HomeThemeFix`, `Navbar`, `Footer`, a visible Home → Guide → article breadcrumb, one route-owned H1, summary, hub return link, and `MarkdownContent` over the complete normalized body per D-02/D-04. Reuse `TechArticlePage.module.css` for the established light-theme, responsive, focus-visible, table, code, and long-form rules. Emit Article and BreadcrumbList through `JsonLdScript` using the same canonical projection as metadata per D-08. The tracer snapshot has no approved required asset and no configured links, so those empty branches produce no visual node.

Create the root `src/app/guide/[slug]/page.tsx` as a thin default-locale alias with `dynamicParams = false`, one tracer static parameter, delegated rendering, and delegated metadata. Create side-effect-free CommonJS `scripts/verify-guide-seo-graph.js` with `--slug`, `--root-articles`, `--articles`, `--hubs`, and full modes. The tracer mode validates one registry slug, the owned root path, the three-target alternate cluster, the root route delegation, the shell/body/schema imports, and absence of any published asset/link item from empty current configuration. Export pure parsing/projection/verification functions for later node:test coverage. Use Node built-ins and concise slug-specific failures.
  </action>
  <acceptance_criteria>
- The default owner can generate `/guide/saas-platform-enterprise-gaps` through a closed root route.
- The page renders one approved localized H1 and the complete `readGuideDocument()` body through `MarkdownContent` inside the existing server shell.
- The visible breadcrumb and JSON-LD use the same owned root canonical; metadata canonical and `openGraph.url` match.
- Alternates contain exactly `en`, `zh-CN`, and `x-default` with the approved cn/io hosts and same slug.
- `--slug saas-platform-enterprise-gaps` proves the route, metadata, schema, body, and empty conditional branches from repository source.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-guide-seo-graph.js --slug saas-platform-enterprise-gaps &amp;&amp; npx --no-install tsc --noEmit</automated>
  </verify>
  <done>One real root Guide article is reachable and coherent from registry source through visible HTML and crawler signals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add validated publication groups, dates, and future asset dimensions</name>
  <reversibility rating="costly">D-03 and the localized timing fields become shared route, hub, schema, sitemap, and verifier contracts across the remaining milestone phases.</reversibility>
  <files>src/content/guides/registry.json, src/content/guides/registry.ts, scripts/verify-guide-content.js, scripts/verify-guide-content.test.js</files>
  <read_first>
- `src/content/guides/registry.json` and `src/content/guides/registry.ts` — retain all approved metadata, hashes, source directives, asset states, and configured-link arrays.
- `scripts/verify-guide-content.js` and `scripts/verify-guide-content.test.js` — preserve the existing GUIDE-03 CLI, isolated mutations, and exact 8×2 success output.
- `.planning/phases/06-guide-hubs-articles-seo-graph/06-CONTEXT.md` D-03, D-05, and D-07 — exact 4/1/3 groups, conditional asset surface, and registry timing authority.
  </read_first>
  <behavior>
- Every one of the sixteen localized snapshots accepts ISO calendar dates and carries `datePublished: 2026-08-11` plus `dateModified: 2026-08-11`.
- A malformed date, impossible calendar date, missing date, or unequal approved launch value fails with the affected slug and locale.
- Every entry accepts exactly one `GuidePublicationGroup`; unknown or missing values fail with the affected slug.
- The approved group assignment is decision for four named slugs, implementation for `support-bot-four-steps`, and industry for the three named industry slugs.
- A `required` asset accepts a contained existing public path, non-empty authored alt, and positive integer width/height; all current asset policies remain byte-equivalent apart from the added publication fields.
- Existing GUIDE-03 mutations and the `8 slugs, 16 documents` success contract remain green.
  </behavior>
  <action>
Extend the shared registry per D-03/D-07. Export `GUIDE_PUBLICATION_GROUPS`, `GuidePublicationGroup`, and a typed ISO-date alias. Add `group` to `GuideEntry`; assign decision to `saas-platform-enterprise-gaps`, `self-build-three-year-tco`, `server-sizing-guide`, and `complex-doc-golden-set`; implementation to `support-bot-four-steps`; and industry to `manufacturing-itops-invoice-audit`, `pharma-compliance-docs`, and `education-retail-support-insight`. Add `datePublished` and `dateModified` to every `GuideSourceSnapshot`, setting both fields to the authorized Week04 delivery date `2026-08-11` for all sixteen snapshots. Validate exact entry keys, allowed groups, strict `YYYY-MM-DD` syntax, real UTC calendar dates, and the approved launch values in both TypeScript and CommonJS boundaries.

Complete the D-05 required-asset type with positive integer `width` and `height`, so `next/image` receives approved responsive dimensions whenever an asset becomes required. Keep every current `assetPolicy`, `sourceImageDirective`, `sourceInternalLinkLabels`, and `configuredInternalLinks` value unchanged. Extend the existing node:test suite first with invalid group/date/dimension mutations, then implement the registry/verifier validation until the full Phase 5 matrix and new publication-field cases pass. Keep the source hashes and Markdown files untouched.
  </action>
  <acceptance_criteria>
- Registry JSON contains eight valid group values and thirty-two ISO timing fields, all with exact value `2026-08-11`.
- TypeScript exposes group and ISO-date types, and both runtime validators identify invalid data with slug/locale context.
- Group counts are decision 4, implementation 1, industry 3.
- Required assets have a typed, verifier-backed path/alt/width/height contract while every approved current asset and link record stays unchanged.
- Existing source fidelity, schema, asset, link, comment, and body mutation tests pass with the extended entry shape.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:guide-content &amp;&amp; npm run verify:guide-content-regression &amp;&amp; npx --no-install tsc --noEmit</automated>
  </verify>
  <done>The registry is the validated source for hub grouping, article timing, and any approved responsive image dimensions.</done>
</task>

<task type="auto">
  <name>Task 3: Expand the root article tracer to all eight dated Guide routes</name>
  <files>src/lib/guideSeo.ts, src/components/guide/GuideArticleRoute.tsx, src/app/guide/[slug]/page.tsx, scripts/verify-guide-seo-graph.js</files>
  <read_first>
- The publication fields and types produced by Task 2.
- `src/app/compare/[slug]/page.tsx` — closed default-locale route delegation and static-parameter pattern.
- `src/components/compare/ComparisonRoute.tsx` — Open Graph article timing and JSON-LD timing consumption.
- `scripts/verify-guide-seo-graph.js` — retain focused tracer behavior while activating root-inventory verification.
  </read_first>
  <action>
Expand the D-01 root static inventory from the tracer to `guideSlugs`, still with `dynamicParams = false` and route-level `notFound()` for any missing registry/source pair. Update `getGuideArticleMetadata()` and the article schema projection to consume each localized snapshot's approved title, description, keywords, `datePublished`, and `dateModified` per D-07. Set `openGraph.type` to article, `openGraph.url` to the self canonical, `publishedTime`/`modifiedTime` to the typed registry values, and include an image only when the current snapshot has `assetPolicy.status === 'required'`.

Update the article JSON-LD to include the same date fields and canonical URL per D-08. Extend the verifier's root-inventory mode to enumerate exactly eight lower-case root paths from registry identity, compare all sixteen metadata snapshots to the owned canonical/alternate projection, and assert one root static parameter per slug. The verifier reads repository source and registry data only; Phase 7 owns dual-export artifact inspection.
  </action>
  <acceptance_criteria>
- Root `generateStaticParams()` returns each registry slug exactly once and `dynamicParams` stays closed.
- All eight default-locale pages use localized registry metadata and the approved dates in Open Graph plus Article JSON-LD.
- Every canonical, alternate, schema, and visible route path derives from `guideSeo.ts` and registry identity.
- Root-inventory verification reports eight Guide article paths with slug-specific failures.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-guide-seo-graph.js --root-articles &amp;&amp; npm run verify:guide-content &amp;&amp; npx --no-install tsc --noEmit</automated>
  </verify>
  <done>All eight root article routes share one dated, registry-derived SEO and schema projection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Registry JSON → typed publication contract | Repository data controls groups, dates, assets, links, and public identity. |
| Route params → source reader | A URL slug selects a contained Markdown source at build time. |
| Authored text → HTML/JSON-LD | Approved text enters visible Markdown and executable script context. |
| Locale → owned domain | Locale projection selects canonical and alternate hosts. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Tampering | Registry group/date/asset fields | medium | mitigate | Runtime group/date/calendar/dimension validation in both TypeScript and the CommonJS verifier, plus mutation tests. |
| T-06-02 | Tampering / Elevation of Privilege | Slug-to-source resolution | high | mitigate | Preserve Phase 5 basename, containment, exact-pair, and hash checks before rendering. |
| T-06-03 | Tampering | JSON-LD serialization | medium | mitigate | Build plain schema objects from validated fields and serialize exclusively through `JsonLdScript` escaping. |
| T-06-04 | Spoofing | Canonical/alternate host projection | medium | mitigate | Generate all absolute URLs through `getOwnedLocaleUrl()` and verify the exact bilingual triad. |
| T-06-SC | Tampering | npm supply chain | high | mitigate | Reuse installed packages and Node built-ins; dependency manifests and lockfile remain byte-stable. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `src/content/guides/registry.json`: approved group and `2026-08-11` publication dates for all eight bilingual entries.
- `src/content/guides/registry.ts`: group/date types and validators plus the dimension-complete required-asset union.
- `src/lib/guideSeo.ts`: central Guide route, locale, metadata, alternate, and schema projection.
- `src/components/guide/GuideArticleRoute.tsx`: server shell and crawler graph for Guide articles.
- `src/components/guide/GuideArticlePage.tsx`: accessible H1/body tracer presentation.
- `src/app/guide/[slug]/page.tsx`: eight closed root article paths.
- `scripts/verify-guide-seo-graph.js`: focused Phase 6 source verifier entry point.
</artifacts_this_phase_produces>

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Phase responsibility | Owner plan(s) | Status | This plan's responsibility |
|---|---|---|---|---|---|
| GOAL | — | Every paired Guide page is discoverable through one owned-domain visitor and crawler graph. | 06-01–04 | COVERED | Establish the source → root article → SEO/schema tracer and shared contracts. |
| REQ | HUB-01 | Exactly eight localized cards in decision, implementation, and industry groups. | 06-01, 06-03, 06-04 | COVERED | Add and validate the exact registry group field; Plan 06-03 renders it. |
| REQ | ARTICLE-01 | All owned articles render approved localized H1 and complete body. | 06-01, 06-02, 06-04 | COVERED | Prove one body path, then expand root params to all eight. |
| REQ | ARTICLE-02 | Breadcrumb, hub return, and configured links reach owned canonicals. | 06-01, 06-02, 06-04 | COVERED | Establish owned path projection and tracer breadcrumb; Plan 06-02 completes conditional links. |
| REQ | ARTICLE-03 | Required approved assets use responsive next/image and authored alt. | 06-01, 06-02, 06-04 | COVERED | Complete the typed required-asset dimensions while preserving current policies. |
| REQ | SEO-04 | Localized metadata, self canonical, and matching Open Graph URL. | 06-01–04 | COVERED | Create the central article/hub metadata projection and consume registry timing. |
| REQ | SEO-05 | Exact reciprocal zh-CN, en, and x-default alternates. | 06-01–04 | COVERED | Create the dedicated three-target Guide helper. |
| REQ | SEO-06 | Required hub/article/breadcrumb and conditional HowTo schemas. | 06-01–04 | COVERED | Prove Article/BreadcrumbList in the tracer and the conditional schema interface. |
| REQ | SEO-07 | Registry identity drives every URL-bearing surface and sitemap. | 06-01–04 | COVERED | Create the shared registry/Guide projection consumed downstream. |
| CONTEXT | D-01 | Owned root topology, locale-filtered adapters, closed registry params. | 06-01–04 | COVERED | Record the resolved one-way checkpoint, root article alias, owned URL helper, and closed params. |
| CONTEXT | D-02 | Existing server shell and fixed server-rendered catalog. | 06-01–03 | COVERED | Reuse dictionary/Navbar/Footer/HomeThemeFix/MarkdownContent in the article tracer. |
| CONTEXT | D-03 | Exact registry-backed 4/1/3 groups. | 06-01, 06-03, 06-04 | COVERED | Add typed group values and both runtime validators. |
| CONTEXT | D-04 | Loader body and authored blocks remain source-faithful. | 06-01, 06-02, 06-04 | COVERED | Route all bodies through readGuideDocument and MarkdownContent. |
| CONTEXT | D-05 | Conditional required-asset rendering. | 06-01, 06-02, 06-04 | COVERED | Preserve launch policies and complete the approved dimension contract. |
| CONTEXT | D-06 | Visible breadcrumb/return and explicit configured links only. | 06-01, 06-02, 06-04 | COVERED | Preserve empty launch mappings and establish owned path projection. |
| CONTEXT | D-07 | Dedicated metadata/canonical/alternate/timing projection. | 06-01–04 | COVERED | Create the helper and set every localized date pair to `2026-08-11`. |
| CONTEXT | D-08 | Registry-derived hub/article schema graph. | 06-01–04 | COVERED | Create escaped article schema projection and downstream interfaces. |
| CONTEXT | D-09 | Current-variant one-hub/eight-article sitemap. | 06-04 | COVERED | Plan 06-04 consumes this plan's helper and registry dates. |
| RESEARCH | — | App Router root aliases plus filtered localized adapters and dynamicParams=false. | 06-01–03 | COVERED | Root detail tracer and reusable locale/static-param helpers. |
| RESEARCH | — | Reuse MarkdownContent, JsonLdScript, siteRouting, article shell, and Node built-ins. | 06-01–04 | COVERED | Named imports/interfaces in tracer and verifier tasks. |
| RESEARCH | — | Approved timing is `2026-08-11`; launch configuredInternalLinks remain empty and labels stay data-only. | 06-01, 06-02, 06-04 | COVERED | Registry values/validators and unchanged mapping arrays. |
| RESEARCH | — | Keep static-export route data build-time and dependency state stable. | 06-01–04 | COVERED | Server-only reader, static params, direct Node verifier, and lockfile gates. |
| CONTEXT | Deferred | Dual-variant export matrix and initial-JavaScript evidence. | 07 | EXCLUDED | Phase 7 owns static artifact and HTML evidence. |
| CONTEXT | Deferred | Immutable artifacts, deployment, cache purge, rollback, and live HTTP evidence. | 08 | EXCLUDED | Phase 8 owns delivery/live verification. |
</source_audit>

<verification>
1. `node scripts/verify-guide-seo-graph.js --root-articles`
2. `npm run verify:guide-content`
3. `npm run verify:guide-content-regression`
4. `npx --no-install tsc --noEmit`
5. `git diff --exit-code -- package-lock.json`
</verification>

<success_criteria>
- One tracer proves the full root publication architecture before expansion.
- Exactly eight root article params consume the complete typed registry.
- All sixteen snapshots carry validated `2026-08-11` published/modified dates.
- Owned canonicals, exact alternates, Open Graph URLs, article timing, and Article/BreadcrumbList schemas agree for every root path.
- Current unapproved asset requests and source-only link labels remain registry data and render no public destination.
</success_criteria>

<output>
Create `.planning/phases/06-guide-hubs-articles-seo-graph/06-01-SUMMARY.md` when done.
</output>
