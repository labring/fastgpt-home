---
phase: 06-guide-hubs-articles-seo-graph
plan: "02"
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/guide/GuideArticleRoute.tsx
  - src/components/guide/GuideArticlePage.tsx
  - src/app/guide/[slug]/page.tsx
  - src/app/[lang]/guide/[slug]/page.tsx
autonomous: true
requirements: [ARTICLE-01, ARTICLE-02, ARTICLE-03, SEO-04, SEO-05, SEO-06, SEO-07]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "ARTICLE-01 / D-04: all eight owned article routes render the exact localized H1 and complete normalized body with authored headings, lists, tables, blockquotes, code, emphasis, and links."
    - "ARTICLE-02 / D-06: every article exposes an accessible Home → Guide → article breadcrumb, a localized hub return link, and a configured-links section only when explicit registry mappings exist."
    - "ARTICLE-03 / D-05: a required registry asset renders through next/image with approved path, alt, width, height, and responsive sizes; every current non-required policy produces no image surface."
    - "SEO-04 / SEO-05 / D-07: root pages are indexable, localized adapters share the root canonical/three-target alternate cluster and are noindex-follow, and Open Graph URL always equals canonical."
    - "SEO-06 / D-08: every article emits Article plus BreadcrumbList, and exactly the three approved HowTo-token slugs add a HowTo node without invented steps."
    - "SEO-07 / D-01: root and adapter static parameters enumerate registry slugs/locales only and remain closed with dynamicParams=false."
  artifacts:
    - path: src/components/guide/GuideArticlePage.tsx
      provides: "Complete accessible article, required-image, hub-return, and configured-link presentation"
    - path: src/components/guide/GuideArticleRoute.tsx
      provides: "Localized shell plus Article/BreadcrumbList/conditional HowTo graph"
    - path: src/app/guide/[slug]/page.tsx
      provides: "Indexable root article aliases for the build default Guide locale"
    - path: src/app/[lang]/guide/[slug]/page.tsx
      provides: "Closed non-indexable localized article adapters"
  key_links:
    - from: src/components/guide/GuideArticlePage.tsx
      to: src/components/tech-center/MarkdownContent.tsx
      via: "complete loader body plus exact localized H1"
      pattern: "MarkdownContent"
    - from: src/components/guide/GuideArticlePage.tsx
      to: src/content/guides/registry.ts
      via: "assetPolicy and configuredInternalLinks gate visible optional sections"
      pattern: "assetPolicy|configuredInternalLinks"
    - from: src/app/[lang]/guide/[slug]/page.tsx
      to: src/lib/guideSeo.ts
      via: "Guide build-locale filter, root canonical, and indexable option"
      pattern: "getGuideBuildLocales|getGuideArticleMetadata"
---

<objective>
Complete the article visitor experience and closed localized adapters across the full eight-pair corpus, implementing D-01, D-02, D-04, D-05, D-06, D-07, and D-08.

Purpose: Turn the proven root tracer into all 16 localized article renderings while keeping public SEO identity at the owned root paths.
Output: Complete responsive article presentation, conditional approved assets/links, conditional HowTo schema, and localized App Router adapters.

Decision trace: D-01: closed root/localized article inventory; D-02: existing server shell; D-04: complete authored body; D-05: responsive required-image branch; D-06: breadcrumb, return, and explicit links; D-07: root SEO identity; D-08: article schema graph.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-guide-hubs-articles-seo-graph/06-CONTEXT.md
@.planning/phases/06-guide-hubs-articles-seo-graph/06-RESEARCH.md
@.planning/phases/06-guide-hubs-articles-seo-graph/06-01-SUMMARY.md
@src/content/guides/registry.ts
@src/lib/guideContent.ts
@src/lib/guideSeo.ts
@src/app/compare/[slug]/page.tsx
@src/app/[lang]/compare/[slug]/page.tsx
@src/components/compare/ComparisonRoute.tsx
@src/components/tech-center/MarkdownContent.tsx
@src/components/tech-center/TechArticlePage.tsx
@src/components/tech-center/TechArticlePage.module.css
@src/components/JsonLd.tsx
@scripts/verify-guide-seo-graph.js

<interfaces>
- Plan 06-01 provides `GuideSourceSnapshot.datePublished/dateModified`, dimension-complete `GuideAssetPolicy`, `getGuideBuildLocales()`, `getGuideArticleMetadata()`, and all owned Guide URL helpers.
- `GuideArticleRoute` receives `{ locale: GuideLocale; slug: string }`; `GuideArticlePage` receives the validated `GuideDocument`, locale-aware paths/copy, and optional projected assets/links.
- `src/app/[lang]/compare/[slug]/page.tsx` establishes localized adapter `notFound()`, closed params, and `{ indexable: false }` metadata behavior.
- `MarkdownContent` owns authored block rendering; `TechArticlePage.module.css` owns the responsive light-theme article rules reused by the Guide surface.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the article route to every authored block and approved optional surface</name>
  <files>src/components/guide/GuideArticleRoute.tsx, src/components/guide/GuideArticlePage.tsx</files>
  <read_first>
- `.planning/phases/06-guide-hubs-articles-seo-graph/06-CONTEXT.md` D-02 and D-04 through D-08 — shell, body, asset, link, metadata, and schema behavior.
- `src/lib/guideContent.ts` and `src/content/guides/registry.ts` — validated body/source, required-asset union, schema tokens, and configured-link mappings.
- `src/components/tech-center/MarkdownContent.tsx`, `TechArticlePage.tsx`, and `TechArticlePage.module.css` — exact renderer, responsive image markup, table overflow, focus, and mobile layout.
- `src/components/JsonLd.tsx` — escaped JSON-LD primitive.
  </read_first>
  <action>
Complete the server-rendered article surface per D-02/D-04. Pass the full `readGuideDocument().body` and exact snapshot H1 to `MarkdownContent`; retain the route-owned H1 so the parser removes only its matching source duplicate. Keep `Navbar`, `Footer`, `HomeThemeFix`, and dictionary loading in the server route. Localize Home, Guide, return-link, and configured-link section labels through one zh/en copy record while preserving every authored article string unchanged.

Implement the D-05 asset branch directly from validated registry policy. For `status: required`, render `next/image` with the registry path, authored alt, approved width/height, a responsive `sizes` value, intrinsic aspect ratio, and visible figure semantics consistent with `TechArticlePage`. The current `none`, `requested-unapproved`, and `source-exception` snapshots produce no image node. Implement D-06 as an explicit `configuredInternalLinks.length > 0` section: preserve each source label, use only its validated owned canonical target, and expose descriptive link text/focus behavior. Leave every current configured-link array empty.

Build one Article and one BreadcrumbList node for every page from the same visible names/URLs. When `schemaTokens` contains `HowTo`, append a HowTo node containing approved name, description, URL, locale, and dates; keep step data absent because the registry supplies no approved structured steps. Serialize every node through `JsonLdScript` per D-08. Ensure the root article projection mode verifies all eight bodies, schema token branches, three approved HowTo slugs, zero current image nodes, and zero current configured-link nodes.
  </action>
  <acceptance_criteria>
- Every Guide page has exactly one visible H1 and retains the complete authored body after the Phase 5 boundary.
- All authored block types render through the existing MarkdownContent implementation with responsive tables and code blocks.
- Required image and configured-link branches read validated registry records; current source-only directives remain visually absent.
- Visible and JSON-LD breadcrumbs contain Home, Guide, and article at identical owned URLs.
- HowTo appears for `server-sizing-guide`, `complex-doc-golden-set`, and `support-bot-four-steps`, with Article and BreadcrumbList on all eight.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-guide-seo-graph.js --root-articles &amp;&amp; npx --no-install tsc --noEmit</automated>
  </verify>
  <done>Every root Guide article renders all approved content and every currently applicable visitor/schema surface.</done>
</task>

<task type="auto">
  <name>Task 2: Close the root and localized article inventories around owned canonicals</name>
  <files>src/app/guide/[slug]/page.tsx, src/app/[lang]/guide/[slug]/page.tsx</files>
  <read_first>
- `src/app/guide/[slug]/page.tsx` from Plan 06-01 — retain default-locale delegation and exact eight-slug inventory.
- `src/app/[lang]/compare/[slug]/page.tsx` — established localized route guard and noindex metadata pattern.
- `src/lib/guideSeo.ts` — `resolveGuideLocale`, `getGuideBuildLocales`, and delegated metadata contract.
- `src/lib/siteRouting.ts` — build locales can include international locales outside the two Guide source locales.
  </read_first>
  <action>
Keep the D-01 root alias indexable and derived from `defaultLocale`, `guideSlugs`, and `GuideArticleRoute`. Add `src/app/[lang]/guide/[slug]/page.tsx` as the thin localized adapter. Generate only the locale values returned by `getGuideBuildLocales()` crossed with all eight registry slugs; set `dynamicParams = false`; resolve only `zh`/`en`; and call `notFound()` for unsupported locale or slug identity. The io fallback emits the `en` adapter and the cn fallback emits `zh`, while every public canonical remains `/guide/<slug>`.

Delegate localized metadata to `getGuideArticleMetadata(locale, slug, { indexable: false })` per D-07. Preserve the same root self canonical, exact `en`/`zh-CN`/`x-default` cluster, matching Open Graph URL, localized registry title/description/keywords/timing, and use `robots: { index: false, follow: true }` on the adapter. Root aliases remain indexable. Extend article-mode verification through the already-created verifier interface so it requires both route files, exact static-param projection, closed dynamic params, root canonical identity, and adapter noindex-follow behavior without inspecting exported HTML.
  </action>
  <acceptance_criteria>
- Root and localized detail route files both delegate rendering and metadata to the same Guide implementation.
- Each build generates eight root article paths plus exactly one adapter locale for its owned Guide source locale.
- Unsupported international locales and unknown slugs resolve through `notFound()` under closed dynamic params.
- Adapter metadata points at the identical root canonical/alternate graph and carries noindex-follow; root metadata remains indexable.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-guide-seo-graph.js --articles &amp;&amp; npm run verify:guide-content &amp;&amp; npx --no-install tsc --noEmit</automated>
  </verify>
  <done>All 16 localized article renderings are build-time reachable while search identity stays on the two owned root URL sets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Route locale/slug → registry | Dynamic segments select a fixed build-time locale pair and article identity. |
| Asset/link configuration → visitor DOM | Validated optional records become navigable or visual content. |
| Authored Markdown → rendered links | Source body may contain inline URLs handled by the existing Markdown renderer. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-05 | Tampering | Localized route params | medium | mitigate | Intersect build locales with `GUIDE_LOCALES`, reject unsupported values, and close dynamic params. |
| T-06-06 | Tampering / Spoofing | Configured internal links | medium | mitigate | Render only Phase 5 validated owned canonical targets and retain source labels as visible text. |
| T-06-07 | Tampering / Information Disclosure | Required asset records | medium | mitigate | Consume contained public path, authored alt, and positive dimensions from the validated union. |
| T-06-08 | Tampering | Authored text and JSON-LD | medium | mitigate | Keep MarkdownContent protocol filtering and JsonLdScript escaping at the render boundaries. |
| T-06-SC | Tampering | npm supply chain | high | mitigate | Use the installed Next/React stack and existing components; dependency state stays unchanged. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `GuideArticlePage.tsx`: complete H1/body, breadcrumb, return, conditional image, and conditional configured-link UI.
- `GuideArticleRoute.tsx`: complete localized shell and Article/BreadcrumbList/conditional HowTo graph.
- `src/app/[lang]/guide/[slug]/page.tsx`: closed locale-filtered adapter inventory with root canonical metadata.
- `src/app/guide/[slug]/page.tsx`: final indexable eight-slug root inventory.
</artifacts_this_phase_produces>

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Phase responsibility | Owner plan(s) | Status | This plan's responsibility |
|---|---|---|---|---|---|
| GOAL | — | Every paired Guide page is discoverable through one owned-domain visitor and crawler graph. | 06-01–04 | COVERED | Complete all article visitor surfaces and localized adapters from Plan 06-01 contracts. |
| REQ | HUB-01 | Exactly eight localized cards in decision, implementation, and industry groups. | 06-01, 06-03, 06-04 | COVERED | Consume hub paths for breadcrumbs/return links; Plan 06-03 owns cards. |
| REQ | ARTICLE-01 | All owned articles render approved localized H1 and complete body. | 06-01, 06-02, 06-04 | COVERED | Render all sixteen locale snapshots through the loader and MarkdownContent. |
| REQ | ARTICLE-02 | Breadcrumb, hub return, and configured links reach owned canonicals. | 06-01, 06-02, 06-04 | COVERED | Implement complete visible navigation and the explicit-mapping-only section. |
| REQ | ARTICLE-03 | Required approved assets use responsive next/image and authored alt. | 06-01, 06-02, 06-04 | COVERED | Implement the conditional required-image surface with approved dimensions. |
| REQ | SEO-04 | Localized metadata, self canonical, and matching Open Graph URL. | 06-01–04 | COVERED | Apply the shared helper to root and localized article adapters. |
| REQ | SEO-05 | Exact reciprocal zh-CN, en, and x-default alternates. | 06-01–04 | COVERED | Preserve the exact shared triad on both route forms. |
| REQ | SEO-06 | Required hub/article/breadcrumb and conditional HowTo schemas. | 06-01–04 | COVERED | Complete Article/BreadcrumbList and token-gated HowTo nodes. |
| REQ | SEO-07 | Registry identity drives every URL-bearing surface and sitemap. | 06-01–04 | COVERED | Use registry slugs/snapshots for params, links, metadata, and schema. |
| CONTEXT | D-01 | Owned root topology, locale-filtered adapters, closed registry params. | 06-01–04 | COVERED | Finish root plus localized detail route inventory and noindex adapter behavior. |
| CONTEXT | D-02 | Existing server shell and fixed server-rendered catalog. | 06-01–03 | COVERED | Retain the Plan 06-01 server shell for every article. |
| CONTEXT | D-03 | Exact registry-backed 4/1/3 groups. | 06-01, 06-03, 06-04 | COVERED | Consume the shared Guide path; Plan 06-03 owns visible grouping. |
| CONTEXT | D-04 | Loader body and authored blocks remain source-faithful. | 06-01, 06-02, 06-04 | COVERED | Preserve all authored blocks and one visible H1 across every page. |
| CONTEXT | D-05 | Conditional required-asset rendering. | 06-01, 06-02, 06-04 | COVERED | Render next/image only from a validated required policy. |
| CONTEXT | D-06 | Visible breadcrumb/return and explicit configured links only. | 06-01, 06-02, 06-04 | COVERED | Implement all visible link semantics while launch arrays stay empty. |
| CONTEXT | D-07 | Dedicated metadata/canonical/alternate/timing projection. | 06-01–04 | COVERED | Consume exact registry titles/descriptions/keywords/dates and root identity. |
| CONTEXT | D-08 | Registry-derived hub/article schema graph. | 06-01–04 | COVERED | Render complete article and conditional HowTo graph through JsonLdScript. |
| CONTEXT | D-09 | Current-variant one-hub/eight-article sitemap. | 06-04 | COVERED | Plan 06-04 consumes final article registry paths and dates. |
| RESEARCH | — | App Router root aliases plus filtered localized adapters and dynamicParams=false. | 06-01–03 | COVERED | Implement exact article adapters and locale fallback policy. |
| RESEARCH | — | Reuse MarkdownContent, JsonLdScript, siteRouting, article shell, and Node built-ins. | 06-01–04 | COVERED | Reuse all named render/SEO primitives directly. |
| RESEARCH | — | Approved timing is `2026-08-11`; launch configuredInternalLinks remain empty and labels stay data-only. | 06-01, 06-02, 06-04 | COVERED | Consume approved dates and keep optional UI conditional on explicit mappings. |
| RESEARCH | — | Current non-required asset policies produce a text-first launch while the required branch remains real. | 06-01, 06-02, 06-04 | COVERED | Implement and verify the responsive branch without changing current records. |
| CONTEXT | Deferred | Dual-variant export matrix and initial-JavaScript evidence. | 07 | EXCLUDED | Phase 7 owns static artifact and HTML evidence. |
| CONTEXT | Deferred | Immutable artifacts, deployment, cache purge, rollback, and live HTTP evidence. | 08 | EXCLUDED | Phase 8 owns delivery/live verification. |
</source_audit>

<verification>
1. `node scripts/verify-guide-seo-graph.js --articles`
2. `npm run verify:guide-content`
3. `npx --no-install tsc --noEmit`
4. `git diff --exit-code -- package-lock.json`
</verification>

<success_criteria>
- ARTICLE-01: all sixteen localized article renders preserve approved H1/body content.
- ARTICLE-02: breadcrumbs, hub return, and configured links resolve through the owned canonical policy.
- ARTICLE-03: the registry-required image branch uses existing next/image behavior and approved accessibility data.
- SEO-04/05/06: metadata, alternates, article timing, Article/BreadcrumbList, and conditional HowTo agree on every route.
- SEO-07: article params, links, and schema URLs derive from the shared Guide registry/projection.
</success_criteria>

<output>
Create `.planning/phases/06-guide-hubs-articles-seo-graph/06-02-SUMMARY.md` when done.
</output>
