---
phase: 06-guide-hubs-articles-seo-graph
plan: "03"
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/guide/GuideHubRoute.tsx
  - src/components/guide/GuideHubPage.tsx
  - src/components/guide/GuideHubPage.module.css
  - src/app/guide/page.tsx
  - src/app/[lang]/guide/page.tsx
autonomous: true
requirements: [HUB-01, SEO-04, SEO-05, SEO-06, SEO-07]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "HUB-01 / D-03: each owned `/guide` hub renders exactly eight localized cards in exactly three visible groups, with the locked decision 4, implementation 1, and industry 3 assignment."
    - "D-02: the fixed hub stays a server component inside the existing localized dictionary, Navbar, Footer, and HomeThemeFix shell, with zero catalog client state."
    - "SEO-04 / SEO-05 / D-07: hub title/description, self canonical, matching Open Graph URL, and exact en/zh-CN/x-default alternates come from the central Guide projection."
    - "SEO-06 / D-08: each hub emits CollectionPage, ItemList, and BreadcrumbList whose names/order/URLs match the visible groups and cards."
    - "SEO-07 / D-01: every hub card and breadcrumb target uses the same registry slug and owned root path policy as article routes."
    - "The hub is keyboard navigable, exposes semantic section headings/list links, and collapses responsively without adding a client bundle."
  artifacts:
    - path: src/components/guide/GuideHubRoute.tsx
      provides: "Localized server shell, hub metadata delegation, and CollectionPage/ItemList/BreadcrumbList graph"
    - path: src/components/guide/GuideHubPage.tsx
      provides: "Exactly three registry-backed localized card groups"
    - path: src/components/guide/GuideHubPage.module.css
      provides: "Responsive light-theme hub layout and focus-visible card behavior"
    - path: src/app/guide/page.tsx
      provides: "Indexable default-locale root Guide hub"
    - path: src/app/[lang]/guide/page.tsx
      provides: "Closed non-indexable localized Guide hub adapter"
  key_links:
    - from: src/components/guide/GuideHubPage.tsx
      to: src/content/guides/registry.ts
      via: "group-filtered guideEntries and localized source snapshots drive all cards"
      pattern: "guideEntries|entry\[locale\]|group"
    - from: src/components/guide/GuideHubPage.tsx
      to: src/lib/guideSeo.ts
      via: "card hrefs use the owned Guide path projection"
      pattern: "getGuidePath|getOwnedLocalePath"
    - from: src/components/guide/GuideHubRoute.tsx
      to: src/components/JsonLd.tsx
      via: "visible cards and breadcrumb become escaped schema graph nodes"
      pattern: "JsonLdScript"
---

<objective>
Publish the two owned-domain Guide hubs as a fixed, accessible, registry-backed three-group catalog with a matching crawler graph, implementing D-01, D-02, D-03, D-07, and D-08.

Purpose: Make all eight paired articles discoverable from one server-rendered owned root surface per locale.
Output: Root and localized hub adapters, three-group card presentation, responsive CSS, localized metadata, and hub JSON-LD.

Decision trace: D-01: owned root hub topology; D-02: server-rendered reusable shell; D-03: exact group taxonomy; D-07: hub SEO identity; D-08: CollectionPage, ItemList, and BreadcrumbList graph.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-guide-hubs-articles-seo-graph/06-CONTEXT.md
@.planning/phases/06-guide-hubs-articles-seo-graph/06-RESEARCH.md
@.planning/phases/06-guide-hubs-articles-seo-graph/06-01-SUMMARY.md
@src/content/guides/registry.ts
@src/lib/guideSeo.ts
@src/lib/siteRouting.ts
@src/app/compare/page.tsx
@src/app/[lang]/compare/page.tsx
@src/components/compare/ComparisonHubRoute.tsx
@src/components/JsonLd.tsx
@scripts/verify-guide-seo-graph.js

<interfaces>
- Plan 06-01 provides `GuideEntry.group`, `guideEntries`, `GuideLocale`, `getGuidePath()`, `getGuideCanonicalUrl()`, `getGuideAlternates()`, `getGuideBuildLocales()`, and `getGuideHubMetadata()`.
- `src/components/compare/ComparisonHubRoute.tsx` demonstrates server shell composition and one graph containing ItemList plus CollectionPage alongside BreadcrumbJsonLd.
- `src/app/compare/page.tsx` and `src/app/[lang]/compare/page.tsx` demonstrate indexable root alias plus closed noindex localized adapter behavior.
- `JsonLdScript({ data })` is the only JSON-LD serialization primitive used by this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the indexable root hub as three exact registry groups</name>
  <files>src/components/guide/GuideHubRoute.tsx, src/components/guide/GuideHubPage.tsx, src/components/guide/GuideHubPage.module.css, src/app/guide/page.tsx</files>
  <read_first>
- `.planning/phases/06-guide-hubs-articles-seo-graph/06-CONTEXT.md` D-01 through D-03 and D-07/D-08 — topology, shell, taxonomy, metadata, and schema graph.
- `src/content/guides/registry.ts` — exact group identity and localized snapshot fields.
- `src/lib/guideSeo.ts` — central hub copy, canonical, alternate, metadata, and path projections.
- `src/components/compare/ComparisonHubRoute.tsx` — shell and CollectionPage/ItemList construction.
- `src/components/JsonLd.tsx` — escaped schema output.
  </read_first>
  <action>
Create `GuideHubPage.tsx` as a server component with one typed zh/en label record for the decision, implementation, and industry headings plus short group descriptions. Iterate the fixed group order and filter `guideEntries` by its validated `group` field per D-03. Render semantic labelled sections and link lists totaling exactly eight cards in 4/1/3 distribution. Each card uses the localized snapshot H1 and meta description, preserves registry order within its group, and links through `getOwnedLocalePath(locale, getGuidePath(slug))` so public navigation remains `/guide/<slug>` per D-01/SEO-07.

Create `GuideHubPage.module.css` for the existing light-theme visual language: constrained readable width, responsive card grid, group spacing, balanced typography, touch-sized full-card links, visible keyboard focus, sufficient contrast, and a single-column mobile layout. Keep the fixed launch catalog entirely server-rendered per D-02.

Create `GuideHubRoute.tsx` with localized dictionary, `HomeThemeFix`, `Navbar`, `Footer`, and the Guide hub page. Build one visible Home → Guide breadcrumb and emit CollectionPage, ItemList, and BreadcrumbList per D-08. The ItemList order matches the eight visible registry cards, and every schema URL comes from the same owned projection. Add thin `src/app/guide/page.tsx` default-locale rendering and delegated `getGuideHubMetadata()`; this root hub is indexable with self canonical, matching Open Graph URL, and the exact alternate triad per D-07.
  </action>
  <acceptance_criteria>
- Root `/guide` renders exactly three labelled groups and exactly eight localized cards in approved 4/1/3 membership.
- Every card displays registry-authored localized text and reaches its root owned article path.
- Hub HTML uses semantic headings/lists/links, visible focus, touch-sized targets, and responsive one-column behavior.
- CollectionPage, ItemList, and BreadcrumbList names/order/URLs match the visible hub.
- Root metadata canonical and Open Graph URL are identical and alternates contain exactly the approved three keys.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-guide-seo-graph.js --hubs &amp;&amp; npx --no-install tsc --noEmit</automated>
  </verify>
  <done>The default-locale owned root hub exposes all eight articles through one accessible visible and structured graph.</done>
</task>

<task type="auto">
  <name>Task 2: Add the closed localized hub adapter and responsive accessibility gate</name>
  <files>src/app/[lang]/guide/page.tsx, src/components/guide/GuideHubPage.tsx, src/components/guide/GuideHubPage.module.css</files>
  <read_first>
- `src/app/[lang]/compare/page.tsx` — localized hub guard, static params, dynamicParams, and noindex metadata pattern.
- `src/lib/guideSeo.ts` — exact Guide build-locale set and hub metadata helper.
- Root hub files produced by Task 1 — retain group/card/schema order and identifiers.
- `scripts/verify-guide-seo-graph.js` hub mode — fulfill its locale, count, and source-wiring contracts.
  </read_first>
  <action>
Add `src/app/[lang]/guide/page.tsx` as the D-01 localized adapter. Generate only `getGuideBuildLocales()` values, set `dynamicParams = false`, accept only `GuideLocale`, route unsupported values to `notFound()`, and delegate rendering to `GuideHubRoute`. Delegate metadata through `getGuideHubMetadata(locale, { indexable: false })`, retaining the owned root `/guide` canonical, identical Open Graph URL, exact bilingual alternate cluster, and `robots: { index: false, follow: true }`.

Exercise the final hub through both zh and en projection modes. Refine the server markup/CSS where the checks reveal inaccessible heading hierarchy, clipped long English titles, narrow-screen overflow, missing `:focus-visible`, or card links smaller than the established interaction target. Preserve the exact eight-card registry mapping and zero client directives. Run hub verification after the localized adapter exists so it proves root delegation, locale filtering/fallback, adapter noindex, three groups, card totals, schema types, and shared URL helpers.
  </action>
  <acceptance_criteria>
- Each site build has one root Guide hub plus exactly one default-source localized adapter.
- Unsupported non-Guide locale params resolve through `notFound()` and cannot expand the static route set.
- Adapter metadata shares the root canonical/alternate/Open Graph identity and is noindex-follow.
- Both locale card sets remain readable and keyboard navigable at desktop and narrow widths with no client component.
- Hub verifier reports three groups and eight cards for both `zh` and `en` projections.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-guide-seo-graph.js --hubs &amp;&amp; npx --no-install tsc --noEmit</automated>
  </verify>
  <done>Both owned hubs and their build adapters expose the same accessible eight-card registry graph.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Registry groups → public catalog | Group/order identity determines discovery and ItemList position. |
| Localized adapter params → hub renderer | Route locale selects the localized card snapshot and canonical owner. |
| Authored metadata → JSON-LD | Titles/descriptions enter a script context through schema serialization. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-09 | Tampering | Hub group/card projection | medium | mitigate | Validated enum plus exact 4/1/3 assignment and verifier count/order assertions. |
| T-06-10 | Spoofing | Card, breadcrumb, and schema URLs | medium | mitigate | Build all paths/URLs through central owned Guide helpers and compare visible/schema projections. |
| T-06-11 | Tampering | Localized hub params | medium | mitigate | Guide-locale narrowing, build-locale filtering, `notFound()`, and closed dynamic params. |
| T-06-12 | Tampering | Hub JSON-LD | medium | mitigate | Serialize validated registry/copy data only through `JsonLdScript`. |
| T-06-SC | Tampering | npm supply chain | high | mitigate | CSS and installed framework primitives only; dependency state stays unchanged. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `GuideHubRoute.tsx`: localized hub shell and CollectionPage/ItemList/BreadcrumbList graph.
- `GuideHubPage.tsx`: exact three-group, eight-card server catalog.
- `GuideHubPage.module.css`: responsive and keyboard-visible hub styling.
- `src/app/guide/page.tsx`: indexable default-locale root hub.
- `src/app/[lang]/guide/page.tsx`: closed non-indexable localized hub adapter.
</artifacts_this_phase_produces>

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Phase responsibility | Owner plan(s) | Status | This plan's responsibility |
|---|---|---|---|---|---|
| GOAL | — | Every paired Guide page is discoverable through one owned-domain visitor and crawler graph. | 06-01–04 | COVERED | Publish the owned discovery hubs that expose all eight article pairs. |
| REQ | HUB-01 | Exactly eight localized cards in decision, implementation, and industry groups. | 06-01, 06-03, 06-04 | COVERED | Render exact 4/1/3 grouped cards in both locales. |
| REQ | ARTICLE-01 | All owned articles render approved localized H1 and complete body. | 06-01, 06-02, 06-04 | COVERED | Link every localized card to its article; Plans 06-01/02 own bodies. |
| REQ | ARTICLE-02 | Breadcrumb, hub return, and configured links reach owned canonicals. | 06-01, 06-02, 06-04 | COVERED | Supply the canonical hub target used by article breadcrumbs and return links. |
| REQ | ARTICLE-03 | Required approved assets use responsive next/image and authored alt. | 06-01, 06-02, 06-04 | COVERED | Plan 06-02 owns article images; hub cards use text snapshots only. |
| REQ | SEO-04 | Localized metadata, self canonical, and matching Open Graph URL. | 06-01–04 | COVERED | Apply the shared metadata helper to both hub route forms. |
| REQ | SEO-05 | Exact reciprocal zh-CN, en, and x-default alternates. | 06-01–04 | COVERED | Preserve the exact shared triad on hubs. |
| REQ | SEO-06 | Required hub/article/breadcrumb and conditional HowTo schemas. | 06-01–04 | COVERED | Emit CollectionPage, ItemList, and BreadcrumbList from visible cards. |
| REQ | SEO-07 | Registry identity drives every URL-bearing surface and sitemap. | 06-01–04 | COVERED | Drive group order, card paths, breadcrumb, params, and schemas from registry/helper. |
| CONTEXT | D-01 | Owned root topology, locale-filtered adapters, closed registry params. | 06-01–04 | COVERED | Implement root and localized hub adapters around the owned `/guide` canonical. |
| CONTEXT | D-02 | Existing server shell and fixed server-rendered catalog. | 06-01–03 | COVERED | Reuse dictionary/Navbar/Footer/HomeThemeFix with zero hub client state. |
| CONTEXT | D-03 | Exact registry-backed 4/1/3 groups. | 06-01, 06-03, 06-04 | COVERED | Render the validated group field and preserve registry order. |
| CONTEXT | D-04 | Loader body and authored blocks remain source-faithful. | 06-01, 06-02, 06-04 | COVERED | Plans 06-01/02 own body rendering; cards use approved snapshot metadata. |
| CONTEXT | D-05 | Conditional required-asset rendering. | 06-01, 06-02, 06-04 | COVERED | Plan 06-02 owns required article assets; hub adds no inferred image. |
| CONTEXT | D-06 | Visible breadcrumb/return and explicit configured links only. | 06-01, 06-02, 06-04 | COVERED | Publish the canonical hub target and preserve data-only source labels. |
| CONTEXT | D-07 | Dedicated metadata/canonical/alternate/timing projection. | 06-01–04 | COVERED | Consume central hub copy/canonical/alternate/Open Graph projection. |
| CONTEXT | D-08 | Registry-derived hub/article schema graph. | 06-01–04 | COVERED | Make visible card order identical to ItemList and breadcrumb URLs. |
| CONTEXT | D-09 | Current-variant one-hub/eight-article sitemap. | 06-04 | COVERED | Plan 06-04 consumes this plan's owned hub URL and card identity. |
| RESEARCH | — | App Router root aliases plus filtered localized adapters and dynamicParams=false. | 06-01–03 | COVERED | Implement exact hub route pair and locale fallback policy. |
| RESEARCH | — | Reuse comparison/technical shell, JsonLdScript, siteRouting, and server components. | 06-01–04 | COVERED | Follow named hub/schema patterns with one focused CSS module. |
| RESEARCH | — | Exact three groups and eight-card fixed launch need no client catalog state. | 06-01, 06-03, 06-04 | COVERED | Server-render semantic sections and responsive links only. |
| RESEARCH | — | Approved timing is `2026-08-11`; launch configuredInternalLinks remain empty and labels stay data-only. | 06-01, 06-02, 06-04 | COVERED | Consume centralized hub SEO and publish only registry card destinations. |
| CONTEXT | Deferred | Dual-variant export matrix and initial-JavaScript evidence. | 07 | EXCLUDED | Phase 7 owns static artifact and HTML evidence. |
| CONTEXT | Deferred | Immutable artifacts, deployment, cache purge, rollback, and live HTTP evidence. | 08 | EXCLUDED | Phase 8 owns delivery/live verification. |
</source_audit>

<verification>
1. `node scripts/verify-guide-seo-graph.js --hubs`
2. `npx --no-install tsc --noEmit`
3. `git diff --exit-code -- package-lock.json`
</verification>

<success_criteria>
- HUB-01: both owned root hubs contain exactly eight localized cards in the approved three groups.
- SEO-04/05: hub metadata has localized copy, self canonical, matching Open Graph URL, and exact reciprocal alternates.
- SEO-06: CollectionPage, ItemList, and BreadcrumbList match visible hub identity and order.
- SEO-07: all card, breadcrumb, static-param, and schema paths come from the shared Guide registry/projection.
- Server-rendered semantics, keyboard focus, and responsive layout meet the Phase 6 accessibility boundary.
</success_criteria>

<output>
Create `.planning/phases/06-guide-hubs-articles-seo-graph/06-03-SUMMARY.md` when done.
</output>
