---
phase: 06-guide-hubs-articles-seo-graph
plan: "04"
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - src/app/sitemap.ts
  - scripts/verify-guide-seo-graph.js
  - scripts/verify-guide-seo-graph.test.js
  - package.json
autonomous: true
requirements: [HUB-01, ARTICLE-01, ARTICLE-02, ARTICLE-03, SEO-04, SEO-05, SEO-06, SEO-07]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "SEO-07 / D-09: each current site variant sitemap contains exactly one owned Guide hub and eight owned Guide article canonicals, each once through the existing seenUrls guard."
    - "SEO-04 / SEO-05 / D-07: one dependency-free source verifier proves localized metadata, self canonical, matching Open Graph URL, and exact reciprocal en/zh-CN/x-default clusters for hubs plus all sixteen snapshots."
    - "SEO-06 / D-08: the verifier proves CollectionPage/ItemList/BreadcrumbList hubs and Article/BreadcrumbList/conditional HowTo articles use the same registry-derived URL projection as visible navigation."
    - "HUB-01 / ARTICLE-01/02/03: the runnable gate checks group/card counts, closed routes, authored H1/body wiring, breadcrumb/return/link wiring, and conditional required-image behavior with slug-specific failures."
    - "The command remains source/build-time verification; dual-variant exported HTML evidence belongs to Phase 7 and deployment/live evidence belongs to Phase 8."
  artifacts:
    - path: src/app/sitemap.ts
      provides: "Current-variant one-hub/eight-article Guide sitemap projection"
    - path: scripts/verify-guide-seo-graph.js
      provides: "Complete dependency-free Phase 6 source graph verifier"
    - path: scripts/verify-guide-seo-graph.test.js
      provides: "Isolated mutation coverage for groups, dates, routes, SEO, schema, assets, links, and sitemap"
    - path: package.json
      provides: "One stable verify:guide-seo-graph regression command"
  key_links:
    - from: src/app/sitemap.ts
      to: src/content/guides/registry.ts
      via: "guideEntries supplies exact current-variant article identity and dates"
      pattern: "guideEntries"
    - from: src/app/sitemap.ts
      to: src/lib/guideSeo.ts
      via: "hub/article canonical helper supplies every Guide sitemap URL"
      pattern: "getGuideCanonicalUrl"
    - from: scripts/verify-guide-seo-graph.test.js
      to: scripts/verify-guide-seo-graph.js
      via: "deep-cloned registry mutations exercise pure graph validators"
      pattern: "require\('./verify-guide-seo-graph'\)"
    - from: package.json
      to: scripts/verify-guide-seo-graph.js
      via: "one npm command runs source verification and node:test regression"
      pattern: "verify:guide-seo-graph"
---

<objective>
Close the Phase 6 discovery graph with registry-derived sitemap entries and one regression command covering every in-scope visitor and crawler invariant, implementing D-07, D-08, and D-09 across HUB-01, ARTICLE-01/02/03, and SEO-04/05/06/07.

Purpose: Detect source-level drift before Phase 7 performs the case-sensitive dual-export evidence matrix.
Output: Guide sitemap integration, complete source verifier, isolated mutation tests, and one stable npm command.

Decision trace: D-07: metadata/canonical/alternate regression; D-08: schema and visible-navigation graph regression; D-09: current-variant one-hub/eight-article sitemap projection.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-guide-hubs-articles-seo-graph/06-CONTEXT.md
@.planning/phases/06-guide-hubs-articles-seo-graph/06-RESEARCH.md
@.planning/phases/06-guide-hubs-articles-seo-graph/06-01-SUMMARY.md
@.planning/phases/06-guide-hubs-articles-seo-graph/06-02-SUMMARY.md
@.planning/phases/06-guide-hubs-articles-seo-graph/06-03-SUMMARY.md
@src/content/guides/registry.json
@src/content/guides/registry.ts
@src/lib/guideSeo.ts
@src/app/sitemap.ts
@scripts/verify-guide-content.js
@scripts/verify-guide-content.test.js
@scripts/verify-guide-seo-graph.js
@package.json

<interfaces>
- `src/app/sitemap.ts` owns `entries`, `seenUrls`, and `addEntry(url, lastModified)`; retain this deduplicated current-variant assembly.
- Plan 06-01 provides `guideEntries`, localized snapshot dates, and `getGuideCanonicalUrl(locale, slug?)`.
- Plans 06-02/03 provide four root/localized route files plus Guide article/hub route and presentation modules consumed by source verification.
- `scripts/verify-guide-content.test.js` establishes `node:test`, `assert/strict`, `structuredClone`, temporary-root cleanup, silent imports, and slug-specific failure conventions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the complete registry-to-sitemap Guide graph</name>
  <files>src/app/sitemap.ts, scripts/verify-guide-seo-graph.js, scripts/verify-guide-seo-graph.test.js</files>
  <read_first>
- `.planning/phases/06-guide-hubs-articles-seo-graph/06-CONTEXT.md` D-01 through D-09 — complete route, rendering, SEO, schema, and sitemap graph.
- `src/app/sitemap.ts` — retain `seenUrls`, current variant selection, and existing FAQ/tech/comparison entries.
- `src/content/guides/registry.json`, `registry.ts`, and `src/lib/guideSeo.ts` — exact identity, groups, dates, owned URL, and alternate projection.
- `scripts/verify-guide-content.js` and `.test.js` — pure CommonJS seam, mutation isolation, and actionable diagnostics.
- The route/component files delivered by Plans 06-02 and 06-03 — verify their shared helper wiring and closed route semantics.
  </read_first>
  <behavior>
- A cn projection contains `/guide` plus eight `https://fastgpt.cn/guide/<slug>` rows exactly once; an io projection contains the matching nine `fastgpt.io` rows exactly once.
- Every article sitemap row uses its localized `dateModified`; hub timing follows the existing build-time `now` policy.
- Duplicate or missing slug, wrong 4/1/3 group, invalid/missing date, canonical host/path drift, alternate-key drift, route-param drift, schema-token drift, or sitemap count drift fails with the affected slug/surface.
- All eight article projections have Article and BreadcrumbList; only the three approved token sets add HowTo.
- Both hub projections have CollectionPage, ItemList, and BreadcrumbList with eight item URLs matching visible cards.
- Empty current configured-link arrays and non-required asset policies publish zero optional nodes; a valid isolated required-asset or configured-link fixture activates exactly one projected node.
- Requiring the verifier module is silent and side-effect free.
  </behavior>
  <action>
Start with `scripts/verify-guide-seo-graph.test.js` and deep-cloned registry/source projections covering every behavior above. Keep fixtures in memory or isolated temporary roots, restore filesystem state in `finally`, and assert slug plus surface in every negative diagnostic. Exercise the verifier's focused modes and full mode without invoking a production export.

Extend `scripts/verify-guide-seo-graph.js` into the complete Phase 6 source gate. Validate exact groups, ISO dates, 8×2 snapshots, owned root paths, exact alternate keys/targets, localized metadata fields, Open Graph equality, route delegation/static params/dynamicParams, shell/body/asset/link imports, schema token projection, hub card/schema count/order, and sitemap helper wiring. Model cn and io expected graphs independently from the same registry and compare every URL-bearing surface. Preserve focused `--slug`, `--root-articles`, `--articles`, and `--hubs` modes; full mode covers the complete Phase 6 graph.

Extend `src/app/sitemap.ts` per D-09. Select `zh` for cn and `en` for io, add `getGuideCanonicalUrl(locale)` once for the hub, iterate `guideEntries` once for eight article URLs, and pass each localized `dateModified` as a `Date`. Reuse the existing `addEntry`/`seenUrls` guard and preserve all FAQ, technical-center, comparison, contact, home, and pricing entries. Every Guide sitemap URL comes from `guideSeo.ts`; adapter paths and unowned locales stay outside the sitemap.
  </action>
  <acceptance_criteria>
- Both modeled variants contain exactly nine Guide sitemap URLs with unique root owned canonicals and registry dates.
- The full verifier covers all eight requirement IDs and every D-01 through D-09 URL/data connection with slug-specific failures.
- Mutation tests cover group/date/route/metadata/alternate/schema/asset/link/sitemap drift and leave repository sources unchanged.
- Existing sitemap families and `seenUrls` behavior remain intact.
- Source verification uses Node built-ins and inspects no Phase 7 export artifact.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-guide-seo-graph.js &amp;&amp; node --test scripts/verify-guide-seo-graph.test.js &amp;&amp; npx --no-install tsc --noEmit</automated>
  </verify>
  <done>The complete hub/article/navigation/SEO/schema/sitemap graph is reproducibly verified from the shared registry.</done>
</task>

<task type="auto">
  <name>Task 2: Publish one stable Phase 6 regression command and run the source gate</name>
  <files>package.json</files>
  <read_first>
- `package.json` — retain every existing script and all dependency versions.
- `scripts/verify-guide-seo-graph.js` and `scripts/verify-guide-seo-graph.test.js` — finalized Phase 6 command entry points.
- `scripts/verify-guide-content.js` and `scripts/verify-guide-content.test.js` — Phase 5 prerequisite gates that remain green.
  </read_first>
  <action>
Add one `verify:guide-seo-graph` package script that runs `node scripts/verify-guide-seo-graph.js` followed by `node --test scripts/verify-guide-seo-graph.test.js`. Preserve every dependency/devDependency key and value plus `package-lock.json`. Run the Phase 5 source and mutation commands, the new Phase 6 command, and strict TypeScript compilation as one source-level gate. Phase 7 owns clean case-sensitive cn/io exports and exported-HTML inspection; Phase 8 owns immutable delivery and live HTTP evidence.
  </action>
  <acceptance_criteria>
- `npm run verify:guide-seo-graph` executes the full source verifier and mutation suite and exits 0.
- `npm run verify:guide-content` and `npm run verify:guide-content-regression` retain the exact eight-slug/sixteen-document contract.
- Strict TypeScript compilation passes across all new routes/components/helpers.
- Dependency objects and package-lock remain unchanged.
- The command requires only committed repository sources and installed tooling.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:guide-content &amp;&amp; npm run verify:guide-content-regression &amp;&amp; npm run verify:guide-seo-graph &amp;&amp; npx --no-install tsc --noEmit</automated>
  </verify>
  <done>Maintainers have one dependency-free Phase 6 regression command covering the complete source discovery graph.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Registry/route source → sitemap | Build-time data becomes crawler discovery URLs and dates. |
| Mutation fixtures → verifier | Adversarial graph data exercises failure contracts. |
| npm command → release workflow | A stable source gate becomes the prerequisite for Phase 7 artifact checks. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-13 | Spoofing / Tampering | Guide sitemap URLs | medium | mitigate | Current-variant locale selection, central canonical helper, exact 1+8 counts, and seenUrls deduplication. |
| T-06-14 | Repudiation | Graph verifier diagnostics | medium | mitigate | Slug/surface-specific failures and isolated mutation proof for every URL-bearing layer. |
| T-06-15 | Tampering | Test fixtures and repository sources | medium | mitigate | Deep clones or temporary roots, `finally` cleanup, and unchanged-source assertions. |
| T-06-SC | Tampering | npm supply chain | high | mitigate | Package scripts call Node built-ins and installed TypeScript only; dependency manifests remain stable. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `src/app/sitemap.ts`: current-variant Guide hub plus eight article canonical rows.
- `scripts/verify-guide-seo-graph.js`: complete focused/full Phase 6 source verifier.
- `scripts/verify-guide-seo-graph.test.js`: isolated graph mutation regression suite.
- `package.json`: one `verify:guide-seo-graph` command.
</artifacts_this_phase_produces>

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Visitors and crawlers discover and use every paired Guide page through one owned-domain route and SEO graph. | 06-01–04 | COVERED | Article tracer, parallel article/hub expansion, then sitemap/regression seal. |
| REQ | HUB-01 | Two owned `/guide` hubs show eight localized cards in decision/implementation/industry groups. | 06-01, 06-03, 06-04 | COVERED | Validated group field, exact visible groups, and full graph check. |
| REQ | ARTICLE-01 | All owned article routes render approved localized H1 and complete body. | 06-01, 06-02, 06-04 | COVERED | Contained loader plus MarkdownContent and closed routes. |
| REQ | ARTICLE-02 | Breadcrumb, hub return, and configured links use owned canonical destinations. | 06-01, 06-02, 06-04 | COVERED | Shared projection and conditional validated mappings. |
| REQ | ARTICLE-03 | Every required approved image uses responsive next/image and authored alt. | 06-01, 06-02, 06-04 | COVERED | Dimension-complete required union, conditional surface, mutation activation. |
| REQ | SEO-04 | Localized metadata, self canonical, and matching Open Graph URL. | 06-01–04 | COVERED | Central helper consumed by root/adapters and verifier. |
| REQ | SEO-05 | Exact reciprocal zh-CN/en/x-default alternates. | 06-01–04 | COVERED | Dedicated Guide helper and exact-target regressions. |
| REQ | SEO-06 | Article/breadcrumb and hub collection/item-list/breadcrumb schemas. | 06-01–04 | COVERED | JsonLdScript graph from visible registry identity; conditional HowTo included. |
| REQ | SEO-07 | Sitemap, cards, links, params, schema, and breadcrumbs share registry identity. | 06-01–04 | COVERED | Central projection plus current-variant sitemap and graph mutation gate. |
| RESEARCH | — | Filter getBuildLocaleCodes to the exact Guide locale union with default fallback. | 06-01, 06-02, 06-03 | COVERED | Shared helper consumed by both localized adapters. |
| RESEARCH | — | Use a dedicated Guide alternate helper because generic alternates include broader locales. | 06-01, 06-04 | COVERED | Exact three-key projection and drift tests. |
| RESEARCH | — | Add typed snapshot timing from approved delivery authority. | 06-01, 06-04 | COVERED | Both fields are `2026-08-11` for all sixteen snapshots and runtime validated. |
| RESEARCH | — | Reuse MarkdownContent, JsonLdScript, server shell, next/image, and Node built-ins. | 06-01–04 | COVERED | Named interfaces in tasks and supply-chain gates. |
| RESEARCH | — | Current assets and configured links remain conditional approved data. | 06-01, 06-02, 06-04 | COVERED | Current records unchanged; isolated valid fixtures prove activation. |
| RESEARCH | — | Preserve static export architecture and closed route inventory. | 06-01–04 | COVERED | Build-time readers/params only; Phase 7 owns export evidence. |
| CONTEXT | D-01 | Root owned topology, thin adapters, same slug, closed registry params. | 06-01–03 | COVERED | Root aliases plus filtered adapters. |
| CONTEXT | D-02 | Server shell with dictionary/Navbar/Footer/HomeThemeFix/MarkdownContent and fixed server hub. | 06-01–03 | COVERED | Reused server components and zero hub client state. |
| CONTEXT | D-03 | Registry-backed exact 4/1/3 publication groups. | 06-01, 06-03, 06-04 | COVERED | Typed field, visible hub, mutation gate. |
| CONTEXT | D-04 | Loader body and authored blocks remain source-faithful. | 06-01, 06-02, 06-04 | COVERED | Route H1 plus title-aware MarkdownContent. |
| CONTEXT | D-05 | Conditional responsive required-asset surface with authored alt. | 06-01, 06-02, 06-04 | COVERED | Approved dimensions complete next/image contract. |
| CONTEXT | D-06 | Visible breadcrumb/return and explicit configured links only. | 06-01, 06-02, 06-04 | COVERED | Empty current mappings retained; exact targets conditionally rendered. |
| CONTEXT | D-07 | Self canonical, exact alternates, matching Open Graph, localized fields/timing. | 06-01–04 | COVERED | One Guide SEO projection across all routes. |
| CONTEXT | D-08 | Registry-derived hub/article/breadcrumb/conditional HowTo graph. | 06-01–04 | COVERED | Visible and structured nodes share URLs/order. |
| CONTEXT | D-09 | Current variant sitemap adds exactly one hub and eight owned article rows. | 06-04 | COVERED | Existing seenUrls retained. |
| CONTEXT | Deferred | ARTICLE-04, HUB-02, CMS, more Guides, programmatic references. | — | EXCLUDED | Assigned to future work by CONTEXT. |
| CONTEXT | Deferred | Dual-export evidence and initial-JavaScript budget. | — | EXCLUDED | Assigned to Phase 7. |
| CONTEXT | Deferred | Immutable deployment, purge, rollback, and live HTTP evidence. | — | EXCLUDED | Assigned to Phase 8. |
</source_audit>

<risks_dependencies>
## Risks and dependencies

- Plan 06-01 proves the shared interfaces and fixes registry group/date shape. Plans 06-02 and 06-03 then run in parallel with disjoint files. Plan 06-04 consumes both finished surfaces.
- The io build locale inventory is broader than the Guide corpus. The shared locale filter and fallback are required before either localized adapter generates params.
- The current registry has zero required assets and zero configured links. Positive isolated fixtures prove those branches while approved production data remains unchanged.
- Phase 7 consumes the stable `verify:guide-seo-graph` gate and adds case-sensitive cn/io export plus HTML evidence. Phase 8 consumes verified immutable artifacts for delivery/live checks.
</risks_dependencies>

<verification>
1. `npm run verify:guide-content`
2. `npm run verify:guide-content-regression`
3. `npm run verify:guide-seo-graph`
4. `npx --no-install tsc --noEmit`
5. `git diff --exit-code -- package-lock.json`
</verification>

<success_criteria>
- Every Phase 6 requirement has a runnable source-level proof.
- Both modeled sitemap variants contain one hub plus eight unique owned article canonicals.
- Visible navigation, static params, metadata, alternates, Open Graph, schemas, and sitemap all project from the same registry and Guide helper.
- Group/date/route/SEO/schema/asset/link/sitemap mutations fail with actionable context.
- Phase 7 export and Phase 8 delivery surfaces remain available for their assigned evidence work.
</success_criteria>

<output>
Create `.planning/phases/06-guide-hubs-articles-seo-graph/06-04-SUMMARY.md` when done.
</output>
