---
phase: 07-dual-variant-release-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-guide-export.js
  - scripts/verify-guide-export.test.js
  - package.json
autonomous: true
requirements: [VERIFY-04, VERIFY-05]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "VERIFY-04 / D-03 / D-04: a standalone dependency-free command derives the Guide artifact contract from registry.json and validates exported HTML plus sitemap evidence with variant, slug-or-hub, path, and surface diagnostics."
    - "VERIFY-05 / D-06: io maps to the English fastgpt.io root inventory and cn maps to the Chinese fastgpt.cn root inventory; each inventory is exactly /guide plus the same eight registry slugs."
    - "D-03 / D-09: every owned page proves localized H1 and metadata, canonical/Open Graph equality, exact reciprocal alternates, required schema, visible navigation, configured assets/links, and sitemap membership from approved registry data."
    - "D-04: focused export checks require explicit --out-dir and --variant io|cn while source-only Guide checks remain independent of generated output."
  artifacts:
    - path: scripts/verify-guide-export.js
      provides: "Registry-driven Guide static HTML and sitemap verifier"
      exports: [parseArgs, buildGuideExpectation, verifyGuideExport, main]
    - path: scripts/verify-guide-export.test.js
      provides: "Happy-path io/cn artifact fixtures and focused CLI coverage"
    - path: package.json
      provides: "Stable verify:guide-export and verify:guide-export-regression commands"
  key_links:
    - from: scripts/verify-guide-export.js
      to: src/content/guides/registry.json
      via: "registry entries project the exact locale, host, slug, metadata, schema, asset, and link expectations"
      pattern: "src/content/guides/registry\\.json"
    - from: scripts/verify-guide-export.js
      to: out/guide
      via: "route.html or route/index.html resolution for one hub and eight root articles"
      pattern: "index\\.html"
    - from: scripts/verify-guide-export.js
      to: out/sitemap.xml
      via: "exact owned Guide URL set comparison"
      pattern: "sitemap\\.xml"
    - from: package.json
      to: scripts/verify-guide-export.js
      via: "direct focused debugging command"
      pattern: "verify:guide-export"
---

<objective>
Create the production-quality vertical slice for Guide release evidence: one registry-driven verifier that reads a selected static export, proves the exact owned Guide inventory, and validates rendered crawler and visitor surfaces for both variants per D-03, D-04, D-06, and D-09.

Purpose: Give maintainers a focused artifact debugger before the aggregate release coordinator composes it.
Output: Standalone Guide export verifier, happy-path fixture harness, and stable npm commands.

Decision trace: D-03: complete artifact contract and scoped diagnostics; D-04: existing static-output and parsing conventions; D-06: locale/owner inventory; D-09: registry data remains authoritative.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dual-variant-release-evidence/07-CONTEXT.md
@.planning/phases/07-dual-variant-release-evidence/07-RESEARCH.md
@.planning/phases/05-guide-content-contract/05-04-SUMMARY.md
@.planning/phases/06-guide-hubs-articles-seo-graph/06-04-SUMMARY.md
@src/content/guides/registry.json
@src/lib/guideSeo.ts
@src/components/guide/GuideHubRoute.tsx
@src/components/guide/GuideArticleRoute.tsx
@src/components/guide/GuideHubPage.tsx
@src/components/guide/GuideArticlePage.tsx
@src/app/sitemap.ts
@scripts/verify-p0.js
@scripts/verify-p1.js
@scripts/verify-p2.js
@scripts/verify-faq-seo-graph.js
@package.json

<interfaces>
- `registry.json` exposes `{ entries: GuideEntry[] }`; every entry has one lower-case `slug`, one `group`, and localized `zh`/`en` snapshots containing H1, metadata, canonical, hreflang, schema tokens, asset policy, configured links, and dates.
- The exact slug order is `saas-platform-enterprise-gaps`, `self-build-three-year-tco`, `server-sizing-guide`, `complex-doc-golden-set`, `support-bot-four-steps`, `manufacturing-itops-invoice-audit`, `pharma-compliance-docs`, `education-retail-support-insight`.
- The planned CLI is `node scripts/verify-guide-export.js --out-dir &lt;dir&gt; --variant io|cn`; export `parseArgs`, `buildGuideExpectation`, `verifyGuideExport`, and `main` for isolated Node tests.
- `io` selects `en` plus `https://fastgpt.io`; `cn` selects `zh` plus `https://fastgpt.cn`. Both own only `/guide` and `/guide/&lt;slug&gt;` root paths.
- Existing verifier helpers establish `route.html`/`route/index.html` resolution, tolerant tag/attribute extraction, HTML-entity decoding, JSON-LD parsing, and sitemap `<loc>` extraction.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace registry identity through one complete io Guide export</name>
  <files>scripts/verify-guide-export.js, scripts/verify-guide-export.test.js</files>
  <read_first>
- `.planning/phases/07-dual-variant-release-evidence/07-CONTEXT.md` D-03, D-04, D-06, and D-09 — artifact, parsing, ownership, and fidelity contracts.
- `src/content/guides/registry.json` — exact eight-slug order and localized expected values.
- `scripts/verify-p1.js` — root-aware HTML resolution, tag/attribute extraction, entity decoding, and canonical handling.
- `scripts/verify-faq-seo-graph.js` — recursive JSON-LD parsing, exact alternate comparison, route inventory, and sitemap set patterns.
- `src/components/guide/GuideHubRoute.tsx` and `GuideArticleRoute.tsx` — actual JSON-LD shapes and breadcrumb URL sources.
  </read_first>
  <behavior>
- An isolated io fixture containing exactly `/guide` plus the eight registry article paths passes the route and sitemap inventory gate.
- The tracer inspects the hub and first registry article end to end: localized H1/title/description, self canonical, matching `og:url`, exact en/zh-CN/x-default links, required JSON-LD types, visible hub/article navigation, and sitemap membership.
- `--variant io` selects the English snapshot and fastgpt.io owner; an unknown variant, missing flag value, or absent output path exits with an actionable argument/path diagnostic.
- Every assertion label begins with `variant=io`, names `slug=hub` or the registry slug, names the resolved output path, and names the failed surface.
- Requiring the verifier module stays silent and performs no CLI work.
  </behavior>
  <action>
Start with an isolated `node:test` fixture that writes the exact nine-page io inventory plus `sitemap.xml` under a temporary output root and invokes an exported pure verifier function. Preserve any repository `out/` directory and clean the temporary root in `finally`.

Create `scripts/verify-guide-export.js` with Node built-ins only. Parse the explicit CLI contract, validate the selected variant, read and validate `registry.json`, project the exact owned route set, resolve static HTML through the established flat-or-index convention, parse tags and JSON-LD defensively, and compare the tracer hub/article plus sitemap against registry and owner expectations. Construct assertion context once as `variant`, `slug|hub`, `path`, and `surface` so parse, identity, and missing-file failures retain the complete diagnostic prefix per D-03/D-04.

Keep all reads artifact-only and deterministic. Resolve route paths from validated lower-case slugs, retain registry order, and derive owner URLs from the fixed io/en projection per D-06/D-09. The tracer code becomes the permanent loop and parser skeleton used by Task 2.
  </action>
  <acceptance_criteria>
- The io tracer fixture contains exactly nine owned root Guide HTML routes and nine matching owner sitemap URLs.
- One hub and one real registry article pass the full visitor/crawler surface path through the production verifier.
- Invalid CLI and missing-artifact cases exit nonzero with variant/path/surface context.
- The module is import-safe, uses Node built-ins, and leaves repository output/source files unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-name-pattern="tracer accepts exact io Guide inventory" scripts/verify-guide-export.test.js</automated>
  </verify>
  <done>A directly runnable io Guide artifact slice proves registry → route file → rendered surface → sitemap wiring end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the verifier across every owned page and both variants</name>
  <files>scripts/verify-guide-export.js, scripts/verify-guide-export.test.js, package.json</files>
  <read_first>
- The Task 1 verifier and fixture seam — retain its CLI, parser, diagnostic prefix, and temporary-root isolation.
- `src/content/guides/registry.json` — inspect every localized snapshot field, conditional asset policy, configured link, schema token, and date.
- `src/components/guide/GuideHubPage.tsx` and `GuideArticlePage.tsx` — visible card, breadcrumb, return, asset, and configured-link markup.
- `src/lib/guideSeo.ts` — canonical, Open Graph, and exact three-language alternate projection.
- `src/app/sitemap.ts` — current-variant hub/article projection through the registry.
- `package.json` — preserve every existing script and both dependency objects.
  </read_first>
  <behavior>
- Both io/en/fastgpt.io and cn/zh/fastgpt.cn happy fixtures pass exactly nine owned root pages and nine exact sitemap URLs in registry order.
- Every hub proves H1/title/description, canonical and `og:url`, exact alternate cluster, CollectionPage/ItemList/BreadcrumbList, and one visible owned card destination per slug.
- Every article proves localized H1/title/description, canonical and `og:url`, exact alternate cluster, Article/BreadcrumbList plus registry-authorized HowTo, Home → Guide → article breadcrumb URLs, localized hub return, and sitemap membership.
- A `required` asset proves exact public `src` plus authored `alt`; each configured internal link proves its exact label and owned target; current non-required/empty records produce no inferred expectation.
- Exact root inventory excludes localized adapter paths from the owned count and rejects missing, duplicate, or extra root Guide pages.
  </behavior>
  <action>
Extend the fixture builder and production verifier from the tracer loop to all eight entries and both locked variants. Compare decoded title/description/H1 text exactly with the selected snapshot; compare canonical, Open Graph URL, alternate keys/targets, breadcrumb URLs, visible card/return/configured-link targets, required asset path/alt, schema type presence, and Guide sitemap URL sets. Traverse JSON-LD objects and `@graph` arrays so compiler serialization order cannot alter the result.

Treat the owned inventory as the registry-projected set of nine root paths per D-03/D-06. Enumerate generated root Guide HTML, normalize flat/index representations to routes, and compare exact sets so localized adapters remain implementation artifacts outside the owned public count. Preserve source asset/link policy verbatim and create expectations only for `required` assets and configured mappings per D-09.

Add `verify:guide-export` as the direct CLI entry and `verify:guide-export-regression` as the Node test entry in `package.json`. Preserve dependency objects, package-lock, and every existing command. Keep source verifiers independent of export availability per D-04.
  </action>
  <acceptance_criteria>
- io and cn happy fixtures each prove the exact nine-path inventory listed in `<interfaces>` with the correct locale and case-sensitive owner host.
- All hub/article metadata, alternate, schema, navigation, optional-surface, and sitemap assertions run for every registry entry.
- The direct verifier requires explicit output and variant arguments and reports success with variant plus nine-page/nine-sitemap counts.
- `verify:guide-export-regression` runs the happy artifact matrix without touching repository `out/`.
- Package dependencies and package-lock remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/verify-guide-export.test.js &amp;&amp; npm run verify:guide-content &amp;&amp; npm run verify:guide-seo-graph</automated>
  </verify>
  <done>Maintainers can focus-debug either owner artifact against the complete registry-driven Guide HTML and sitemap contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI/registry → filesystem | Variant, output directory, and registry slugs select generated files to read. |
| Generated HTML/JSON-LD → assertions | Compiler output becomes release evidence only after defensive parsing and exact comparison. |
| Registry snapshots → public URL expectations | Repository content data controls host, path, assets, links, and schema expectations. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-01 | Tampering | Registry slug and output route resolution | medium | mitigate | Validate lower-case slug identity, resolve only projected Guide paths, and compare an exact nine-route set. |
| T-07-02 | Tampering | Generated metadata and JSON-LD parsing | medium | mitigate | Parse defensively, reject malformed JSON-LD on required pages, and compare exact localized values and schema types. |
| T-07-03 | Repudiation | Artifact failure diagnostics | medium | mitigate | Prefix every failure with variant, slug-or-hub, resolved path, and surface. |
| T-07-SC | Tampering | npm supply chain | high | mitigate | Use Node built-ins and existing packages only; dependency manifests and lockfile stay unchanged. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `scripts/verify-guide-export.js`: standalone registry-driven Guide artifact verifier.
- `scripts/verify-guide-export.test.js`: isolated exact io/cn happy artifact fixtures.
- `package.json`: focused Guide artifact and regression command entries.
</artifacts_this_phase_produces>

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reproduce and inspect complete release-safe Guide exports for both owned variants. | 07-01, 07-03 | COVERED | Focused artifact verifier precedes aggregate dual-build composition. |
| REQ | VERIFY-04 | Validate registry, source-derived metadata/assets/links, routes, SEO, sitemap, and exported HTML with slug-specific failures. | 07-01, 07-02, 07-03 | COVERED | Plan 01 creates the complete artifact half of the unified gate. |
| REQ | VERIFY-05 | Exact case-sensitive cn/io one-hub/eight-article artifacts retain the initial-JavaScript budget. | 07-01, 07-03 | COVERED | Plan 01 locks inventory; Plan 03 supplies host and budget evidence. |
| RESEARCH | — | Add one dependency-free standalone Guide export verifier and reuse existing parsing conventions. | 07-01 | COVERED | Node built-ins plus P0/P1/P2/FAQ patterns. |
| RESEARCH | — | Project exact registry-owned io/en and cn/zh inventories. | 07-01 | COVERED | Same eight slugs plus one root hub on both owners. |
| CONTEXT | D-03 | Verify complete Guide HTML/sitemap surfaces with scoped diagnostics. | 07-01, 07-02 | COVERED | Full artifact contract and negative matrix. |
| CONTEXT | D-04 | Reuse static-output parsing and explicit output/variant CLI. | 07-01 | COVERED | Direct focused command with import-safe module API. |
| CONTEXT | D-06 | Exact owner locale and nine-page root inventory per variant. | 07-01, 07-03 | COVERED | Artifact set and release build env share the locked mapping. |
| CONTEXT | D-09 | Preserve registry order and approved bytes/metadata/asset/link policy. | 07-01, 07-02 | COVERED | Expectations come solely from committed registry data. |
| CONTEXT | Deferred | Deployment, live HTTP, cache, revision, rollback, language switcher, filtering, CMS, more content. | — | EXCLUDED | Assigned to Phase 8 or future requirements. |
</source_audit>

<risks_dependencies>
## Risks and dependencies

- Phase 5 provides the immutable 8×2 registry/source contract; Phase 6 provides rendered hub/article/schema/sitemap surfaces.
- Generated HTML may serialize tags and JSON-LD in variable order; the verifier compares parsed attributes and values.
- The current workspace is case-insensitive; isolated fixtures run here while Plan 03 owns full Linux/container build evidence.
</risks_dependencies>

<verification>
1. Run `node --test scripts/verify-guide-export.test.js`.
2. Run `npm run verify:guide-content` and `npm run verify:guide-seo-graph` to prove the source contracts remain green.
3. Confirm `git diff --exit-code -- package-lock.json` and inspect package dependency objects for zero changes.
</verification>

<success_criteria>
- One direct command validates any selected io or cn `out/` artifact against exact registry-backed HTML and sitemap expectations.
- Happy fixtures prove all nine owned paths for both variants without repository artifact mutation.
- Failures carry the exact diagnostic dimensions required by D-03.
- Existing Guide source and source-graph gates remain runnable without an export.
</success_criteria>

<output>
Create `.planning/phases/07-dual-variant-release-evidence/07-01-SUMMARY.md` when done.
</output>
