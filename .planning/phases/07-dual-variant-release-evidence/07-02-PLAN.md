---
phase: 07-dual-variant-release-evidence
plan: "02"
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - scripts/verify-guide-export.js
  - scripts/verify-guide-export.test.js
autonomous: true
requirements: [VERIFY-04, VERIFY-05]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "VERIFY-04 / D-03: isolated mutations prove every required Guide HTML, SEO, schema, link, asset, route, and sitemap failure names variant, slug-or-hub, output path, and surface."
    - "VERIFY-05 / D-05 / D-06: route-set fixtures reject missing, duplicate, extra, wrong-owner, wrong-locale, and adapter-inflated inventories while preserving the exact nine owned paths."
    - "D-04 / D-09: regression fixtures reuse production parser semantics, mutate temporary output only, preserve registry/source files, and retain approved metadata, asset, link, and order expectations."
  artifacts:
    - path: scripts/verify-guide-export.test.js
      provides: "Complete Guide export mutation and CLI regression matrix"
    - path: scripts/verify-guide-export.js
      provides: "Any fixes required for every negative contract to fail closed"
  key_links:
    - from: scripts/verify-guide-export.test.js
      to: scripts/verify-guide-export.js
      via: "direct pure-function imports plus spawned CLI checks"
      pattern: "require\('./verify-guide-export'\)"
    - from: scripts/verify-guide-export.test.js
      to: src/content/guides/registry.json
      via: "structured-cloned approved expectations drive isolated mutations"
      pattern: "registry\\.json"
---

<objective>
Seal the standalone Guide artifact verifier with focused regression evidence for every VERIFY-04/05 failure class, implementing D-03, D-04, D-05, D-06, and D-09 before coordinator composition.

Purpose: Make a green Guide export result trustworthy and a red result immediately diagnosable across all sixteen localized article expectations.
Output: Isolated dual-variant fixture factory, complete mutation matrix, and any fail-closed parser corrections exposed by the tests.

Decision trace: D-03: diagnostic dimensions and rendered surfaces; D-04: parser/CLI patterns; D-05: case-sensitive evidence; D-06: exact owner inventories; D-09: approved source projections.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/07-dual-variant-release-evidence/07-CONTEXT.md
@.planning/phases/07-dual-variant-release-evidence/07-RESEARCH.md
@.planning/phases/07-dual-variant-release-evidence/07-01-SUMMARY.md
@src/content/guides/registry.json
@scripts/verify-guide-content.test.js
@scripts/verify-guide-seo-graph.test.js
@scripts/verify-release.test.js
@scripts/verify-guide-export.js
@scripts/verify-guide-export.test.js

<interfaces>
- Plan 07-01 exports `parseArgs`, `buildGuideExpectation`, `verifyGuideExport`, and `main`; regressions exercise pure verification directly and the guarded CLI through `spawnSync`.
- `buildGuideExpectation('io')` and `buildGuideExpectation('cn')` must each project exactly nine root paths and nine owned absolute sitemap URLs from the same eight ordered registry entries.
- Failure labels use `variant=&lt;io|cn&gt; slug=&lt;hub|registry-slug&gt; path=&lt;resolved path&gt; surface=&lt;contract surface&gt;: &lt;reason&gt;`.
- Established Guide tests use `structuredClone`, `node:test`, temporary roots, `finally` cleanup, silent import checks, and source-unchanged assertions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mutate every rendered Guide contract surface</name>
  <files>scripts/verify-guide-export.test.js, scripts/verify-guide-export.js</files>
  <read_first>
- `.planning/phases/07-dual-variant-release-evidence/07-CONTEXT.md` D-03, D-04, and D-09 — exact surfaces, diagnostics, and approved-data policy.
- `scripts/verify-guide-export.js` and its happy fixtures from Plan 07-01 — extend the established fixture/parser seam.
- `scripts/verify-guide-content.test.js` — slug/label assertions and isolated source mutation pattern.
- `scripts/verify-guide-seo-graph.test.js` — cloned registry and URL/schema/sitemap mutation pattern.
- `src/content/guides/registry.json` — choose real localized strings, HowTo entries, and conditional asset/link fixture data.
  </read_first>
  <behavior>
- Hub mutations independently fail H1, title, description, canonical, Open Graph URL, each alternate target/key, CollectionPage, ItemList, BreadcrumbList, card link, and sitemap membership surfaces.
- Article mutations independently fail H1, title, description, canonical, Open Graph URL, each alternate target/key, Article, BreadcrumbList, authorized HowTo, breadcrumb URL sequence, hub return, and sitemap membership surfaces.
- A cloned registry fixture with one required asset detects wrong/missing `src` and `alt`; one configured-link fixture detects wrong/missing label or target.
- Both io and cn cases assert the exact variant, real slug or hub token, output path, and surface in the thrown diagnostic.
- Malformed JSON-LD on a page requiring that schema fails with the same scoped context.
  </behavior>
  <action>
Extend the temporary artifact builder so each test can mutate one rendered field or one cloned registry snapshot while generating every other page from approved expectations. Table-drive hub and article cases across both variants, using at least one HowTo slug plus one standard Article slug. Assert the complete diagnostic prefix and surface-specific reason for every mutation.

Exercise exact metadata and link values after HTML entity decoding, JSON-LD traversal through objects/arrays/`@graph`, conditional schema tokens, visible breadcrumb/card/return targets, asset `src`/`alt`, configured labels/targets, and exact sitemap membership per D-03/D-04. Keep parser corrections inside `verify-guide-export.js` narrowly tied to a failing regression.

Use approved registry copies as expectation data per D-09. Required asset and configured-link activation happens only inside isolated cloned fixtures; repository registry and authored source files remain byte-stable.
  </action>
  <acceptance_criteria>
- Every required hub and article surface has one focused mutation that fails with all four diagnostic dimensions.
- Tests cover io and cn localized values, both standard Article and HowTo schema policy, and conditional asset/link activation.
- Malformed required JSON-LD and missing required tags fail closed.
- Fixture setup/cleanup cannot mutate repository Guide data or `out/`.
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-name-pattern="Guide export surface mutations" scripts/verify-guide-export.test.js</automated>
  </verify>
  <done>Every rendered contract surface has executable, slug-scoped regression evidence for both owner variants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove exact inventory, path safety, and focused CLI behavior</name>
  <files>scripts/verify-guide-export.test.js, scripts/verify-guide-export.js</files>
  <read_first>
- `.planning/phases/07-dual-variant-release-evidence/07-CONTEXT.md` D-05 and D-06 — case-sensitive host and exact owned inventory policy.
- `scripts/verify-release.js` — current case-fold probe, collision diagnostic, and explicit variant argument behavior.
- `scripts/verify-faq-seo-graph.js` — exact route set, folded-path collision, and sitemap owner checks.
- `scripts/verify-guide-export.js` — retain the explicit output/variant contract and exact root route normalization.
  </read_first>
  <behavior>
- Removing a hub/article, adding an extra root Guide page, or duplicating a flat/index representation fails route cardinality and names the affected path/surface.
- A case-fold collision fixture fails with both published route spellings and case-sensitive-host guidance.
- Root inventory remains exactly the hub plus eight slugs; `/en/guide`, `/zh/guide`, and their article adapters never count as owned pages.
- Wrong owner host, locale snapshot, sitemap duplicate, sitemap extra, sitemap omission, and malformed sitemap URL each fail with scoped evidence.
- CLI regression covers missing/invalid `--out-dir`, missing/invalid `--variant`, unknown arguments, successful io/cn direct invocation, concise success counts, silent import, and nonzero failure exit.
  </behavior>
  <action>
Add exact-set regression cases for missing, duplicate, extra, wrong-owner, wrong-locale, adapter-path, case-fold, and sitemap inventory drift per D-05/D-06. Model flat `.html` and nested `index.html` outputs and assert each route normalizes once. Use a deliberate mixed-case fixture pair to prove the path collision gate independently of the host filesystem.

Spawn the guarded CLI against temporary io/cn artifacts and assert status, stdout success counts, and stderr diagnostic context. Validate argument parsing before artifact reads and keep output paths explicit per D-04. Retain source-only verifier independence by importing the module in an empty process and asserting silent output.

Run the entire regression matrix, then run the Phase 5 and Phase 6 Guide source gates to prove D-09 content/graph contracts remain intact.
  </action>
  <acceptance_criteria>
- Exact route and sitemap inventories reject every missing/duplicate/extra/adapter/wrong-owner case.
- Case-fold collision coverage runs deterministically on the current case-insensitive workspace and on Linux.
- Both focused CLI variants pass valid fixtures and fail invalid arguments/artifacts with the locked diagnostic dimensions.
- The complete Guide export regression command passes alongside Phase 5/6 Guide source commands.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:guide-export-regression &amp;&amp; npm run verify:guide-content &amp;&amp; npm run verify:guide-seo-graph</automated>
  </verify>
  <done>The focused verifier is regression-proven against artifact identity, case, locale, host, route, and sitemap drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Mutation fixture → production verifier | Adversarial generated artifacts exercise every fail-closed contract. |
| Temporary output → repository workspace | Tests must isolate and clean generated files without touching authored data. |
| Route spelling → filesystem identity | Case folding can collapse distinct published paths on some hosts. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-04 | Tampering | Artifact mutation coverage | medium | mitigate | One isolated mutation per required surface with exact scoped failure assertions. |
| T-07-05 | Tampering | Case-folded route inventory | high | mitigate | Deterministic collision fixture plus exact-set path comparison and host guidance. |
| T-07-06 | Repudiation | Fixture cleanup and source integrity | medium | mitigate | Temporary roots, `finally` cleanup, and byte-stable source assertions. |
| T-07-SC | Tampering | npm supply chain | high | mitigate | Node built-ins and existing lockfile only; no dependency manifest change. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `scripts/verify-guide-export.test.js`: complete dual-variant Guide artifact mutation matrix.
- `scripts/verify-guide-export.js`: regression-driven fail-closed corrections, if required by the matrix.
</artifacts_this_phase_produces>

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Inspect trustworthy release-safe Guide exports for both variants. | 07-01–03 | COVERED | This plan proves artifact verifier failure quality. |
| REQ | VERIFY-04 | Slug-specific failures across every source-derived exported surface. | 07-02 | COVERED | Complete mutation matrix with four diagnostic dimensions. |
| REQ | VERIFY-05 | Exact case-sensitive nine-page owner inventories. | 07-02, 07-03 | COVERED | Deterministic route/case regressions plus Linux/container execution. |
| RESEARCH | — | Use node:test, temporary output fixtures, cloned registries, and production parser helpers. | 07-02 | COVERED | Reuses established Phase 5/6 test patterns. |
| RESEARCH | — | Reject adapter inflation, stale/wrong variant data, and generic aggregate-only errors. | 07-02, 07-03 | COVERED | Exact-set fixture and coordinator freshness ordering. |
| CONTEXT | D-03 | Every failure names variant, slug/hub, output path, and surface. | 07-01, 07-02 | COVERED | Assertions cover every required surface. |
| CONTEXT | D-04 | Explicit output/variant CLI and existing parser conventions. | 07-01, 07-02 | COVERED | Spawned CLI and pure seam share production code. |
| CONTEXT | D-05 | Case-sensitive full export policy with actionable collision evidence. | 07-02, 07-03 | COVERED | Fixture-level collision proof plus host-level gate. |
| CONTEXT | D-06 | io/en and cn/zh each own exactly nine root Guide pages. | 07-01–03 | COVERED | Exact path/host/locale matrix. |
| CONTEXT | D-09 | Preserve approved registry/source policy and order. | 07-01, 07-02 | COVERED | Cloned fixture activation and source-unchanged checks. |
| CONTEXT | Deferred | Phase 8 delivery/live evidence and future Guide enhancements. | — | EXCLUDED | Outside Phase 7. |
</source_audit>

<risks_dependencies>
## Risks and dependencies

- Plan 07-01 must expose import-safe functions and a reusable temporary fixture seam.
- Compiler output can vary in attribute and JSON-LD ordering; tests mutate parsed meaning instead of serialized order.
- The current registry has no required assets or configured links; cloned test records activate those approved contract branches.
</risks_dependencies>

<verification>
1. Run `npm run verify:guide-export-regression`.
2. Run `npm run verify:guide-content` and `npm run verify:guide-seo-graph`.
3. Confirm repository `out/`, registry JSON, and authored Guide documents retain their pre-test state.
</verification>

<success_criteria>
- Every VERIFY-04 artifact surface and VERIFY-05 inventory/case invariant has an isolated regression.
- Diagnostics always identify the variant, hub-or-slug, resolved path, and failed surface.
- Test fixtures remain portable across case-insensitive macOS and case-sensitive Linux.
- Approved Guide registry/source content stays unchanged.
</success_criteria>

<output>
Create `.planning/phases/07-dual-variant-release-evidence/07-02-SUMMARY.md` when done.
</output>
