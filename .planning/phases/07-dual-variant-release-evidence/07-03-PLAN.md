---
phase: 07-dual-variant-release-evidence
plan: "03"
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - scripts/verify-release.js
  - scripts/verify-release.test.js
  - .github/workflows/guide-release-verification.yml
  - Dockerfile.verify
autonomous: true
requirements: [VERIFY-04, VERIFY-05]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "VERIFY-04 / D-01 / D-02: npm run verify:release preserves every FAQ gate and existing CLI option, runs Guide source gates against each selected variant immediately before its clean build, then validates the matching generated Guide artifact before cleanup."
    - "VERIFY-04 / D-03 / D-04 / D-09: the aggregate command composes the standalone registry-driven Guide export verifier and retains command, variant, slug-or-hub, path, and surface evidence on failure."
    - "VERIFY-05 / D-05 / D-06: full mode fails closed on a case-insensitive host and a dedicated Ubuntu workflow or verification container runs clean io then cn builds with exactly nine owned Guide paths each."
    - "VERIFY-05 / D-07: existing verify:p1 remains a blocking 260 KiB gzip gate for each variant and successful release output records the measured KiB value with its variant."
    - "D-08: all changes stop at repository source, build, generated artifact, CI, and container verification; Phase 8 retains deployment, live HTTP, cache, revision, and rollback ownership."
  artifacts:
    - path: scripts/verify-release.js
      provides: "Single FAQ plus Guide source/build/artifact release coordinator"
    - path: scripts/verify-release.test.js
      provides: "Coordinator order, P1 evidence, option, and Linux/container regression coverage"
    - path: .github/workflows/guide-release-verification.yml
      provides: "Case-sensitive Ubuntu io/cn release-evidence job"
    - path: Dockerfile.verify
      provides: "Dedicated case-sensitive container-filesystem fallback for the same release command"
  key_links:
    - from: scripts/verify-release.js
      to: scripts/verify-guide-content.js
      via: "Guide registry/source fidelity gate before each selected variant build"
      pattern: "verify-guide-content\\.js"
    - from: scripts/verify-release.js
      to: scripts/verify-guide-seo-graph.js
      via: "Guide route/SEO/sitemap source graph gate before each selected variant build"
      pattern: "verify-guide-seo-graph\\.js"
    - from: scripts/verify-release.js
      to: scripts/verify-guide-export.js
      via: "matching out artifact inspection inside the same variant lifecycle"
      pattern: "verify-guide-export\\.js"
    - from: scripts/verify-release.js
      to: scripts/verify-p1.js
      via: "existing per-variant blocking budget check and surfaced success measurement"
      pattern: "verify:p1"
    - from: .github/workflows/guide-release-verification.yml
      to: scripts/verify-release.js
      via: "npm run verify:release on ubuntu-24.04"
      pattern: "npm run verify:release"
    - from: Dockerfile.verify
      to: scripts/verify-release.js
      via: "container build executes the same command on container-local case-sensitive storage"
      pattern: "npm run verify:release"
---

<objective>
Compose the proven Guide source and artifact gates into the established release command, then provide reproducible case-sensitive Linux and container evidence for exact io/cn exports, implementing D-01 through D-09 across VERIFY-04 and VERIFY-05.

Purpose: Close Phase 7 with one maintainer command whose success means the FAQ and Guide source contracts, both fresh static exports, exact owner inventories, rendered SEO surfaces, sitemap, and 260 KiB performance budget all passed.
Output: Extended release coordinator/regressions, Ubuntu verification workflow, and dedicated Docker verification fallback.

Decision trace: D-01: established coordinator and CLI; D-02: FAQ preservation plus per-variant freshness; D-03: registry-driven artifact evidence; D-04: explicit output/variant parsing; D-05: case-sensitive host policy; D-06: exact io/cn owner inventories; D-07: measured blocking budget; D-08: build/artifact-only phase boundary; D-09: approved Guide identity and order.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dual-variant-release-evidence/07-CONTEXT.md
@.planning/phases/07-dual-variant-release-evidence/07-RESEARCH.md
@.planning/milestones/v1.0-phases/04-redirects-and-release-gate/04-01-SUMMARY.md
@.planning/phases/05-guide-content-contract/05-04-SUMMARY.md
@.planning/phases/06-guide-hubs-articles-seo-graph/06-04-SUMMARY.md
@.planning/phases/07-dual-variant-release-evidence/07-01-SUMMARY.md
@.planning/phases/07-dual-variant-release-evidence/07-02-SUMMARY.md
@scripts/verify-release.js
@scripts/verify-release.test.js
@scripts/verify-guide-content.js
@scripts/verify-guide-seo-graph.js
@scripts/verify-guide-export.js
@scripts/verify-p1.js
@scripts/clean-locale-output.js
@next.config.js
@Dockerfile
@.dockerignore
@.github/workflows/preview.yml
@package.json

<interfaces>
- `parseArgs()` already exposes `--source-only`, `--variant io|cn`, and `--keep-artifacts`; every option and validation remains stable per D-01.
- `runStep`/`nodeStep`/`npmStep` collect child stdout/stderr, aggregate command/variant failures, and log concise success lines.
- `runSourceChecks` currently owns the complete FAQ generator/source/TypeScript sequence; retain its order and contents per D-02.
- `variantEnvironment('io'|'cn')` supplies explicit `NODE_ENV`, site variant, owned home URLs, and language region; `clearBuildArtifacts()` brackets each variant.
- `verify-guide-content.js` and `verify-guide-seo-graph.js` are source-only commands; `verify-guide-export.js --out-dir out --variant &lt;variant&gt;` is the matching artifact command from Plans 07-01/02.
- `verify:p1` emits `P1 verification passed for &lt;owner&gt;: &lt;value&gt; KiB initial JavaScript gzip` and fails above the existing 260 KiB constant.
- The exact full-mode variant order is `io`, then `cn`; each artifact lifecycle is clear → Guide source gates → build → existing artifact checks including P1 → Guide export check → retain on failure when requested → clear.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compose one fresh FAQ plus Guide release gate</name>
  <reversibility rating="costly">D-01 changes the established release command consumed by CI and maintainer workflows; undo requires coordinated command and workflow changes.</reversibility>
  <files>scripts/verify-release.js, scripts/verify-release.test.js</files>
  <read_first>
- `.planning/phases/07-dual-variant-release-evidence/07-CONTEXT.md` D-01 through D-09 — complete locked order, evidence, host, budget, and scope policy.
- `scripts/verify-release.js` — preserve argument handling, FAQ checks, failure aggregation, case probe, variant env, retention, generated-public snapshot, and cleanup.
- `scripts/verify-release.test.js` — retain FAQ metadata fixtures, P1 advisory contract, case-sensitive skip behavior, and import-safety style.
- `scripts/verify-guide-content.js`, `verify-guide-seo-graph.js`, and `verify-guide-export.js` — exact new source/artifact entry points and diagnostics.
- `scripts/verify-p1.js` — unchanged 260 KiB assertion and successful measured-output format.
  </read_first>
  <behavior>
- `--source-only` runs every existing FAQ source check followed by one Guide content and one Guide SEO graph source check, performs no build/artifact reads, and retains the existing success diagnostic.
- Full default mode runs current FAQ source checks once, probes case sensitivity, then processes io and cn in locked order; `--variant` selects exactly one lifecycle.
- Each lifecycle clears artifacts, sets its explicit owner env, runs both Guide source gates with variant context, builds, runs every existing artifact gate, runs the matching Guide export verifier, applies retention policy, and clears before the next lifecycle.
- Any Guide source/export failure enters the existing aggregate report with originating command and variant while preserving its slug/hub/path/surface evidence.
- A successful P1 step remains blocking at 260 KiB and the coordinator success log includes variant plus the measured KiB; historical 266.9 KiB comparison remains advisory only after a budget failure.
- Existing FAQ commands, counts, options, generated-public restoration, and final cleanup behavior remain unchanged.
  </behavior>
  <action>
Add coordinator regressions first. Assert existing FAQ source-step tokens retain order, Guide content/SEO steps follow them in source-only mode, each full variant places Guide source steps between artifact cleanup/environment selection and build, and the Guide export step stays inside the same post-build check block before variant cleanup. Add focused coverage for Guide failure aggregation, io-before-cn order, selected-variant behavior, all three existing CLI options, and P1 success-measurement formatting.

Extend `verify-release.js` per D-01/D-02 with a small `runGuideSourceChecks` seam. Source-only mode calls it once after the current FAQ/TypeScript gates. Full mode calls it with each selected variant immediately before that variant's build, then calls `verify-guide-export.js --out-dir out --variant &lt;variant&gt;` after the existing matching artifact checks and before retention/cleanup. Keep every FAQ step and `verifyExportCardinality` intact.

Surface successful P1 child evidence by extracting its existing concise measurement line from captured output and appending it to the coordinator's P1 pass log with variant; preserve the 260 KiB failure in the aggregate list and the existing c77cf48 advisory logic per D-07. Preserve case-probe failure context and enhance its remediation text to name the Guide Release Verification workflow, a container-local verification build, and case-sensitive APFS per D-05.

Keep all implementation inside build/artifact verification per D-08. Preserve registry order, owner projection, source gates, and slug-scoped child output per D-03/D-04/D-06/D-09.
  </action>
  <acceptance_criteria>
- `npm run verify:release -- --source-only` includes all unchanged FAQ source gates plus Guide content and Guide SEO graph gates and exits 0.
- Coordinator regressions prove io-before-cn order, selected-variant mode, per-variant Guide source freshness, matching Guide export inspection, cleanup, and failure aggregation.
- Existing `--source-only`, `--variant`, `--keep-artifacts`, FAQ cardinality, generated-public restoration, and artifact retention contracts remain green.
- Each successful P1 log names the variant and measured KiB; values above 260 KiB remain blocking failures with advisory context kept separate.
- The coordinator references no deployment, network, cache, revision, or rollback operation.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:release-regression &amp;&amp; npm run verify:release -- --source-only</automated>
  </verify>
  <done>`npm run verify:release` is the single complete FAQ plus Guide source/build/artifact gate with fresh variant-scoped evidence and measured blocking budgets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add case-sensitive Ubuntu evidence and container-local fallback</name>
  <files>.github/workflows/guide-release-verification.yml, Dockerfile.verify, scripts/verify-release.test.js</files>
  <read_first>
- `.planning/phases/07-dual-variant-release-evidence/07-CONTEXT.md` D-05, D-06, D-07, and D-08 — case-sensitive host, exact variants, budget, and phase fence.
- `.github/workflows/preview.yml` — existing ubuntu-24.04, checkout, Node 24, concurrency, and read-only permission pattern.
- `Dockerfile` — preserve its cn publication guard and production image behavior unchanged.
- `.dockerignore` — existing build-context exclusions keep generated artifacts and local dependencies outside the verification image.
- `scripts/verify-release.js` — full command owns env, io/cn order, cleanup, failure retention, and all evidence checks.
  </read_first>
  <behavior>
- A repository workflow runs on `ubuntu-24.04`, installs the locked npm tree, and executes `npm run verify:release -- --keep-artifacts` with read-only repository permission.
- The workflow runs both io and cn through the coordinator and uploads retained `.release-artifacts` only after failure when evidence exists.
- A dedicated `Dockerfile.verify` copies the repository into container-local case-sensitive storage, runs `npm ci`, then runs the same full release command during image build.
- The verification container leaves the production Dockerfile and its cn-only publication guard unchanged.
- Workflow/container configuration contains no deploy, HTTP-live, cache purge, revision recording, rollback, credential, or production mutation step.
  </behavior>
  <action>
Add `.github/workflows/guide-release-verification.yml` using the existing trusted action majors, `permissions: contents: read`, branch-scoped concurrency, `ubuntu-24.04`, Node 24 with npm cache, `npm ci`, and `npm run verify:release -- --keep-artifacts`. Trigger it manually and for pull requests that change Guide content/routes/components/SEO, release/Guide verifiers, build configuration, dependency manifests, or this workflow. Add failure-only artifact upload for `.release-artifacts` with a short retention window and graceful empty-path handling.

Add `Dockerfile.verify` as a build-only verification target based on Node 24. Copy package manifests, run `npm ci`, copy the repository through `.dockerignore`, and run `npm run verify:release` during image build. This creates the export on container-local case-sensitive storage even when the host workspace is case-insensitive. Document the exact fallback command in an English file comment: `docker build --file Dockerfile.verify --tag fastgpt-guide-release-verify .`. Preserve the production `Dockerfile` byte-for-byte per D-05/D-08.

Extend `verify-release.test.js` with a structural regression named `Linux release evidence stays build-only`: assert the workflow runner, read-only permissions, locked install, full release command, failure artifact upload, and the verification Dockerfile's container-local COPY/build gate; assert Phase 8 operational tokens are absent from these new files by checking only executable workflow steps and Docker instructions. Retain lockfile and dependency manifests per D-09 and the Package Legitimacy Audit.
  </action>
  <acceptance_criteria>
- The Ubuntu workflow provides a reproducible case-sensitive full io/cn gate and preserves retained artifacts on failure.
- `docker build --file Dockerfile.verify --tag fastgpt-guide-release-verify .` runs the identical full gate on container-local storage when Docker is available.
- Structural regression proves both evidence paths use the single release command, locked dependencies, and build-only scope.
- Production Dockerfile, deployment workflows, dependency manifests, package-lock, source documents, and Phase 8 surfaces remain unchanged.
- Successful full evidence includes exact nine-page io/cn Guide inventories and two blocking P1 measurements at or below 260 KiB.
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-name-pattern="Linux release evidence stays build-only" scripts/verify-release.test.js &amp;&amp; git diff --check</automated>
    <human-check>Confirm the GitHub Guide Release Verification job or `docker build --file Dockerfile.verify --tag fastgpt-guide-release-verify .` completes on a case-sensitive host and reports io plus cn nine-page Guide inventories and both P1 KiB measurements.</human-check>
  </verify>
  <done>Maintainers have reproducible Linux CI and container-local paths for the exact dual-variant release evidence required by VERIFY-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository source → release coordinator | FAQ and Guide registries, source, routes, and verifier code become build inputs. |
| Variant environment → static export | Explicit owner variables determine locale, host, route inventory, and sitemap output. |
| Generated export → release verdict | Matching artifact checks and the P1 budget convert disposable output into release evidence. |
| Pull request → GitHub Actions/container | Untrusted branch code executes with package install and build permissions. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-07 | Tampering | Cross-variant stale artifacts | high | mitigate | Clear `.next`/`out`, set explicit owner env, run source/build/artifact checks inside one variant lifecycle, then clear again. |
| T-07-08 | Spoofing / Tampering | Variant ownership and Guide inventory | medium | mitigate | Whitelist io/cn, compare exact registry-driven locale/host paths, and retain FAQ cardinality gates. |
| T-07-09 | Repudiation | P1 and Guide release evidence | medium | mitigate | Preserve command/variant diagnostics, surface successful measured KiB, and retain failing artifacts on request. |
| T-07-10 | Elevation of Privilege | Pull-request workflow | high | mitigate | Read-only contents permission, zero deployment credentials, build-only commands, and failure-only artifact upload. |
| T-07-SC | Tampering | npm/container/action supply chain | high | mitigate | `npm ci` consumes the existing lockfile, no package is added, existing trusted action majors are reused, and production Dockerfile remains unchanged. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `scripts/verify-guide-export.js`: registry-driven Guide exported HTML/sitemap verifier.
- `scripts/verify-guide-export.test.js`: exact dual-variant fixtures and complete mutation diagnostics.
- `package.json`: focused Guide artifact commands while retaining `verify:release` as the single aggregate entry.
- `scripts/verify-release.js`: composed FAQ plus Guide source/build/artifact release gate with measured P1 output.
- `scripts/verify-release.test.js`: coordinator ordering, option, evidence, and host-path regressions.
- `.github/workflows/guide-release-verification.yml`: case-sensitive Ubuntu full release evidence.
- `Dockerfile.verify`: container-local case-sensitive full release fallback.
</artifacts_this_phase_produces>

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Maintainers reproduce and inspect complete release-safe Guide exports for both owned variants. | 07-01–03 | COVERED | Standalone artifact tracer, complete regressions, aggregate dual-build evidence. |
| REQ | VERIFY-04 | One repository command validates Guide registry, fidelity, metadata, assets, links, routes, SEO, sitemap, and exported HTML with slug-specific failures. | 07-01–03 | COVERED | Existing source gates plus new artifact verifier compose into `verify:release`. |
| REQ | VERIFY-05 | Case-sensitive cn/io production exports each contain one hub/eight articles and preserve the initial-JS budget. | 07-01–03 | COVERED | Exact inventory fixtures, full Linux/container builds, retained 260 KiB P1 gate. |
| RESEARCH | — | Add one dependency-free standalone Guide artifact verifier with explicit output/variant arguments. | 07-01 | COVERED | Direct command and import-safe pure seam. |
| RESEARCH | — | Reuse route resolution, HTML attribute/entity, JSON-LD, and sitemap parsing patterns. | 07-01, 07-02 | COVERED | Focused verifier and mutation matrix use existing Node conventions. |
| RESEARCH | — | Use exact registry-driven nine-page io/en and cn/zh inventories; exclude adapter copies. | 07-01, 07-02 | COVERED | Exact route and sitemap set comparisons. |
| RESEARCH | — | Run Guide source gates immediately before each clean variant build and artifact checks before cleanup. | 07-03 | COVERED | One fresh lifecycle per selected variant. |
| RESEARCH | — | Keep existing P1 260 KiB assertion and surface its successful measurement. | 07-03 | COVERED | Blocking check retained and successful child evidence recorded. |
| RESEARCH | — | Use Ubuntu CI as preferred case-sensitive host and a dedicated container-local fallback. | 07-03 | COVERED | New workflow plus build-only verification Dockerfile. |
| RESEARCH | — | Add no dependency and preserve package-lock. | 07-01–03 | COVERED | Node built-ins and current locked install only. |
| CONTEXT | D-01 | Extend existing verify:release with deterministic FAQ/Guide source, io, and cn evidence while preserving CLI ergonomics and aggregation. | 07-03 | COVERED | Coordinator remains the single public release gate. |
| CONTEXT | D-02 | Preserve FAQ gates; run Guide source gates before each build and matching export checks within that artifact lifecycle. | 07-03 | COVERED | Explicit prebuild/postbuild placement and regression. |
| CONTEXT | D-03 | Registry-driven exact inventory plus complete rendered surfaces and four-part diagnostics. | 07-01, 07-02 | COVERED | Production verifier and exhaustive mutation coverage. |
| CONTEXT | D-04 | Reuse existing static output and parsing conventions; keep explicit output/variant and source-only independence. | 07-01–03 | COVERED | Standalone CLI plus aggregate composition. |
| CONTEXT | D-05 | Fail closed on case-insensitive full builds and name Linux CI/container/APFS remediation. | 07-02, 07-03 | COVERED | Case fixture, existing host probe, workflow, Dockerfile. |
| CONTEXT | D-06 | Clear artifacts, set explicit owners, io=en and cn=zh, exactly nine owned root paths each. | 07-01–03 | COVERED | Shared inventory and coordinator lifecycle. |
| CONTEXT | D-07 | Retain blocking 260 KiB P1 budget per export and record measured value. | 07-03 | COVERED | Existing assertion plus success evidence formatter. |
| CONTEXT | D-08 | Keep Phase 7 at build/artifact evidence; Phase 8 retains operational delivery/live work. | 07-03 | COVERED | Workflow and container execute repository verification only. |
| CONTEXT | D-09 | Preserve approved Guide bytes, metadata, asset/link mappings, ownership, and registry order without inference. | 07-01–03 | COVERED | Existing source gates plus exact registry-projected artifact expectations. |
| CONTEXT | Deferred | Immutable packaging, deployment revisions, cache purge, rollback, live HTTP, production URL evidence. | — | EXCLUDED | Assigned to Phase 8. |
| CONTEXT | Deferred | Language switcher, hub controls, CMS, more Guides, historical FAQ recovery, programmatic content. | — | EXCLUDED | Future requirements or outside milestone. |
</source_audit>

<risks_dependencies>
## Risks and dependencies

- Plans 07-01/02 must finish the standalone verifier and regression command before coordinator composition.
- The current workspace fails the full host probe; source-only and Node fixtures validate locally, while Ubuntu/container execution supplies full evidence.
- The production Dockerfile intentionally supports cn publication only; the dedicated verification Dockerfile isolates dual-variant proof from deployment behavior.
- Full dual builds may exceed a task-local test window; the workflow/container job owns the long-running evidence while focused automated gates remain fast.
</risks_dependencies>

<verification>
1. Run `npm run verify:guide-export-regression` and `npm run verify:release-regression`.
2. Run `npm run verify:release -- --source-only` on any development volume.
3. Run `npm run verify:release -- --keep-artifacts` on Ubuntu/case-sensitive APFS, or build `Dockerfile.verify`; require io and cn success, exact nine-page Guide counts, nine sitemap Guide URLs, and measured P1 values at or below 260 KiB.
4. Confirm `git diff --exit-code -- package-lock.json Dockerfile` and inspect the phase diff for zero Phase 8 operational work.
</verification>

<success_criteria>
- `npm run verify:release` is the single complete FAQ plus Guide release gate with stable focused commands retained.
- Both source-only and selected-variant ergonomics remain deterministic and regression-covered.
- Fresh case-sensitive io and cn builds each prove one owned Guide hub plus eight owned articles, complete rendered contracts, and exact sitemap membership.
- Each export records a blocking successful P1 measurement at or below 260 KiB.
- Linux CI and container-local fallback reproduce the full result while production delivery surfaces remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/07-dual-variant-release-evidence/07-03-SUMMARY.md` when done.
</output>
