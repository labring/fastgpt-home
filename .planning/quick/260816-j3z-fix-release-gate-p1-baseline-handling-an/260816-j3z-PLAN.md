---
phase: quick
plan: 260816-j3z
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-release.js
  - scripts/verify-faq-metadata.js
  - scripts/verify-release.test.js
  - package.json
  - .planning/STATE.md
  - .planning/phases/04-redirects-and-release-gate/04-01-SUMMARY.md
  - .planning/phases/04-redirects-and-release-gate/04-UAT.md
  - .planning/phases/04-redirects-and-release-gate/04-VERIFICATION.md
autonomous: true
requirements:
  - VERIFY-01
  - VERIFY-03
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The unchanged 260 KiB P1 initial-JavaScript assertion remains a hard release failure, and every failed child check remains in the aggregate failure set through final exit-code selection (D-07)."
    - "The c77cf48 266.9 KiB measurement is reported only as a labeled historical comparison with current size, delta, budget, command, and variant; it never changes a failed P1 result into a passing release result (D-07)."
    - "FAQ metadata HTML verification is owner-aware: io checks the 1,195 approved English workbook mappings at registry canonical slugs, while cn checks all 1,490 published Chinese pages at contentId route keys against their authored Chinese title, description, keywords, H1, and FAQ question (D-10/D-11)."
    - "The aggregate release gate invokes owner-scoped FAQ metadata HTML verification immediately after each matching io or cn build and exits nonzero when either metadata verifier fails (D-07/D-10/D-11)."
    - "A dependency-free Node regression command proves failure preservation, advisory separation, Chinese-only record inclusion, owner route-key selection, and CLI argument validation."
    - "Phase 4 evidence documents identify the hard P1 blocker and the completed CN metadata HTML coverage with exact commands and observed outcomes; prior pass claims that depended on failure reclassification or the CN HTML omission are explicitly corrected."
  artifacts:
    - path: "scripts/verify-release.js"
      provides: "Failure-preserving aggregate result handling, separate P1 baseline advisory reporting, and per-owner metadata HTML invocation"
    - path: "scripts/verify-faq-metadata.js"
      provides: "Variant-aware io approved-metadata and cn authored-metadata HTML verification"
    - path: "scripts/verify-release.test.js"
      provides: "Node built-in regression tests for aggregate failure semantics and owner-aware metadata expectation sets"
    - path: "package.json"
      provides: "Runnable verify:release-regression command without a new dependency"
    - path: ".planning/phases/04-redirects-and-release-gate/04-VERIFICATION.md"
      provides: "Corrected goal-backward release evidence and current P1 disposition"
  key_links:
    - from: "scripts/verify-release.js"
      to: "scripts/verify-p1.js"
      via: "P1 subprocess status remains in failures; baseline extraction appends a separate advisory without mutating failures"
    - from: "scripts/verify-release.js"
      to: "scripts/verify-faq-metadata.js"
      via: "Each variant passes --html --variant io|cn against its immediately preceding build artifact"
    - from: "scripts/verify-faq-metadata.js"
      to: "src/faq/generated-en-metadata.json and src/faq/generated-en-route-registry.json"
      via: "io expectations join approved metadata by contentId and resolve the final canonicalSlug"
    - from: "scripts/verify-faq-metadata.js"
      to: "src/faq/zh.ts, src/faq/w2.ts, and src/faq/w3.ts"
      via: "cn expectations merge unique authored Chinese contentIds and use contentId as the published route key"
    - from: "scripts/verify-release.test.js"
      to: "scripts/verify-release.js and scripts/verify-faq-metadata.js"
      via: "Import-safe exported policy helpers exercised through node:test and node:assert/strict"
    - from: ".planning/phases/04-redirects-and-release-gate/04-VERIFICATION.md"
      to: "npm run verify:release and owner-scoped metadata commands"
      via: "Documented status and evidence mirror actual process exit codes and checked record counts"
---

<objective>
Restore the Phase 4 release gate as a truthful fail-closed signal and complete owner-aware FAQ metadata HTML coverage (D-07/D-10/D-11/D-12).

Purpose: Close the two release-review findings without changing the 260 KiB P1 budget, FAQ content, workbook mapping, routing policy, or build topology. A historical bundle baseline may explain drift; the release decision continues to follow the current hard assertion and every child exit status.
Output: Correct aggregate failure semantics, cn HTML metadata verification against Chinese-owned source data, a dependency-free regression check, and corrected Phase 4 evidence documents.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-redirects-and-release-gate/04-CONTEXT.md
@.planning/phases/04-redirects-and-release-gate/04-01-PLAN.md
@.planning/phases/04-redirects-and-release-gate/04-01-SUMMARY.md
@.planning/phases/04-redirects-and-release-gate/04-UAT.md
@.planning/phases/04-redirects-and-release-gate/04-VERIFICATION.md
@scripts/verify-release.js
@scripts/verify-p1.js
@scripts/verify-faq-metadata.js
@scripts/verify-faq-seo-graph.js
@scripts/generate-faq-metadata.js
@src/faq/index.ts
@src/faq/generated-en-metadata.json
@src/faq/generated-en-route-registry.json
@src/faq/zh.ts
@src/faq/w2.ts
@src/faq/w3.ts
@src/app/[lang]/faq/[id]/page.tsx
@src/lib/faqMetadata.ts
@package.json

<interfaces>
Existing contracts to preserve:

- `scripts/verify-p1.js`: `maxInitialJavaScriptGzipBytes = 260 * 1024`; `verifyInitialJavaScript()` asserts `gzipBytes <= maxInitialJavaScriptGzipBytes` and emits `Initial JavaScript is N KiB gzip, budget is 260 KiB` on failure.
- `scripts/verify-release.js`: `runStep()` appends `{ label, variant, command, output }` to `failures`; final `process.exitCode` is derived from `failures.length`.
- `scripts/generate-faq-metadata.js`: `normalizeFaqMetadataPolicy({ title, description })`, `loadRouteIdentity()`, and the 1,195-record English approved artifact remain the io source of truth.
- `src/faq/index.ts`: English publishes `canonicalSlug`; Chinese publishes the durable `contentId`; the approved workbook overlay applies only to the English catalog.
- `src/app/[lang]/faq/[id]/page.tsx`: both owners normalize `Title`/`Description` through `normalizeFaqMetadata()` and serialize `Keywords` from the selected locale item.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make release failures immutable and verify owner-specific metadata HTML</name>
  <reversibility rating="costly">Release-result classification and owner metadata coverage are shared maintainer contracts; undoing them can recreate a false release approval.</reversibility>
  <files>scripts/verify-release.js, scripts/verify-faq-metadata.js, scripts/verify-release.test.js, package.json</files>
  <behavior>
    - "Regression 1 (D-07): given a failed P1 result containing 267.0 KiB against the 260 KiB budget, baseline reporting leaves the same failure in `failures`, adds one advisory naming c77cf48/266.9 KiB and the +0.1 KiB delta, and produces a nonzero aggregate result."
    - "Regression 2 (D-07): unrelated failed checks remain failures and create no P1 baseline advisory; multiple failures retain their original order and command/variant evidence."
    - "Regression 3 (D-10/D-11): importing `buildOwnerExpectationSet()` from `scripts/verify-faq-metadata.js` returns exactly 1,195 io expectations with registry canonicalSlug route keys and exactly 1,490 cn expectations with contentId route keys."
    - "Regression 4 (D-10/D-11): the imported cn expectation-set helper includes at least one Chinese-only contentId absent from the English route registry and derives Title, Description, Keywords, Question, and Answers from Chinese sources rather than generated-en-metadata.json."
    - "Regression 5: importing `scripts/verify-faq-metadata.js` produces no CLI execution or output; its exported `parseArgs()` accepts `--variant io|cn` only with `--html`, while missing values, unsupported variants, and unknown arguments fail with actionable diagnostics."
  </behavior>
  <action>Start with `scripts/verify-release.test.js` using the Node 18+ built-in `node:test` runner and `node:assert/strict`; make the listed regressions fail before implementation. In `scripts/verify-release.js`, replace the post-variant classification loop that removes matching P1 entries with an import-safe helper that scans only failures added by the current variant, leaves the failure array unchanged, and appends a distinct `P1 historical baseline comparison` advisory containing the current gzip size, the fixed c77cf48 APFS value 266.9 KiB, signed delta, the unchanged 260 KiB budget, command, and variant. The release success message and `process.exitCode` continue to depend exclusively on `failures.length`; retained-artifact behavior must treat the P1 failure like every other variant failure. Guard CLI execution with `require.main === module` and export only the small policy helpers required by the regression test.

Extend `scripts/verify-faq-metadata.js` for D-10/D-11 with `--html --variant io|cn`, defaulting the variant from `NEXT_PUBLIC_SITE_VARIANT` only in HTML mode. Extract and export exactly the minimal test seams `parseArgs(argv, env = process.env)` and `buildOwnerExpectationSet(variant)`. `parseArgs()` owns missing-value, supported-variant, html-mode, environment-default, and unknown-argument validation. `buildOwnerExpectationSet()` loads the committed owner sources and returns deterministic records carrying variant, contentId, route key, Title, Description, Keywords, Question, and Answers; io records join the approved artifact to registry canonicalSlug values, while cn records merge the authored Chinese catalogs at contentId route keys. Make HTML verification consume this same expectation-set helper so the tested contract and CLI path cannot drift. Wrap the existing CLI try/catch in `if (require.main === module)` before assigning `module.exports = { parseArgs, buildOwnerExpectationSet }`, making a plain `require('./scripts/verify-faq-metadata.js')` side-effect free.

Preserve source mode as the 1,195-row English workbook/artifact fidelity check. For io HTML, retain the current artifact-to-contentId join, registry canonicalSlug resolution, normalized approved title/description, serialized approved keywords, English H1, and FAQ JSON-LD question assertions. For cn HTML, parse the existing `src/faq/zh.ts`, `src/faq/w2.ts`, and `src/faq/w3.ts` object literals with the installed TypeScript compiler API, require unique contentIds and all six authored fields (`Question`, `Answers`, `Category`, `Title`, `Description`, `Keywords`), enforce the current 1,490 published-record cardinality, use `/faq/<contentId>` as the HTML route, and compare normalized Chinese Title/Description plus serialized Chinese Keywords, H1, and FAQ JSON-LD question. Require at least one Chinese-only identity so the cn path proves independence from the English-only workbook. Keep record-level diagnostics labeled with variant, contentId, route, source file, and HTML path. In `scripts/verify-release.test.js`, import both exported helpers directly, assert the 1,195 io and 1,490 cn cardinalities, assert every io route key equals its registry canonicalSlug, assert every cn route key equals its contentId, identify and validate a Chinese-only expectation, and cover valid and invalid CLI forms. Add an import-safety subprocess assertion that requiring the verifier exits zero with empty stdout/stderr and performs no source or HTML verification.

In `scripts/verify-release.js`, run `verify:faq-metadata -- --html --variant <variant>` for both io and cn in the existing immediate post-build check list; remove the cn omission message. Keep D-08/D-09 artifact flags, case-sensitive probe, build isolation, D-01–D-06 redirect checks, and D-12 content/routing scope intact. Add `verify:release-regression` to `package.json` as `node --test scripts/verify-release.test.js`; add no dependency and make no change to `scripts/verify-p1.js`, FAQ source data, generated registries, or application routes.</action>
  <verify>
    <automated>node -e "const assert = require('node:assert/strict'); const verifier = require('./scripts/verify-faq-metadata.js'); assert.equal(typeof verifier.parseArgs, 'function'); assert.equal(typeof verifier.buildOwnerExpectationSet, 'function');" &amp;&amp; npm run verify:release-regression &amp;&amp; npm run verify:release -- --source-only &amp;&amp; node scripts/generate-faq-metadata.js --check &amp;&amp; git diff --check -- scripts/verify-release.js scripts/verify-faq-metadata.js scripts/verify-release.test.js package.json</automated>
  </verify>
  <acceptance_criteria>
    <criterion>D-07: A matched P1 budget failure remains byte-for-byte represented in the aggregate failure report, contributes to retained-artifact handling, and forces exit code 1.</criterion>
    <criterion>D-07: Baseline output is a separately labeled advisory with c77cf48, 266.9 KiB, current KiB, signed delta, 260 KiB budget, command, and variant; it contains no release-acceptance semantics.</criterion>
    <criterion>The original `scripts/verify-p1.js` 260 KiB constant and `gzipBytes <= maxInitialJavaScriptGzipBytes` assertion are unchanged.</criterion>
    <criterion>D-10/D-11: io metadata HTML verifies 1,195 approved English mappings at final English canonical slugs.</criterion>
    <criterion>D-10/D-11: cn metadata HTML verifies 1,490 Chinese pages, including Chinese-only identities, at contentId routes against authored Chinese metadata and identity fields.</criterion>
    <criterion>`scripts/verify-faq-metadata.js` guards CLI execution with `require.main === module` and exports `parseArgs` plus `buildOwnerExpectationSet`; requiring the module performs no verification, logging, or exit-code mutation.</criterion>
    <criterion>`scripts/verify-release.test.js` exercises the exported verifier helpers and proves 1,195 io expectations, 1,490 cn expectations, registry canonicalSlug io route keys, contentId cn route keys, Chinese-only inclusion, and valid/invalid CLI argument handling.</criterion>
    <criterion>D-07/D-10: `runVariantChecks()` invokes the matching owner-aware metadata HTML command for each variant and retains any nonzero result in the aggregate failure set.</criterion>
    <criterion>`npm run verify:release-regression` runs through Node built-ins and passes without package installation or build artifacts.</criterion>
  </acceptance_criteria>
  <done>The release coordinator cannot launder a P1 failure, baseline context remains advisory-only, and both owner exports have metadata HTML checks derived from their actual published source of truth.</done>
</task>

<task type="auto">
  <name>Task 2: Correct Phase 4 release evidence and milestone state</name>
  <precondition>A case-sensitive Linux, Docker, or APFS workspace is available for fresh io/cn static-export evidence required by D-09.</precondition>
  <files>.planning/STATE.md, .planning/phases/04-redirects-and-release-gate/04-01-SUMMARY.md, .planning/phases/04-redirects-and-release-gate/04-UAT.md, .planning/phases/04-redirects-and-release-gate/04-VERIFICATION.md</files>
  <action>Run the focused regression and source gate, then build each owner in isolation on the case-sensitive workspace and run `verify:faq-metadata -- --html --variant io|cn` against the matching artifact. Finally run the aggregate release gate and record its real exit status. Update `.planning/STATE.md`, `04-UAT.md`, and `04-VERIFICATION.md` so their current status follows that evidence: the 260 KiB P1 result is a hard release signal, the c77cf48 value is historical comparison context, and cn metadata HTML coverage is backed by the 1,490-record Chinese authored catalog. With the current measured 267.0 KiB artifact, record the release gate as open/blocked by P1 until a later product optimization satisfies 260 KiB; keep source checks, redirect evidence, owner build counts, and metadata coverage reported independently by their actual results. Update `04-01-SUMMARY.md` with a dated corrective-review note that preserves the historical commit record while superseding its two inaccurate conclusions: P1 failure reclassification and the cn metadata HTML omission. Include exact commands, exit codes, io/cn checked counts, and the quick-task plan identifier. Keep D-12 deployment, content, routing, generated data, and UI scope unchanged.</action>
  <verify>
    <automated>npm run verify:release-regression &amp;&amp; npm run verify:release -- --source-only &amp;&amp; rg -n "260 KiB|266\.9 KiB|1,195|1,490|verify:faq-metadata|260816-j3z" .planning/STATE.md .planning/phases/04-redirects-and-release-gate/04-01-SUMMARY.md .planning/phases/04-redirects-and-release-gate/04-UAT.md .planning/phases/04-redirects-and-release-gate/04-VERIFICATION.md &amp;&amp; git diff --check -- .planning/STATE.md .planning/phases/04-redirects-and-release-gate/04-01-SUMMARY.md .planning/phases/04-redirects-and-release-gate/04-UAT.md .planning/phases/04-redirects-and-release-gate/04-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    <criterion>All four evidence documents distinguish hard P1 gate status from historical baseline comparison and state the aggregate's actual exit result.</criterion>
    <criterion>All four evidence documents describe io as the 1,195-row approved English metadata surface and cn as the 1,490-page authored Chinese metadata surface.</criterion>
    <criterion>`04-VERIFICATION.md` and `04-UAT.md` no longer claim a passing full release while the current P1 assertion exits nonzero; their status/count fields match the recorded commands.</criterion>
    <criterion>`04-01-SUMMARY.md` retains the original execution history and contains an explicit correction linked to quick plan 260816-j3z.</criterion>
    <criterion>STATE identifies the current release decision and next concrete blocker without changing the deferred deployment/content scope.</criterion>
  </acceptance_criteria>
  <done>Project state, UAT, summary, and verification evidence accurately represent the repaired gate, complete cn HTML metadata coverage, and the remaining measured P1 release blocker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Child verifier exit/output → aggregate release result | Subprocess status and diagnostics determine whether a maintainer may approve a release. |
| Build variant environment → exported HTML expectations | `io` and `cn` publish different route keys and metadata sources under different owner domains. |
| Authored/generated FAQ sources → metadata verifier | Committed TypeScript catalogs, route registry, and workbook snapshot define distinct expected record sets. |
| Verification output → planning status | Evidence documents can authorize release work only when they preserve actual command outcomes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q-J3Z-01 | Tampering | `scripts/verify-release.js` failure classification | high | mitigate | Keep child failures immutable, derive final exit solely from failure count, and regression-test the P1 match path. |
| T-Q-J3Z-02 | Spoofing | `scripts/verify-faq-metadata.js` owner selection | high | mitigate | Require explicit html variant, derive io/cn expectation sets from their owner sources, and assert route keys plus cardinalities. |
| T-Q-J3Z-03 | Integrity | cn Chinese-only metadata coverage | high | mitigate | Merge all three published Chinese source files by unique contentId and require a Chinese-only fixture plus 1,490 pages. |
| T-Q-J3Z-04 | Repudiation | Phase 4 evidence documents | medium | mitigate | Record exact commands, exit codes, counts, baseline identity, and corrective quick-plan reference. |
| T-Q-J3Z-05 | Supply chain | Regression and source parsing | medium | mitigate | Use Node built-ins and the already-installed TypeScript dependency; add no package or runtime network/workbook access. |
</threat_model>

<source_coverage>
## Source Coverage Audit

| Source | ID | Feature/Requirement | Task | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Legacy migration and the complete static SEO surface are truthfully verified as release-ready | 1–2 | COVERED | Task 1 fixes gate semantics and owner HTML coverage; Task 2 aligns release status with evidence. |
| REQ | VERIFY-01 | One aggregate command reports every failed check and exits nonzero | 1 | COVERED | Failure preservation and regression tests directly close the P1 laundering finding. |
| REQ | VERIFY-03 | Exported FAQ HTML verifies applicable metadata and identity for every final route | 1 | COVERED | io checks 1,195 approved mappings; cn checks 1,490 authored Chinese pages. |
| RESEARCH | — | Compose existing P0/P1/P2/i18n and metadata/SEO verifiers per owner | 1 | COVERED | Existing release ordering remains; metadata HTML becomes owner-aware for both variants. |
| CONTEXT | D-01–D-06 | Registry redirects, collision denies, one-hop/query behavior | 1 | COVERED | Redirect implementation is untouched and source-only regression continues to execute its focused verifier. |
| CONTEXT | D-07 | Every aggregate child failure remains visible and forces nonzero exit | 1 | COVERED | P1 stays in failures; baseline is separate advisory data. |
| CONTEXT | D-08 | Preserve source-only and artifact-retention diagnostics | 1 | COVERED | Existing CLI flags and failure artifact behavior remain wired to the unchanged failure set. |
| CONTEXT | D-09 | Full release evidence requires a case-sensitive host | 2 | COVERED | Task 2 carries an explicit precondition and records case-sensitive commands. |
| CONTEXT | D-10 | Verify isolated io and cn artifacts | 1–2 | COVERED | Both variants run owner-scoped metadata HTML checks immediately after build. |
| CONTEXT | D-11 | Exported HTML is the release contract, with approved metadata where mapped | 1 | COVERED | English workbook scope and Chinese authored scope are explicit and exhaustive. |
| CONTEXT | D-12 | Preserve routing/content/deployment/UI scope | 1–2 | COVERED | Plans modify verification and evidence files only. |
| DEFERRED | — | Live CDN checks, historical recovery, content rewrites, metadata policy changes, UI work | — | EXCLUDED | Remain outside the quick task by D-12. |
</source_coverage>

<verification>

- [ ] `npm run verify:release-regression` passes and covers the P1 failure/advisory and io/cn expectation-set contracts.
- [ ] `npm run verify:release -- --source-only` passes on the development filesystem.
- [ ] On a case-sensitive io build: `npm run verify:faq-metadata -- --html --variant io` reports 1,195 mapped English pages.
- [ ] On a case-sensitive cn build: `npm run verify:faq-metadata -- --html --variant cn` reports 1,490 authored Chinese pages, including Chinese-only records.
- [ ] On the current 267.0 KiB artifact, `npm run verify:release -- --variant io` and `--variant cn` retain `P1 HTML verification` in the failure report, print a separate c77cf48 baseline comparison, retain artifacts when requested, and exit 1.
- [ ] When the current initial JavaScript eventually satisfies 260 KiB, the same aggregate reaches its remaining checks without any baseline-driven status override.
- [ ] `git diff -- src/faq/en.ts src/faq/zh.ts src/faq/w2.ts src/faq/w3.ts src/faq/generated-en-metadata.json src/faq/generated-en-route-registry.json scripts/verify-p1.js` remains empty.
- [ ] `git diff --check` passes for all modified files.

</verification>

<success_criteria>

- A failed P1 process can never be removed or downgraded by baseline reporting.
- The historical baseline remains visible as diagnostic context and has zero authority over the release exit code.
- Both owner variants execute metadata HTML verification against the correct source and route identity, including every Chinese-only page.
- One fast Node regression command guards the two review findings without new dependencies or static builds.
- Phase 4 and milestone evidence match the repaired gate's actual status and retain an auditable correction trail.

</success_criteria>

<output>
After completion, create `.planning/quick/260816-j3z-fix-release-gate-p1-baseline-handling-an/260816-j3z-SUMMARY.md`.
</output>
