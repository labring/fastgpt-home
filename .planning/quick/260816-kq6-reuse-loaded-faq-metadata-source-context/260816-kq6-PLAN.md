---
phase: quick
plan: 260816-kq6
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-faq-metadata.js
  - scripts/verify-release.test.js
autonomous: true
requirements:
  - VERIFY-03
estimate:
  tokens: 7000
  raw_tokens: 7000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "A metadata-verifier CLI run loads and validates the English FAQ artifact, authored records, and route identity once, then reuses that same source context for optional HTML verification."
    - "HTML expectation generation preserves the exact 1,195 io records, 1,490 cn records, owner route keys, metadata fields, identity assertions, and diagnostics already covered by the release regression."
    - "Direct callers of the exported buildOwnerExpectationSet(variant) helper keep working through its internal source-context fallback."
    - "An HTML-mode regression drives the real CLI entry point across all 1,195 io fixtures and fails unless that child process reads the approved metadata artifact exactly once."
  artifacts:
    - path: "scripts/verify-faq-metadata.js"
      provides: "One validated source context threaded from main through HTML verification to owner expectation generation"
    - path: "scripts/verify-release.test.js"
      provides: "A temporary-fixture HTML CLI regression that observes single-load context reuse while retaining direct-helper fallback coverage"
  key_links:
    - from: "main()"
      to: "verifyHtmlExport()"
      via: "passes the sourceContext returned by the single loadSourceContext() call"
    - from: "verifyHtmlExport()"
      to: "buildOwnerExpectationSet()"
      via: "forwards the same sourceContext while preserving the exported helper's fallback"
    - from: "scripts/verify-release.test.js"
      to: "main() -> verifyHtmlExport() -> buildOwnerExpectationSet()"
      via: "spawns --html --variant io with complete temporary HTML fixtures and a preload read counter that requires one approved-artifact read"
---

<objective>
Reuse the FAQ metadata source context already loaded by `main()` when HTML verification builds owner expectations.

Purpose: Remove the duplicate English artifact read and validation from HTML-mode CLI runs while preserving the complete VERIFY-03 assertion surface.
Output: A surgical call-chain refactor in `scripts/verify-faq-metadata.js` plus an HTML-mode CLI regression in `scripts/verify-release.test.js`, verified alongside the existing source gates.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/quick/260816-j3z-fix-release-gate-p1-baseline-handling-an/260816-j3z-SUMMARY.md
@scripts/verify-faq-metadata.js
@scripts/verify-release.test.js

<interfaces>
Existing contracts to preserve:

- `loadSourceContext()` reads and validates `{ artifact, faqRecords, routeIdentity }`.
- `main()` already calls `loadSourceContext()` before source checks and the optional HTML branch.
- `verifyHtmlExport(variant)` currently obtains expectations, checks case-insensitive export collisions, verifies every HTML record, and returns the checked count.
- `buildOwnerExpectationSet(variant)` is exported and directly exercised by `scripts/verify-release.test.js`; io uses artifact + authored English data + route identity, while cn uses authored Chinese records + route identity.
- `module.exports = { parseArgs, buildOwnerExpectationSet }` is the import-safe verifier surface.
- HTML resolution is rooted at `out/`; a regression can preserve any existing directory, create exact temporary FAQ HTML fixtures there, run the CLI child process, and restore the prior directory in `finally`.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Thread the validated source context through HTML expectation generation and prove CLI reuse</name>
  <files>scripts/verify-faq-metadata.js, scripts/verify-release.test.js</files>
  <action>Refactor `main()` to retain the result of its single `loadSourceContext()` call as `sourceContext`, destructure that object for the existing catalog-overlay and failure-diagnostic checks, and pass the exact object to `verifyHtmlExport(options.variant, sourceContext)`. Extend `verifyHtmlExport(variant, sourceContext)` to forward it to `buildOwnerExpectationSet(variant, sourceContext)`. Extend `buildOwnerExpectationSet(variant, sourceContext)` so the supported-variant assertion still runs first, then use `sourceContext ?? loadSourceContext()` as the standalone-call fallback: the io branch consumes artifact, faqRecords, and routeIdentity; the cn branch consumes routeIdentity alongside its existing Chinese source parsing. Keep every current cardinality, uniqueness, metadata, route-key, collision, H1, JSON-LD, and record-level diagnostic assertion, along with the existing `parseArgs` and `buildOwnerExpectationSet` exports.

Add one Node test to `scripts/verify-release.test.js` that exercises the real HTML CLI path. Retain the current direct `buildOwnerExpectationSet('io'|'cn')` test unchanged so standalone fallback remains covered. In the new test, preserve any existing `out/` directory under a test-owned temporary directory beside the repository, use the exported io expectation set to write all 1,195 matching `out/faq/{routeKey}.html` fixtures with normalized title, description, serialized keywords, exactly one H1, and a JSON-LD Question name, then spawn `node --require {temporary-read-counter} scripts/verify-faq-metadata.js --html --variant io`. The temporary preload module wraps `fs.readFileSync`, counts reads of `src/faq/generated-en-metadata.json` in that child, and makes the child fail at process exit unless the count is exactly one; this distinguishes the threaded `main() -> verifyHtmlExport() -> buildOwnerExpectationSet()` path from the former fallback reload. Assert exit status zero and the `1,195 FAQ pages` success diagnostic. Use `finally` to remove generated fixtures, restore the preserved `out/` directory exactly, and remove the test-owned temporary directory after success or failure. Reuse Node built-ins and the existing metadata policy helper. Confine committed changes to the verifier and regression test; preserve package manifests, FAQ data, generated registries, application routes, and UI files.</action>
  <verify>
    <automated>node scripts/verify-faq-metadata.js &amp;&amp; npm run verify:release-regression &amp;&amp; npm run verify:release -- --source-only &amp;&amp; git diff --check -- scripts/verify-faq-metadata.js scripts/verify-release.test.js</automated>
  </verify>
  <done>`main()` performs one source-context load and passes that object through `verifyHtmlExport()` into `buildOwnerExpectationSet()`; the HTML-mode child checks all 1,195 io fixtures and observes exactly one approved-artifact read; direct helper calls retain their fallback; all existing source checks and owner-expectation regressions pass with the same counts, route keys, fields, and diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Committed FAQ sources and generated artifacts → HTML expectations | Repository-owned data is validated once before it drives exhaustive static HTML assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q-KQ6-01 | Tampering | `main()` → `verifyHtmlExport()` → `buildOwnerExpectationSet()` | medium | mitigate | Pass the validated context object through both calls and preserve every existing expectation-set and HTML assertion. |
| T-Q-KQ6-02 | Repudiation | Owner expectation output | low | mitigate | Run the existing Node regression that asserts io/cn counts, owner route keys, and Chinese-only source coverage. |
</threat_model>

<source_coverage>
## Source Coverage Audit

| Source | ID | Feature/Requirement | Task | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Reuse loaded FAQ metadata source context in HTML verification | 1 | COVERED | The call chain receives one validated context object. |
| REQ | VERIFY-03 | Exported FAQ HTML verifies metadata and identity for every applicable final route | 1 | COVERED | Existing expectation and HTML assertions remain intact; a real io HTML-mode CLI run covers all 1,195 mapped routes. |
| RESEARCH | — | No research artifact; this is a Level 0 internal refactor using established functions | — | EXCLUDED | The repository already contains the implementation pattern and regression seam. |
| CONTEXT | — | Preserve assertions/tests, dependencies, product behavior, and UI behavior | 1 | COVERED | One verification script changes; existing tests execute unchanged. |
</source_coverage>

<verification>

- [ ] `node scripts/verify-faq-metadata.js` passes the complete source snapshot and diagnostic checks.
- [ ] `npm run verify:release-regression` passes the existing io/cn expectation-set and import-safety coverage, then drives `--html --variant io` through all 1,195 temporary fixtures while observing one approved-artifact read in the CLI child.
- [ ] `npm run verify:release -- --source-only` passes the aggregate source gate.
- [ ] The diff is confined to `scripts/verify-faq-metadata.js` plus `scripts/verify-release.test.js` and passes `git diff --check`.

</verification>

<success_criteria>

- HTML-mode CLI execution validates source context once and reuses it across source and HTML checks, proven by the one-read preload assertion on the real child-process path.
- io and cn expectations preserve their current counts, route identities, metadata sources, and assertions.
- The exported helper retains direct-call compatibility and the repository gains zero dependencies or product/UI changes.

</success_criteria>

<output>
After completion, create `.planning/quick/260816-kq6-reuse-loaded-faq-metadata-source-context/260816-kq6-SUMMARY.md`.
</output>
