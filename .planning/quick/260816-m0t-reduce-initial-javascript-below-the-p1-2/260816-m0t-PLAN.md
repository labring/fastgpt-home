---
phase: quick
plan: 260816-m0t
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/contact/ConsultationProvider.tsx
  - src/components/contact/contactCopy.ts
  - src/components/contact/dialogCopy.ts
  - src/app/DeferredSiteIntegrations.tsx
  - src/app/DeferredSiteIntegrationsContent.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - VERIFY-01
  - VERIFY-02
  - VERIFY-03
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "The production homepage initial JavaScript passes the existing P1 check at or below the unchanged 260 KiB gzip budget."
    - "Opening any intercepted consultation link renders the existing dialog immediately, announces a localized polite loading status while the form chunk loads, and then exposes the unchanged modal form workflow."
    - "Dialog close controls, Escape handling, focus trapping, body-scroll restoration, and trigger-focus restoration retain their current behavior before, during, and after form loading."
    - "The dedicated /contact route retains its direct ContactForm import and production static form output."
    - "The release regression and aggregate source-only gate continue to pass after the bundle split."
    - "Optional analytics and attribution integrations remain lazy and retain their existing runtime behavior."
    - "The fixed dark theme remains present in server HTML and the homepage light override remains intact without a client theme runtime."
  artifacts:
    - path: "src/components/contact/ConsultationProvider.tsx"
      provides: "A module-scope next/dynamic boundary for the modal-only ContactForm plus an accessible localized loading renderer"
    - path: "src/components/contact/contactCopy.ts"
      provides: "English, Simplified Chinese, and Traditional Chinese loading labels within the existing contact-copy contract"
    - path: "src/components/contact/dialogCopy.ts"
      provides: "Published-locale dialog shell copy kept outside the full form metadata chunk"
    - path: "src/app/DeferredSiteIntegrations.tsx"
      provides: "A client-only dynamic boundary for optional analytics and attribution integrations"
    - path: "src/app/DeferredSiteIntegrationsContent.tsx"
      provides: "The unchanged optional integration mounts loaded after idle"
  key_links:
    - from: "ConsultationProvider"
      to: "src/components/contact/ContactForm.tsx"
      via: "a module-scope next/dynamic import configured with ssr: false"
    - from: "the dynamic loading renderer"
      to: "getContactCopy(document.documentElement.lang)"
      via: "the active document language selects the existing published contact locale before announcing the loading label"
    - from: "src/components/contact/ContactPage.tsx"
      to: "src/components/contact/ContactForm.tsx"
      via: "the existing direct import and page-variant render remain unchanged"
    - from: "src/app/layout.tsx"
      to: "src/app/DeferredSiteIntegrations.tsx"
      via: "one client boundary replaces the five optional integration mounts"
    - from: "out/index.html"
      to: "scripts/verify-p1.js"
      via: "the unchanged verifier enumerates initial /_next/*.js scripts, gzips each emitted asset, and enforces 260 KiB"
---

<objective>
Defer the modal-only contact form from the homepage initial JavaScript while preserving the existing consultation and dedicated contact-page experiences.

Purpose: Clear the hard P1 release blocker through the smallest established client-side split point and retain the fixed performance policy.
Output: A top-level `next/dynamic` modal-form boundary with a localized accessible loading state plus a client-only boundary for optional integrations, proven by the production artifact and release gates.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/quick/260816-m0t-reduce-initial-javascript-below-the-p1-2/260816-m0t-RESEARCH.md
@src/components/contact/ConsultationProvider.tsx
@src/components/contact/ContactPage.tsx
@src/components/contact/ContactForm.tsx
@src/components/contact/contactCopy.ts
@scripts/verify-p1.js

<interfaces>
Existing contracts to preserve:

- `ConsultationProvider({ children, defaultLocale })` derives the active locale, intercepts same-tab contact links, portals one `role="dialog"`, traps focus, closes on Escape/backdrop/button, restores body overflow, and returns focus to the trigger.
- `ContactForm({ locale, variant = 'page', onDone })` uses `variant="modal"` only inside the provider; the dedicated `ContactPage` directly imports it and relies on the default page variant.
- `getContactCopy(locale)` resolves the contact-published locale set `en`, `zh`, and `zh-hant`; its returned object already supplies every dialog and form label.
- `next/dynamic` accepts a module-scope loader plus `{ ssr: false, loading }` inside this existing Client Component boundary.
- `npm run verify:p1` reads `out/index.html`, resolves every initial `/_next/*.js` asset, sums gzip bytes, and enforces `260 * 1024`.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Split the modal contact form and prove the complete release path</name>
  <files>src/components/contact/ConsultationProvider.tsx, src/components/contact/contactCopy.ts, src/components/contact/dialogCopy.ts</files>
  <action>Keep the full form metadata contract in `src/components/contact/contactCopy.ts` for `ContactForm`, and create/maintain `src/components/contact/dialogCopy.ts` with the published `en`, `zh`, and `zh-hant` title, subtitle, eyebrow, close, and loading strings used by the dialog shell. In `src/components/contact/ConsultationProvider.tsx`, replace the static modal-form import with a module-scope `next/dynamic` component that imports the existing default export from `@/components/contact/ContactForm` and sets `ssr: false`; read shell text from `getDialogCopy` so the full form copy stays out of the initial chunk. Add one compact loading renderer in this provider; it resolves the active published copy from `document.documentElement.lang` with an English server-safe fallback and renders the new label inside the form viewport using `role="status"`, `aria-live="polite"`, and `aria-atomic="true"`. Give the loading surface enough height to keep the dialog stable while the chunk arrives. Render this dynamic component with the current `locale`, `variant="modal"`, and `onDone={close}` props under the existing `mounted &amp;&amp; open` portal gate.

Preserve the provider's consultation-link interception, dialog structure and labels, close button, backdrop handling, focus trap, Escape behavior, body-scroll cleanup, and return-focus cleanup exactly. Preserve `src/components/contact/ContactPage.tsx` and its direct `ContactForm` import so `/contact` keeps its page-specific static bundle and render. Run the production build and type check for this split, then inspect `git diff --stat` and the full diff line by line; confirm the declared source files contain the complete product diff and confirm the P1 budget constant, script-selection logic, package manifests, contact page, and form implementation are unchanged. Final P1 and release-gate proof belongs to Task 2 after both bundle splits are present.</action>
  <verify>
    <automated>npm run build &amp;&amp; npx tsc --noEmit &amp;&amp; git diff --check -- src/components/contact/ConsultationProvider.tsx src/components/contact/contactCopy.ts src/components/contact/dialogCopy.ts &amp;&amp; git diff --exit-code -- src/components/contact/ContactPage.tsx src/components/contact/ContactForm.tsx scripts/verify-p1.js package.json package-lock.json src/app/GoogleAnalytics.tsx src/app/BaiDuAnalytics.tsx src/app/ClarityAnalytics.tsx src/app/RybbitAnalytics.tsx src/app/LeadAttribution.tsx &amp;&amp; git diff --stat &amp;&amp; git diff -- src/components/contact/ConsultationProvider.tsx src/components/contact/contactCopy.ts src/components/contact/dialogCopy.ts</automated>
    <human-check>With browser network throttling enabled, open a consultation link on `/` in each published contact locale. Confirm the dialog title and close control appear immediately, the localized loading status is announced, the form becomes keyboard-usable, Tab remains inside the dialog, Escape and the close button restore focus to the trigger, and `/contact` still renders the complete form.</human-check>
  </verify>
  <done>The first modal open fetches ContactForm on demand behind a localized polite status; all existing dialog and form behaviors remain intact; `/contact` retains its direct form path; the production build, type check, protected-surface diff gate, and whitespace validation pass for this split.</done>
</task>

<task type="tracer">
  <name>Task 2: Defer optional site integrations from the shared initial graph</name>
  <files>src/app/DeferredSiteIntegrations.tsx, src/app/DeferredSiteIntegrationsContent.tsx, src/app/layout.tsx</files>
  <action>Create a small `'use client'` outer boundary in `src/app/DeferredSiteIntegrations.tsx` that waits for `runAfterIdle` and then imports `./DeferredSiteIntegrationsContent`; render the imported content component only after that idle callback. Create `src/app/DeferredSiteIntegrationsContent.tsx` as the loaded client module containing the five existing mounts (`GoogleAnalytics`, `BaiDuAnalytics`, `ClarityAnalytics`, `RybbitAnalytics`, and `LeadAttribution`) without changing those implementation files or their contracts. Replace the five direct layout imports/mounts with one `DeferredSiteIntegrations` mount. Keep each integration's existing `next/script` strategy and LeadAttribution idle import untouched so runtime timing and optional configuration behavior remain stable. Replace the unused fixed `ThemeProvider` runtime in `src/app/layout.tsx` with static `className="dark"` and `style={{ colorScheme: 'dark' }}` on `<html>`, preserving the existing `HomeThemeFix` light override and leaving MotionProvider/ConsultationProvider unchanged. Rebuild both io and cn variants, prove P1 is at or below 260 KiB, and run the existing release regression/source-only gates plus protected-file and diff checks.</action>
  <verify>
    <automated>NEXT_PUBLIC_SITE_VARIANT=io NEXT_PUBLIC_HOME_URL=https://fastgpt.io npm run build &amp;&amp; node -e "const fs=require('fs');const tag=fs.readFileSync('out/index.html','utf8').match(/&lt;html[^&gt;]*&gt;/)?.[0]||'';if(!/class=\"dark\"/.test(tag)||!/color-scheme:\s*dark/.test(tag))throw Error('io html theme contract failed')" &amp;&amp; NEXT_PUBLIC_SITE_VARIANT=io NEXT_PUBLIC_HOME_URL=https://fastgpt.io npm run verify:p1 &amp;&amp; NEXT_PUBLIC_SITE_VARIANT=cn NEXT_PUBLIC_HOME_URL=https://fastgpt.cn npm run build &amp;&amp; node -e "const fs=require('fs');const tag=fs.readFileSync('out/index.html','utf8').match(/&lt;html[^&gt;]*&gt;/)?.[0]||'';if(!/class=\"dark\"/.test(tag)||!/color-scheme:\s*dark/.test(tag))throw Error('cn html theme contract failed')" &amp;&amp; NEXT_PUBLIC_SITE_VARIANT=cn NEXT_PUBLIC_HOME_URL=https://fastgpt.cn npm run verify:p1 &amp;&amp; npm run verify:release-regression &amp;&amp; npm run verify:release -- --source-only &amp;&amp; git diff --check -- src/app/DeferredSiteIntegrations.tsx src/app/DeferredSiteIntegrationsContent.tsx src/app/layout.tsx &amp;&amp; git diff --exit-code -- src/components/contact/ContactPage.tsx src/components/contact/ContactForm.tsx scripts/verify-p1.js package.json package-lock.json src/app/GoogleAnalytics.tsx src/app/BaiDuAnalytics.tsx src/app/ClarityAnalytics.tsx src/app/RybbitAnalytics.tsx src/app/LeadAttribution.tsx &amp;&amp; git diff --stat</automated>
    <human-check>Confirm optional analytics still load after hydration according to their existing lazyOnload/idle behavior, while the homepage renders light after HomeThemeFix runs, navigation restores the dark shell on non-home pages, the contact dialog remains interactive, and `/contact` remains available.</human-check>
  </verify>
  <done>The optional integrations are absent from the homepage initial script graph, the fixed dark theme is server-rendered with the homepage light override intact, their existing runtime components remain unchanged, and explicit io and cn builds pass the fixed P1 budget; release regression and source-only gates pass.</done>
</task>

</tasks>

<prohibitions>

- `scripts/verify-p1.js`, its `260 * 1024` budget, and its initial-script enumeration remain byte-for-byte unchanged.
- `src/components/contact/ContactPage.tsx` and `src/components/contact/ContactForm.tsx` remain byte-for-byte unchanged.
- The five integration component implementations remain byte-for-byte unchanged; only their shared mount boundary may change.
- `package.json`, `package-lock.json`, installed dependencies, and build tooling remain unchanged.
- Homepage sections, Framer Motion boundaries, FAQ data, generated registries, routes, metadata, and redirect policy remain outside this product diff.
- MotionProvider and ConsultationProvider remain in the shared layout wrapper; the unused ThemeProvider runtime is removed in favor of equivalent static dark HTML state, and optional integrations use the new deferred boundary.
- The dynamic boundary stays at module scope inside `ConsultationProvider`; the server-rendered homepage composition retains its current imports and structure.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static homepage shell -> browser-loaded contact chunk | A user action crosses from the release-built initial shell to an on-demand JavaScript asset before the CRM form becomes interactive. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q-M0T-01 | Tampering | Homepage initial-script graph | medium | mitigate | Run a fresh production build and the unchanged artifact-based P1 verifier, then protect the verifier and package manifests with a zero-diff gate. |
| T-Q-M0T-02 | Denial of Service | On-demand ContactForm chunk | low | accept | The dialog retains an announced loading state while normal chunk delivery completes, and the dedicated `/contact` route remains an independent direct-form path. |
</threat_model>

<source_coverage>
## Source Coverage Audit

| Source | ID | Feature/Requirement | Task | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Reduce homepage initial JavaScript below the fixed P1 limit while preserving contact behavior | 1-2 | COVERED | The modal-only form and optional integrations become on-demand chunks and the emitted homepage artifact must pass the existing gate. |
| REQ | VERIFY-01 | One repository release command retains correct source and regression behavior | 1-2 | COVERED | The focused release regression and aggregate source-only gate run after both splits. |
| REQ | VERIFY-02 | The production static export succeeds | 1-2 | COVERED | `npm run build` precedes artifact measurement and must complete successfully for both variants. |
| REQ | VERIFY-03 | Exported FAQ HTML and SEO assertions retain their release coverage | 1-2 | COVERED | The source-only aggregate release gate runs unchanged; FAQ and SEO surfaces stay outside the diff. |
| RESEARCH | — | Use module-scope next/dynamic with ssr: false at the existing client provider | 1 | COVERED | The modal form boundary follows the researched Next.js loading contract. |
| RESEARCH | — | Retain an accessible localized in-dialog loading state | 1 | COVERED | Published contact copy gains one loading label rendered through a polite atomic status. |
| RESEARCH | — | Preserve the direct `/contact` form and every modal accessibility control | 1 | COVERED | Protected files have a zero-diff gate and the manual browser check covers focus and close behavior. |
| RESEARCH | — | Keep the P1 budget, dependencies, homepage sections, and route/data surfaces unchanged | 1-2 | COVERED | Prohibitions plus protected-surface and package diff gates enforce the boundary. |
| PLAN REVISION | — | Defer only optional integrations from the shared initial graph | 2 | COVERED | A client-only boundary changes the mount location while leaving the five integration implementations and their runtime strategies unchanged. |
| CONTEXT | — | No quick-task CONTEXT.md exists | — | EXCLUDED | The research artifact and parent task provide the complete locked scope. |
</source_coverage>

<verification>

- [ ] `npm run build` completes the production static export.
- [ ] `npm run verify:p1` passes and reports homepage initial JavaScript at or below the unchanged 260 KiB gzip budget.
- [ ] `npm run verify:release-regression` passes its dependency-free Node regression suite.
- [ ] `npm run verify:release -- --source-only` passes the aggregate release source gate.
- [ ] The protected files and package manifests have zero product diff; all declared source files pass `git diff --check` and receive a full diff review.
- [ ] Browser throttling exposes a localized announced loading state, then the unchanged modal form; focus containment and restoration work, and `/contact` renders its direct form.

</verification>

<success_criteria>

- The fixed P1 budget changes from a release blocker to a passing production-artifact gate through modal and optional-integration code deferral.
- Consultation users receive the same dialog and form workflow with a localized accessible loading transition on first open.
- Dedicated contact-page rendering, FAQ/SEO output, release policy, dependencies, and bundle-budget policy remain unchanged.

</success_criteria>

<output>
After completion, create `.planning/quick/260816-m0t-reduce-initial-javascript-below-the-p1-2/260816-m0t-SUMMARY.md`.
</output>
