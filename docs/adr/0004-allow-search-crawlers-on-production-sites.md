# Allow search crawlers on both production sites

Both fastgpt.cn and fastgpt.io allow standards-compliant search crawlers to access all public pages. Canonical URLs, cross-domain hreflang annotations, and site-specific sitemaps express locale ownership, ensuring every crawler can read the complete localization signals.
