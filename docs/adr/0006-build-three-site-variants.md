# Build three site variants

Static builds use `cn`, `io`, and `preview` Site Variants as the single input for route generation, metadata, robots directives, sitemaps, and redirects. The `cn` variant publishes Simplified Chinese, the `io` variant publishes International Site locales, and the `preview` variant serves every supported locale on its own hostname with search indexing, production redirects, and sitemap generation disabled.
